# Project state

- Active development repository: `d-yacenko/secretary-prerelease`.
- Canonical Executor bootstrap is mandatory and documented in `docs/executor_bootstrap.md`. Wrong-origin/dirty/stale local checkouts are never repaired in place; use a fresh temporary clone of `https://github.com/d-yacenko/secretary-prerelease.git`. Git/SSH readiness is plumbing, not a product phase. A bootstrap/pre-SSH stop must be reported once as a sanitized Executor blocker and is not evidence about Secretary/Telegram/production.
- Production/runtime work relies on the pre-existing Executor/workstation SSH credential integration plus the pinned target contract. Do not create/copy/request new production private-key material as a workaround.
- Production canonical Git repository: `d-yacenko/secretary-prerelease`.
- Architect encrypted recovery context is stored in the same repository as `secretary_architect_context_encrypted.md`; plaintext is not committed.
- Production application/runtime: `296b4735f9473ea60ef22f1827ed94260603128e`.
- Production branch/ref: `296b4735f9473ea60ef22f1827ed94260603128e`.
- Production Alembic: `0047 / 0047`.
- Production health: PASS.
- Unified per-user generative model selector: LIVE.
- Google Sync Resilience A: COMPLETE / DEPLOYED / RUNTIME VERIFIED.
- Production Deploy Contract v2: MERGED / MANDATORY FOR NORMAL SCHEMA-NEUTRAL PRODUCTION DEPLOYMENT.
- Yandex transient retry hotfix: DEPLOYED / RUNTIME VERIFIED.
- Telegram A1/A2: MERGED TO MAIN / NOT PRODUCTION DEPLOYED.
- Telegram A3: ACCEPTED / MAIN / NOT PRODUCTION DEPLOYED.
- Telegram A4.1–A4.4: ACCEPTED / MAIN / NOT PRODUCTION DEPLOYED.
- Telegram Integration Gate I1: ACCEPTED.
- Production Line Reconciliation R1: ACCEPTED.
- Historical Telegram migration chain remains `0041 -> 0042 -> 0043 -> 0044 -> 0045 -> 0046`. Assistant revision `0047` is applied in production.
- Production Migration Rollout M1 harness: ACCEPTED at exact SHA `917eebed4b0ffb6bf55f573a24d99d00dc1f8fbb`.
- Telegram platform credentials are provisioned in production `/opt/secretary/.env`; values/hashes are not committed or logged.
- M2 production readiness retry: READY.
- After the successful M3 cutover, production runtime/ref was `8091736337689b68b4510126e74d9e409397f696`.
- Production Migration Rollout M3: SUCCESS. Production ref/runtime is exact `8091736337689b68b4510126e74d9e409397f696`; production Alembic is `0046`; api/worker/db healthy; DB container/volume and `.env` preserved; no Telegram login/history import occurred; Bot API untouched.
- Telegram MTProto M4 activation: human login including Telegram 2FA completed through the exact-release client; one manual group was selected; initial manual group history sync returned HTTP 409 surfaced as `Telegram MTProto authorization is no longer valid`.
- Telegram MTProto M4AD/M4ADH4R3 diagnostics: strict pinned SSH transport verified; production runtime/ref remains exact `8091736337689b68b4510126e74d9e409397f696`; Alembic `0046`; health/api/worker/db PASS; exactly one MTProto account; encrypted session non-empty; challenges 0; manual-selected groups 1; configured folders 0; active scope 0; recurring Telegram job pending/recent but cannot make scope/history provider calls in this state; manual group sync route/HTTP 409 confirmed; no worker auth/provider error evidence and no AuthKeyDuplicated/AuthKeyUnregistered/SessionRevoked/Unauthorized/AuthKeyNotFound evidence.
- Telegram MTProto M4AE human discovery probe: PASS. First reopen showed only Secretary-server connectivity failure; subsequent repeated reopens loaded connected identity, folders, and groups through provider-backed discovery without Telegram authorization error. Current stored MTProto session is therefore demonstrably usable for Telegram discovery; the earlier history-sync auth-invalid message remains history-path-specific/misclassified until further diagnosis.
- Exact release `fetch_history()` currently maps broad `ValueError`/`TypeError` failures to `TelegramMtprotoAuthorizationInvalidError`, so the manual-sync message alone does not prove session revocation.
- Telegram MTProto M4AG2 raw history-stage probe: PASS on exact diagnostic SHA `b9b6cb6e3444d78ae7bb438eedb30e30fc83b3d1`. Strict pinned SSH passed on attempt 1; exactly one account/manual-selected group; session decrypt/StringSession parse/reference decrypt/reference parse/peer match/client construction all PASS; one `connect()`, one `is_user_authorized()` returned true, and one raw `iter_messages(input_peer, limit=1, reverse=False)` completed successfully. No login/write/discovery/materialization/production mutation occurred. This rules out a generally revoked session and a broken selected-group provider reference at probe time; the original manual-sync HTTP 409 remains either transient or specific to later/full-page history handling.
- Telegram MTProto M4AH2 first-page probe: transport PASS on attempt 1 but inner API-container runtime exited before provider calls; sanitized result `STAGE_1_RUNTIME / RuntimeError / MESSAGE_ORDINAL=0 / TELEGRAM_NETWORK_CALLS=0`. No auth/history conclusion can be drawn from this run. Static review also found the embedded child script references `PAGE_SIZE` without defining it in child scope; a corrective review phase is required before any further provider probe.
- Telegram MTProto M4AH3 corrected first-page probe: PASS on exact diagnostic SHA `2b9b8d3d7f19f2174808ecdadfd4bddc148aa5e9`. Strict pinned SSH passed attempt 1; account/manual-group cardinality PASS; session/StringSession/reference/peer/client construction PASS; connect PASS; live authorization true; exactly one `iter_messages(limit=100, reverse=False)`; 100 messages seen and 100 exact `_history_entry_from_message` conversions succeeded; ENTRIES_NONE=0; no login/write/discovery/materialization/production mutation. The original manual-sync HTTP 409 is not evidence of a currently invalid session and was transient or arose outside the now-proven first-page path.
- Next production fix direction: remove broad read-path `ValueError`/`TypeError` => authorization-invalid remapping; preserve genuine auth-key/session failures as authorization-invalid and classify non-auth read/discovery failures as provider-unavailable before a controlled application Sync retry.
- Telegram MTProto M4AI1 read-path taxonomy fix: ARCHITECT ACCEPTED at `0cc9353e9263f97b461b4e0c0b3186dcc9f3d88b`. Non-auth ValueError/TypeError in history/discovery no longer map to authorization-invalid; genuine auth/session/AuthKeyError paths remain auth-invalid; shared provider 503 wording is neutral.
- Telegram MTProto M4AI2 integration candidate: ACCEPTED exact main SHA `23fa07df213d5a70a6dc1d3c8b32af39228107eb`, parents `db11f907e48066fadac26c20796bd45fa3f8d2f5` and `0cc9353e9263f97b461b4e0c0b3186dcc9f3d88b`. A1-A4.4 Telegram suite 137 passed; M4AI 10 passed; Ruff/diff-check PASS; Alembic remains 0046. Compared with production `8091736337689b68b4510126e74d9e409397f696`, only runtime changes are `backend/app/api/telegram_mtproto.py` and `backend/app/connectors/telegram/mtproto_transport.py`; remaining differences are docs/recovery context/tests. Client/infra/schema/dependencies unchanged.
- M4AJ initial deploy attempt: BLOCKED fail-closed before production SSH/mutation because `origin/production` still pointed to rollback/current production SHA `8091736337689b68b4510126e74d9e409397f696`, while `remote_deploy.py` requires `origin/production == --release-sha` before any rollout. No production services/DB/env/runtime/ref were changed. Correct workflow is an explicitly-authorized fast-forward promotion of `origin/production` to exact candidate `23fa07df213d5a70a6dc1d3c8b32af39228107eb`, then rerun the normal pinned deploy harness.
- Telegram MTProto M4AJR2 production deploy: SUCCESS. Production runtime/ref is exact `23fa07df213d5a70a6dc1d3c8b32af39228107eb`; origin/production matches; API and worker recreated; DB container/volume/data and `.env` preserved; Alembic remains `0046`; health PASS; credential key preserved; `TELEGRAM_MTPROTO_AI_ENABLED=false`; Bot API untouched; no Telegram login/history/manual Sync performed during deploy.
- Next authorized validation should be one human-controlled manual Sync of the already-selected Telegram group through the current Secretary UI, with no re-login, no Apply Scope, no folder/group changes, and no automatic retry.
- Telegram MTProto M4AK human controlled Sync: performed exactly once on production runtime `23fa07df213d5a70a6dc1d3c8b32af39228107eb`. UI returned provider-neutral red error `Telegram provider is temporarily unavailable`. After leaving and reopening Account settings, the error was gone; connected account state remained visible and Telegram groups continued to load. No retry/re-login/Apply Scope/group-folder change was performed. This is evidence against current general session invalidation; the remaining failure is history-path provider-unavailable/transient and may occur on the second initial backfill page or another history-specific provider step.
- Immediate next diagnostic direction: inspect sanitized production API/worker logs for the single M4AK sync without making Telegram provider calls. If logs do not expose a root exception class/stage, authorize one read-only second-page history probe using the existing stored session/reference, no writes/materialization/content output.
- Telegram MTProto M4AL1 sanitized log review: PASS as read-only inspection, but root cause remains unobserved. The M4AK manual sync route is visible with exactly one HTTP 503 and zero 409; no ValueError/TypeError/FloodWait/AuthKey/SessionRevoked/Unauthorized/root exception class or traceback is logged. No Telegram provider calls or production mutations occurred. Ordinary logs are therefore insufficient to classify the provider-unavailable failure.
- Next diagnostic phase: build/review one deterministic read-only current-history provider-sequence probe. It must inspect current selection state, reproduce the same provider fetch ordering as `_sync_selection` (first page and, only when production state/cutoff requires it, second backfill page), use a fresh Telethon client per page like production `fetch_history`, apply exact `_history_entry_from_message` conversion, emit only aggregate/stage/class facts, make no DB writes/materialization/content output, and not execute live until separate review authorization.
- Telegram MTProto M4AM1/M4AM1R/M4AM1R2 diagnostic harness: ARCHITECT ACCEPTED for one live run at exact review SHA `48816aedd639268770b2fb67caa22e048ce03493`. Final focused suite reported 51 passed; Ruff/diff-check PASS. Reviewed protocol now preserves structural/preflight failures, realistic page2 failures, InvalidHistoryEntry classification, strict fail-closed output, fresh client per page, <=2 pages/<=200 messages/<=6 bounded provider-operation calls, and no DB writes/materialization/application fetch_history.
- M4AM2 first execution attempt: BLOCKED locally before probe start. Executor was in reused checkout on `review/yandex-sync-resilience-a`, worktree had numerous untracked `.tmp_*` files, and the approved review ref had not been fetched into local remote refs. No SSH, production, DB, Telegram, or provider call occurred; the single live probe authorization remains unused. GitHub-side refs were rechecked by Architect: `review/telegram-mtproto-m4am = 48816aedd639268770b2fb67caa22e048ce03493` and `production = 23fa07df213d5a70a6dc1d3c8b32af39228107eb`. Next execution should use a fresh clean detached worktree rather than deleting unknown local scratch files or switching the dirty reused checkout.
- M4AM2R second preflight stop: root cause identified as wrong local repository/origin. Executor checkout origin is `https://github.com/d-yacenko/secretary.git`, while the canonical active repository is `https://github.com/d-yacenko/secretary-prerelease.git`. The wrong remote advertises only its own main and therefore cannot provide the canonical production/review refs. No diagnostic script, SSH, production, DB, Telegram, or provider call occurred; the one live M4AM2 authorization remains unused. Next execution must use a fresh clone of the canonical `secretary-prerelease` repository and must leave the old dirty checkout/origin untouched.
- M4AM2R2 live probe: EXECUTED exactly once from fresh canonical clone at approved diagnostic SHA `48816aedd639268770b2fb67caa22e048ce03493`. Canonical refs/target/clean detached checkout PASS. Probe stopped before session/reference/client/provider work at sanitized `STAGE_1_DB_SESSION / RuntimeError / MESSAGE_ORDINAL=0 / TELEGRAM_NETWORK_CALLS=0`; no page/provider calls, DB writes, materialization, login/discovery/write RPC, or production mutation occurred. This result is structural-only and gives no Telegram auth/history conclusion. The current diagnostic's STAGE_1_DB_SESSION is too broad: it covers compose/service/DB/Alembic checks, child container execution, DB query/cardinality, and generic child stderr/nonzero collapse. Do not rerun the provider probe; next step is a zero-provider read-only structural localization.
- M4AN1 zero-provider structural localization: BLOCKED before remote helper start. Canonical target/pin and strict host-key verification passed, but SSH authentication/remote execution failed. No production guards, structural child, TelegramClient, provider calls, DB writes, or production mutations occurred. No SSH retry was performed. This differs from the earlier M4AM2R2 run, which authenticated and reached remote code, so the immediate next step is local-only SSH credential-readiness inspection with no production network connection.
- M4AN1R local SSH credential-readiness: current Executor environment has no usable ambient SSH credential path. SSH client exists; SSH_AUTH_SOCK is set and points to a socket, but agent query did not yield usable identities and key count is 0; no readable identity-file candidates were found; M4AM/M4AN do not configure an explicit IdentityFile/IdentityAgent and rely on ambient/default SSH authentication. Production network connections remained 0. Therefore further production SSH diagnostics are blocked until SSH credential availability is restored in the Executor environment; do not retry SSH/Telegram diagnostics before that.
- Executor-bootstrap process correction: do not continue turning wrong repo/origin, dirty checkout, missing refs, SSH-agent forwarding, or ambient credential availability into separate M4 product phases. These are now handled by the canonical bootstrap runbook before the authorized task. Once a suitable Executor environment is available, resume the actual zero-provider structural localization of M4AM's broad STAGE_1_DB_SESSION under a new explicit runtime authorization.
- Telegram Bot API retirement is NOT AUTHORIZED.
- Telegram API Terms clarification remains pending through external support channels.
- Product direction while clarification is pending: complete MTProto as a full ordinary messenger while keeping MTProto-derived objects outside ML/LLM by default.
- Canonical installation flag: `TELEGRAM_MTPROTO_AI_ENABLED=false` by default.
- Telegram MTProto Q1/Q1R/Q1R2 AI quarantine: ACCEPTED / INTEGRATED at `b50e1e62f825cff960f5a1a3a50711fd25863697`.
- Telegram MTProto C1A/C1AR send/reply: ACCEPTED / INTEGRATED at `311682ad7b245398835b585d67deec4f6074e020`.
- Telegram MTProto C1B/C1BR/C1BR2 edit/delete/mark-read: ACCEPTED / INTEGRATED TO MAIN at merge `37d2106fedc3d6cd3a9b5fd15dc70ded48d00caf`. Reviewed implementation tip is `0174b82375837b428f9e0b09c811394dc0f1d884`.
- C1B accepted semantics include approval-gated edit/delete/read, exact-peer destructive preflight, definite-vs-uncertain write classification, replay-safe local convergence, truthful mark-read effects, and Q1-aware semantic edit enqueue.
- Non-blocking normalization debt identified during review: immediate MTProto send/edit titles use a different bound than A3 history normalization. C2A must unify canonical presentation-title generation.
- Telegram MTProto C2A/C2AR/C2AR2/C2AR3/C2AR4 reconciliation: ACCEPTED / INTEGRATED TO MAIN at merge `5823d1f557e7040b0396e895032ed3c3b16d4d0d`. Reviewed implementation tip is `802d9aacb217bd2d3b3e012bdd2a644059d55ad4`.
- Accepted reconciliation semantics: 60-second Telegram cadence; A3 new-message sync; bounded exact-message remote edit/delete reconciliation; independent recent-head + historical sweep traversal; fair account rotation; <=5 lookups/peer and <=20/account; auth/FloodWait/provider error classification; confirmed absence tombstone; canonical title normalization; Q1 quarantine preserved.
- Telegram MTProto C2B/C2BR/C2BR2 deterministic notification surface: ACCEPTED / INTEGRATED TO MAIN at merge `0cdcbaa0498f8e4c1ef3033c5c7d544bb8590319`. Reviewed implementation tip is `2653de562f72ddb0b81063fb816a02b330022a4b`.
- Accepted C2B direction: deterministic UUIDv5 Notification IDs; created/edited/deleted transport event keys; existing notifications table/API; no migration; initial/backfill anti-spam intent; Q1 AI quarantine preserved.
- C2B blocking defect: reconciliation can swallow an unexpected local Notification persistence failure under broad candidate isolation after edit/delete mutation, risking committed object convergence with a permanently missing deterministic event.
- C2B blocking verification gap: required focused matrix was not completed; missing explicit conflict-path, backfill/replay, service/inactive, missing-edited-at/metadata-only, outgoing-delete/transient/mismatch, and Notification API behavior coverage.
- Accepted C2B semantics: deterministic UUIDv5 transport notifications for created/edited/deleted inbound Telegram MTProto events; initial/backfill anti-spam; Q1 AI quarantine preserved; generic Notification API lifecycle; typed local persistence boundary including SAVEPOINT creation; deterministic PK-conflict recovery; local notification persistence failures cannot silently commit edit/delete convergence without the corresponding event.
- Telegram MTProto C3A/C3AR/C3AR2 Flutter account/auth/scope UX: ACCEPTED / INTEGRATED TO MAIN at merge `f6f66dff6e03ae290dc7fc68d945734eba9a99a5`. Reviewed implementation tip is `b8455ec7b48106a284d81e50c524c8d8ef278d19`.
- Accepted C3A direction: Flutter-only integration over existing MTProto endpoints; typed client models/methods; in-memory challenge state; account/folder/scope/group controls; backend/migrations/production untouched.
- Accepted C3A semantics: explicit in-memory phone/code/password state machine; real 503 server-not-configured handling; connected identity; folder/ignore-muted configuration including zero-folder state; preview/reconcile authoritative peer scope without stale duplicates; manual groups with forum/unavailable state; exact per-peer/group sync; 401s route through global AuthController; no backend/client Telegram SDK changes.
- Telegram MTProto C3B Inbox transport-event presentation: ACCEPTED / INTEGRATED TO MAIN at merge `e503f680543d2eeafbfbb9b641b1b1942d7990ca`. Reviewed implementation tip is `ef27577b61b5762e00c5f96183f9abec05e64e36`.
- Accepted C3B semantics: typed Telegram MTProto transport-event detection; localized created/edited/deleted Inbox presentation; neutral `Готово` accept label; existing generic accept/ignore semantics preserved; existing best-effort mark-read/context navigation reused; raw transport identifiers are not rendered; Telegram source objects remain in the normal Inbox feed without duplication.
- Telegram rollout RF1/RF1R release freeze: ACCEPTED / INTEGRATED TO MAIN at merge `9e74e892fa133b68679af1e3152d0a6947cd99f6`.
- Exact accepted RF1R release candidate eligible for M3: `8091736337689b68b4510126e74d9e409397f696`.
- Validated product base `e503f680543d2eeafbfbb9b641b1b1942d7990ca` is NOT an M3 release SHA; RF1 head `a90716602d35cf5525554a1005a543dd8dda5491` is superseded.
- Accepted RF1/RF1R evidence: disposable incremental 0041->0046 PASS; fresh install ->0046 PASS; intermediate downgrade matrix 0042..0046 ->0041 PASS with sentinel preservation; round-trip 0041->0046->0041->0046 PASS; focused Telegram/notification/source-sync/client gates PASS; Flutter analyzer head-only diagnostics zero; full backend comparison has head-only failed/error identities zero; production untouched.
- Current authorized phase: Telegram MTProto M4A production account activation and first controlled sync, exactly as specified in `CURRENT_TASK.md`.
- M4A production preflight: PASS at production runtime/ref `8091736337689b68b4510126e74d9e409397f696`, Alembic `0046`, health/api/worker/db PASS, MTProto backend configured.
- M4A human Telegram authorization: COMPLETE through existing Account/Settings -> Telegram MTProto UI; connected status confirmed; no sensitive auth/session values inspected or logged; AI quarantine remains disabled; Bot API untouched.
- M4A current stop point revised: user UI evidence shows the running desktop client is stale. At deployed release SHA `8091736337689b68b4510126e74d9e409397f696`, AccountScreen unconditionally includes `TelegramMtprotoAccountSection` between Connections and Sync, but the user's current client shows legacy Telegram Bot block followed directly by Sync. M3 rebuilt backend api/worker only; Flutter desktop client is outside production Compose.
- M4AR exact-SHA Linux build/tests passed. M4ARR determined that window closure is caused by Executor sandbox lifetime: the fresh client survives while the shell lives, `setsid + nohup` children are reaped when sandbox ends, and a control detached sleep is terminated the same way; no Flutter crash evidence exists.
- Human live evidence confirms MTProto controls are present in the fresh client, but Telegram authorization cannot be completed before sandbox teardown. Earlier Executor claims of completed MTProto auth are superseded until a persistent human-launched client reports connected.
- Current authorized phase: M4ARR2 copies the exact release bundle to a stable user-owned preview path and hands launch to the human's normal desktop terminal/session. No scope selection or sync yet.
- C2A uses the existing Postgres recurring source-sync lane; no long-lived Telegram listener/daemon is authorized.
- C2A target default Telegram polling cadence is 60 seconds, configurable through the existing `source_sync_telegram_mtproto_interval_seconds` setting.
- C3A/C3B and RF1/RF1R are accepted. M3 is now authorized only for exact release SHA `8091736337689b68b4510126e74d9e409397f696`, exact rollback SHA `5cce4b57b14e0052a038acae1354a2821a2bb77b`, and exact schema transition `0041 -> 0046` through the accepted migration harness.
- Existing release candidate `917eebed4b0ffb6bf55f573a24d99d00dc1f8fbb` must NOT be deployed as-is because it predates the AI quarantine and subsequent Telegram messenger work.
- No production ref move, deploy, service mutation, Alembic write, DB mutation, env mutation, production MTProto login/history import, bot disable/delete, or destructive cleanup is authorized during M4A.
- IMAP IDLE: NOT STARTED.

`CURRENT_TASK.md` is the source of active authorization.

- Bootstrap correction: prior `ssh_credentials_unavailable` conclusion from SSH_AUTH_SOCK/ssh-add/ssh -G inspection was a false negative. Human evidence showed successful `ssh root@web-itx.duckdns.org` from the same sandbox. Canonical SSH readiness is now determined by an actual pinned read-only `BatchMode=yes` SSH no-op; agent/socket/key-inventory checks are non-gating diagnostics only. If simple sandbox SSH succeeds while a harness fails, investigate invocation differences rather than credentials.

- Bootstrap correction 2: repeated `ssh_invocation_mismatch` came specifically from treating `BatchMode=yes` as mandatory even though direct sandbox SSH to the canonical production user succeeds. For BREAK-GLASS read-only diagnostics, the required invariant is now strict pinned host-key verification plus public-key-only auth (`PasswordAuthentication=no`, `KbdInteractiveAuthentication=no`, `PreferredAuthentications=publickey`). `BatchMode` is non-gating for this diagnostic path. Normal deploy harness remains unchanged.

- Executor SSH context mismatch confirmed: human interactive sandbox shell can open `ssh root@web-itx.duckdns.org`, while the Executor's automated SSH subprocess cannot complete either the pinned BatchMode no-op or the pinned public-key-only no-op. Therefore the repeated blockers are not evidence that production SSH itself is unavailable; they indicate the agent subprocess does not share the same interactive SSH credential/broker context as the human sandbox terminal. Stop changing auth flags and stop treating this as a Secretary/Telegram diagnostic result. The next viable bridge is to expose an already-authenticated SSH transport to the Executor (for example a shared OpenSSH ControlMaster socket) or run the read-only structural diagnostic from the human-authenticated session.

- Human-shell bridge added: `ops/production/manual_mtproto_structural_probe.sh` provides the approved zero-provider M4AN2 structural localization from the human sandbox shell where SSH already works. It pins the production ED25519 host key, disables password/keyboard-interactive auth, performs remote release/service/DB/Alembic and pre-Telegram session/reference checks only, and emits sanitized booleans/stages. This avoids the Executor subprocess credential-context mismatch without weakening the production deploy harness.

- Telegram MTProto M4AN2R manual bridge completion fix: local wrapper now validates the ordered, allowlisted remote transcript and requires an explicit terminal outcome before `MANUAL_M4AN2_END=true`; premature EOF produces `MANUAL_M4AN2_BLOCKED=remote_incomplete`. Alembic exec stdin is isolated from the SSH-streamed script. Alembic start/result and child start/validated terminal result are localized; incomplete/unknown/unsafe child output becomes an explicit `CHILD_PROTOCOL` blocker without exposing raw output. Local mocked integration tests cover EOF at every remote stage, valid completion, explicit outer failure, unsafe output, and provider/write-call exclusion. This is a local bridge correction only, not a completed production structural localization; production SSH, Telegram/provider calls, and production mutations during M4AN2R are all 0.

- Telegram MTProto M4AN2 human-shell structural localization: PASS on production runtime/ref `23fa07df213d5a70a6dc1d3c8b32af39228107eb`. Canonical repo/target pin/remote HEAD/production ref/clean worktree/Compose/db/api/worker/DB health/Alembic 0046 all PASS. Read-only API child also PASS for imports, DB query/cardinality, history-state read, credential-encryption construction, session decrypt/StringSession parse, provider-reference decrypt/parse/peer-match; child rc=0, stderr absent, `TELEGRAM_NETWORK_CALLS=0`, `FAILURE_SUBSTAGE=NONE`, `M4AM_GENERIC_DB_STAGE_CAUSE=NOT_REPRODUCED_PRE_PROVIDER`. State remains initial (`INITIAL_STATE=true`, history incomplete, no backfill cursor before sync). No TelegramClient/provider calls, DB writes/materialization, or production mutation occurred. The earlier M4AM `STAGE_1_DB_SESSION` failure is therefore not reproducible as a current structural/pre-provider defect.
- Diagnostic consequence: M4AH3 already proved page1 with 100 messages and 100 exact conversions. With current state still initial and structural path clean, the remaining unproven portion of the failed manual Sync is the bounded provider sequence after page1, especially derived second-page backfill when page1 is full. Next step is a reviewed human-shell two-page provider probe with at most two pages/200 messages/6 logical provider operations, no DB writes/materialization, and sanitized stage/class output; build/review first, live run separately.
- Telegram MTProto M4AP1 boundary normalization: implemented locally and reviewed. `TelethonMtprotoTransport.fetch_history` now maps absent `min_message_id`/`max_message_id` to numeric Telethon sentinels `min_id=0`/`max_id=0`, preserving the public signature, page/reverse/auth/error/retry/conversion behavior. New regressions cover initial, incremental, backfill bounds, iterator initialization rejecting `None`, and preservation of non-auth `TypeError` -> `TelegramMtprotoProviderUnavailableError`. New regressions pass; M4AI passes; available A3 transport/static tests pass. DB-backed A3 service tests could not initialize because local `localhost:5432` is unavailable. No production SSH, provider calls, deployment, or production mutation occurred.

- Telegram MTProto M4AO1 human-shell two-page probe: BUILT / LOCALLY REVIEWED ONLY. New `ops/production/manual_mtproto_history_two_page_probe.sh` and its streamed Python helper pin the canonical target/ED25519 key, require production HEAD and origin/production `23fa07df213d5a70a6dc1d3c8b32af39228107eb`, clean worktree, Compose/services/DB health and Alembic 0046 before child execution. The child fails closed on initial-state drift, validates cardinality/session/reference, uses a fresh in-memory Telethon client per page with retries/reconnect/flood-sleep retries disabled, exact production conversion and `_next_backfill_state`/cutoff semantics, and at most two pages/200 messages/6 logical connect+authorization+iteration calls. Disconnect cleanup is outside those logical counters. No application fetch_history, Sync/login/discovery/write RPC, DB flush/commit/materialization, or production file/env/ref/service mutation path is invoked. Ordered allowlisted output, stage-specific aggregate counts and an explicit terminal outcome are checked both remotely and locally; EOF/unsafe output/uncertain child execution fail closed. Focused local suite: 22 tests PASS (including staged page failures, EOF, fake-module child entrypoint, mocked bridge/runtime, count bounds and static call-path checks); wrapper bash syntax, Ruff and diff-check PASS. Live probe NOT RUN: production SSH = 0, Telegram/provider calls = 0, production mutation = 0. This is probe-build evidence only, not a provider diagnostic result.

- Telegram MTProto M4AO2 one-shot human-shell two-page provider probe: LIVE RUN COMPLETE, NO RETRY AUTHORIZED. Production guards and structural/session/reference checks all PASS. Page1 failed before yielding the first message: `PAGE1_PASS=false`, seen/converted/none = 0/0/0, `FAILURE_STAGE=STAGE_3_PAGE1_ITERATION`, `RAW_EXCEPTION_CLASS=TypeError`, `MESSAGE_ORDINAL=1`, logical counts connect/auth/iter = 1/1/1, `TELEGRAM_NETWORK_CALLS=3`. No DB writes/materialization or production mutation occurred. This localizes the current failure to Telethon iterator initialization/first iteration, after successful connect and authorization, before message conversion.
- Root cause identified from production code + Telethon iterator semantics: `TelethonMtprotoTransport.fetch_history` forwards optional `min_message_id=None` / `max_message_id=None` directly as Telethon `min_id=None` / `max_id=None`. Telethon v1 message iteration uses numeric comparisons such as `max(offset_id, max_id)` (or `max(offset_id, min_id)` for reverse iteration), so explicit `None` can raise `TypeError` before the first history message is yielded. This exactly matches M4AO2 and explains why M4AH3 could pass when the optional bounds were omitted instead of explicitly passed as None. Required fix: normalize absent bounds to Telethon's numeric sentinel 0 at the transport boundary, with focused regressions for initial, incremental, and backfill calls.

- Telegram MTProto M4AQ1 production deploy: PASS through the unchanged canonical `ops/production/deploy.py` harness. Runtime moved from `23fa07df213d5a70a6dc1d3c8b32af39228107eb` to exact release `b7fbdc71584cfde042a998fbfefb06015175a205`; `origin/production` is the same release. API and worker were recreated; DB container, DB volume, and `.env` remained unchanged; health PASS; Alembic remains `0046`; no rollback or migration occurred. `TELEGRAM_MTPROTO_AI_ENABLED=false`; Bot API untouched; no Telegram login/history/provider actions were performed during deploy.

- Telegram MTProto M4AR1 human Sync: the single authorized Sync click was used after deploy, but the user navigated away from Account before observing the result and then returned. The Flutter client stores the latest sync summary only in widget-local `_lastSync` state inside `client/lib/account/telegram_mtproto_account_section.dart`; leaving the screen disposes that state, so the absence of counters after returning does NOT indicate success or failure. No second Sync is authorized. Outcome must be reconstructed read-only from persisted production history state before any retry.

- Telegram MTProto M4AR2 read-only reconstruction after the one-shot human Sync: PASS. Production/runtime guards, DB/Alembic, account/manual-selection cardinality, session decrypt/StringSession parse, and provider-reference validation all PASS with `TELEGRAM_NETWORK_CALLS=0`, `FAILURE_SUBSTAGE=NONE`, and no production mutation. Persisted history state changed from pre-Sync `INITIAL_STATE=true / HISTORY_COMPLETE=false / BACKFILL_CURSOR_PRESENT=false` to `INITIAL_STATE=false / HISTORY_COMPLETE=false / BACKFILL_CURSOR_PRESENT=true`. Because the sync service writes `history_latest_message_id`, backfill cursor, cutoff, completion state, and `history_last_synced_at` only after its bounded fetch/apply sequence completes, and the request-scoped DB session commits only on successful endpoint completion, this is evidence that the deployed bound-normalization fix resolved the prior iterator TypeError and the human Sync completed successfully with persistent progress. History is not yet complete; a backfill cursor remains for a later separately authorized Sync.

- Telegram MTProto M4AS1 controlled backfill continuation Sync: SUCCESS. Human UI summary showed `scanned=100`, `materialized=49`, `created=49`, `updated=0`, `unchanged=0`, `skipped=51`, `history_complete=true`. Therefore the bounded initial 14-day history backfill for the selected group is complete on production runtime `b7fbdc71584cfde042a998fbfefb06015175a205`. No retry was performed.
- Telegram MTProto M4AT1R visibility probe Alembic guard: BUILT / LOCALLY REVIEWED ONLY. The read-only helper now uses the accepted production DB credential/host contract (`PGPASSWORD`, `127.0.0.1`, default secretary user/database), exact `0046` stdout matching, and isolated empty stdin while suppressing stderr. Visibility query semantics and zero-provider/read-only behavior are unchanged. Twelve local focused tests pass, including exact/wrong Alembic transcripts, contract and stdin assertions, prior visibility transcripts, EOF/unsafe output and static no-write/provider checks. Live probe was not run.
- Telegram MTProto M4AT1 visibility-state probe: BUILT / LOCALLY REVIEWED ONLY. New human-shell wrapper and protocol helper verify the pinned target and production release `b7fbdc71584cfde042a998fbfefb06015175a205`, then perform only API-container read-only cardinality/state and exact MTProto object aggregate queries. Active visibility uses the canonical `telegram_mtproto_active_object_predicate`; no recent-source query is needed. Ordered output is fail-closed on EOF/unknown values, reports `TELEGRAM_NETWORK_CALLS=0`, and requires an explicit terminal marker. Nine local transcript/static tests pass; live probe was not run.

- Telegram MTProto M4AT2 one-shot human visibility-state probe: remote execution started and stopped at the probe's Alembic guard with `ALEMBIC_0046_PASS=false`, `FAILURE_STAGE=STAGE_0_ALEMBIC`, `RAW_EXCEPTION_CLASS=RuntimeError`, `TELEGRAM_NETWORK_CALLS=0`. All prior production guards (repo/ref/worktree/Compose/db/api/worker/DB health) passed. This is not evidence of a production migration regression: the reviewed visibility probe's Alembic command uses `psql -At` without the production DB credential/host contract, while the previously accepted structural probe uses `PGPASSWORD="$POSTGRES_PASSWORD" psql -h 127.0.0.1 -U "${POSTGRES_USER:-secretary}" -d "${POSTGRES_DB:-secretary}" ... </dev/null`. The deploy and prior read-only production checks already verified Alembic 0046 on this runtime. M4AT2 live authorization is consumed; do not rerun until the probe guard is corrected and re-reviewed.

- Telegram MTProto M4AT2R corrected live visibility-state probe: SUCCESS. Production guards and Alembic 0046 PASS; exactly one MTProto account and one manual selection; `MANUAL_SELECTED=true`, `SCOPE_ACTIVE=false`, `HISTORY_COMPLETE=true`, `BACKFILL_CURSOR_PRESENT=false`, `IMPORTED_OBJECT_COUNT=228`, `ACTIVE_VISIBLE_OBJECT_COUNT=0`, `TELEGRAM_NETWORK_CALLS=0`. Therefore history import is complete and persisted, but normal Inbox/search/retrieval visibility is intentionally blocked by the inactive transport scope. This is not an import failure. Current accepted architecture keeps `manual_selected` and `scope_active` independent; manual selection alone does not activate normal read-surface visibility.

- Telegram MTProto manual-only visibility direction selected for the next validation: option 1. Goal is to make an explicitly manual-selected peer eligible for ordinary non-AI product read surfaces even when `scope_active=false`, while preserving folder-derived scope semantics, fail-closed ownership/metadata matching, and the production `TELEGRAM_MTPROTO_AI_ENABLED=false` quarantine. This phase is build/review only; no deploy, production mutation, provider call, Sync, Apply Scope, or recurring-sync semantic change is authorized yet.
- Telegram MTProto M4AU1 manual-only ordinary visibility: BUILT / LOCALLY REVIEWED ONLY. Ordinary transport visibility now accepts an owned matching selection when `manual_selected=true OR scope_active=true`; malformed, wrong-user, wrong-account, and wrong-peer metadata remain fail-closed. AI policy is explicitly scope-only, so manual-only objects remain excluded from AI retrieval/context and embedding catch-up, while recurring sync continues to query `scope_active=true` only. Focused A4.3/Q1 additions cover the four-state truth table, RecentSource/ObjectQuery/ordinary graph visibility, manual deselection retention, and AI quarantine. DB-backed tests could not initialize because local `db` host resolution is unavailable; collection/static/lint checks passed. No deploy, production SSH, provider call, or production mutation occurred.

- Telegram MTProto future folder-scope bootstrap preference: do NOT backfill deep history per newly activated chat. Product intent is a shallow per-peer bootstrap based on recent message count, roughly 10–20 latest messages per chat (exact default/configuration to be finalized), sufficient to establish initial context; after bootstrap, continue only incremental new-message sync. Prefer a message-count bound over a months/deep time-window crawl for folder-derived scope. Current production history semantics (`TELEGRAM_MTPROTO_HISTORY_DAYS=14`, bounded per-run import) remain unchanged until a separately authorized implementation task; this note is a future product requirement, not current production authorization.

- Telegram MTProto M4AU1 manual-only ordinary visibility: ACCEPTED / INTEGRATED TO MAIN. Implementation commit `2db36510fe884eadc40d63fed8661ed3627f1cbb` is an ancestor of current main `8ce740c91f51de5036c97341e5af3e3c97c56b6a`. Accepted semantics: ordinary transport visibility uses owned matching `manual_selected=true OR scope_active=true`; fail-closed ownership/account/peer/metadata checks remain; AI policy is explicitly scope-only and remains quarantined when `TELEGRAM_MTPROTO_AI_ENABLED=false`; recurring sync remains `scope_active=true` only. Local DB-backed regressions could not initialize because the executor host DB name was unavailable; collection/static/policy-fragment checks, Python compile, Ruff, and diff-check passed. No deploy or production mutation has been authorized or performed for M4AU1.

- Telegram MTProto M4AV1 schema-neutral production deploy: PASS. Production release/runtime and `origin/production` are exact `2db36510fe884eadc40d63fed8661ed3627f1cbb`; health PASS; Alembic remains `0046`; DB container unchanged; DB volume unchanged; production `.env` unchanged; API and worker recreated. The deployment was application-only with no migration / no `0047`. This deploy activates the accepted ordinary-visibility rule `manual_selected=true OR scope_active=true` for non-AI product reads. The deploy report did not independently expose the MTProto AI flag value, so do not claim a fresh runtime read of that flag from this transcript; the deployment preserved `.env` unchanged and M4AU1 code keeps AI eligibility scope-only.

- Telegram MTProto M4AW1 human Inbox verification after M4AV1 deploy: NOT YET VISIBLE in the current Inbox UI snapshot. The normal Inbox loaded successfully and showed other current source items, but no Telegram items were visible in the presented feed. Per the task stop condition, no Sync, Apply Scope, selection change, login, provider probe, or production mutation followed. This does not yet prove a transport-visibility regression: the Inbox has additional eligibility and ordering filters (including outbound-chat suppression and feed pagination), so the next step is read-only aggregate diagnosis of imported -> transport-visible -> inbox-eligible -> first-page placement.
- Telegram MTProto M4AW2 Inbox eligibility probe: BUILT / LOCALLY REVIEWED ONLY. New `ops/production/manual_mtproto_inbox_eligibility_probe.sh` and bundled helper pin production `2db36510fe884eadc40d63fed8661ed3627f1cbb`, verify the accepted target/host/Alembic contract, and run only API-container read-only SQL plus canonical `RecentSourceService` eligibility/list-page semantics. It reports imported, transport-visible, inbound/outbound, Inbox-eligible, first-page, and first-50 Telegram aggregates without conversation grouping or summary/job enqueue. Sixteen local protocol/static tests pass, including positive/zero/outbound diagnostics, EOF/unsafe output, provider/write/job exclusion, Alembic credentials and stdin isolation; bash syntax, helper compile, Ruff, and diff-check pass. Live probe was not run; production SSH, Telegram/provider calls, and production mutation are all 0.

- Telegram MTProto M4AW2 Inbox eligibility probe: ACCEPTED / READY FOR ONE HUMAN-SHELL LIVE RUN at commit `1d25e033376b9a9215b02a9ef9b02946c9e82ae9`. Review confirms zero-provider/read-only semantics, production pin `2db36510fe884eadc40d63fed8661ed3627f1cbb`, Alembic 0046 with accepted DB credential/host contract and isolated stdin, canonical RecentSourceService eligibility/page semantics, aggregate-only output, and no conversation-grouping/job/summary path. Sixteen focused local tests, bash syntax, bundled helper compile, Ruff, and diff-check passed. Live probe not yet run.

- Telegram MTProto M4AW3 one-shot Inbox eligibility probe: SUCCESS. Production guards/Alembic 0046 PASS; `IMPORTED_OBJECT_COUNT=228`; `TRANSPORT_VISIBLE_COUNT=228`; `INBOUND_COUNT=219`; `OUTBOUND_COUNT=9`; `INBOX_ELIGIBLE_COUNT=219`; `FIRST_PAGE_TOTAL_COUNT=30`; `FIRST_PAGE_TELEGRAM_COUNT=0`; `FIRST_50_TELEGRAM_COUNT=0`; `TELEGRAM_NETWORK_CALLS=0`. Therefore M4AU1 transport visibility is effective in production and 219 imported Telegram messages are canonical Inbox-eligible. Their absence in the visible UI snapshot is ordering/pagination, not import or visibility failure: none of those 219 objects are among the 50 newest Inbox-eligible items. The persistent desktop client has cursor-based infinite loading and calls `_loadMore()` as the user approaches the bottom of the Inbox feed.

- Telegram MTProto M4AX1 paginated Inbox human verification: PASS. After scrolling/loading older Inbox pages in the persistent preview client, Telegram items from the already imported manually-selected group became visible in the normal Inbox. This confirms end-to-end ordinary non-AI visibility in production: import -> persisted objects -> manual-only transport visibility -> canonical Inbox eligibility -> cursor pagination -> client presentation. No additional Sync or Apply Scope was needed.

- Telegram MTProto next major product phase selected: folder-derived scope onboarding with shallow bootstrap. Product target: for a newly activated folder-scope peer, import only the latest 20 provider history entries (materializing the eligible text subset), do not start a time-based/deep backfill, then use incremental sync for newer messages. Keep the existing manual-selected legacy history path unchanged in this phase; keep recurring peer selection `scope_active=true`, AI quarantine `TELEGRAM_MTPROTO_AI_ENABLED=false`, no migration/0047, and Bot API untouched. This is intended as the final major MTProto transport feature phase; production rollout and human acceptance remain separate steps after implementation review.
- Telegram MTProto M4AY1 shallow scope bootstrap: BUILT / LOCALLY REVIEWED ONLY. `sync_scope_peer` now uses a fixed `TELEGRAM_MTPROTO_SCOPE_BOOTSTRAP_MESSAGES = 20` newest-first fetch for fresh scope rows, performs no historical second page even when `has_more`, stores the fetched maximum message ID, clears the scope backfill cursor, and marks scope history complete. Existing scope rows use incremental `min_message_id` only and preserve legacy/manual backfill cursor, completion, and unset cutoff state; `sync_group` manual 14-day/bounded behavior is unchanged. Recurring selection remains `scope_active=true` only. Focused additions cover exact transport bounds/call counts, empty and skipped pages, existing cursor preservation, and fresh recurring scope selection. DB-backed A3/A4.2/A4.4/Q1 suite collection ran but all 76 tests could not initialize because local `db` host resolution is unavailable; compile, Ruff, and diff-check pass. No production SSH, provider calls, live Sync, deployment, or production mutation occurred.

- Telegram MTProto M4AY1 review of commit `443dc8c91bd7e66f5c24fdf8ab0f2bd270402673`: NOT YET ACCEPTED. The one-page/limit-20 scope control flow is correct, but fresh scope bootstrap still computes the legacy 14-day cutoff and passes it into `_normalize_entry`. Therefore a quiet chat whose latest 20 provider entries are older than 14 days can fetch exactly 20 entries yet materialize none of them, contradicting the selected count-based bootstrap product intent. The implementation also persists that synthetic 14-day cutoff on a fresh scope row even though scope bootstrap is intended to be count-bounded, not time-bounded. No deploy is authorized. Corrective M4AY1R must make scope-mode normalization independent of the legacy 14-day cutoff and leave/preserve `history_cutoff_at` rather than introducing a time window, while keeping manual `sync_group` behavior unchanged.
- Telegram MTProto M4AY1R scope count-bound correction: BUILT / LOCALLY REVIEWED ONLY. Scope-mode normalization now uses an aware `datetime.min` cutoff, so fresh and incremental scope entries are not rejected by the manual 14-day window; scope persistence preserves existing `history_cutoff_at`, backfill cursor, and completion state, and fresh scope does not create a synthetic cutoff. Manual `sync_group` remains unchanged. The DB-independent cutoff regression passes with `SKIP_DB_FIXTURES=1`; the DB-backed A3/A4.2/A4.4/Q1 matrix remains blocked at setup because local `db` host resolution is unavailable. No schema/Alembic/provider/AI/recurring behavior changes were made.

- Telegram MTProto M4AY1R review of corrective commit `e0f89a30bb204b904bb49e47974dc4dbba5bbb48`: NOT YET ACCEPTED. The normalization cutoff is correctly non-time-limiting in scope mode, but fresh scope persistence still executes `selection.history_cutoff_at = cutoff` because the condition only skips persistence when `scope_mode=true` AND a previous latest-message cursor already exists. For a fresh scope row, this writes aware `datetime.min` into `history_cutoff_at`, contradicting the required "fresh scope leaves cutoff unset/preserved" semantics. This is also a cross-mode hazard: if the same row is later manually selected, manual `sync_group` will treat persisted `datetime.min` as its existing cutoff and can backfill arbitrarily deep instead of using the intended 14-day manual window. No deploy is authorized.
- Telegram MTProto M4AY1R2 scope cutoff persistence correction: BUILT / LOCALLY REVIEWED ONLY. Scope normalization still uses aware `datetime.min` transiently, while `_persist_history_cutoff` now never writes `history_cutoff_at` in scope mode and preserves any existing value. Manual mode continues to persist/use the normal existing cutoff behavior. Five DB-independent persistence/normalization tests pass; the focused A3/A4.2/A4.4/Q1 matrix collected 81 DB-backed tests but remains unavailable because local `db` hostname resolution fails. No schema/Alembic, recurring, AI, provider taxonomy, production SSH, live provider, or mutation changes occurred.

- Telegram MTProto M4AY1/M4AY1R/M4AY1R2 shallow folder-scope bootstrap: ACCEPTED FOR DEPLOY CANDIDACY at canonical main `cbd5e7dbe5b8030a1ad100f97bcd2d12f9dccfaa`. Final accepted semantics: fresh `scope_active=true` peer with no latest cursor fetches exactly one newest-first page with limit 20 and no min/max bounds; scope normalization uses aware `datetime.min` only transiently so message age does not impose the legacy 14-day filter; scope mode never persists or rewrites `history_cutoff_at`; fresh scope clears/no backfill cursor and marks history complete; existing scope peers are incremental-only and preserve legacy cutoff/backfill/completion state; manual `sync_group` retains the existing 14-day bounded backfill behavior; recurring remains `scope_active=true` only; Q1 AI quarantine remains unchanged. Five DB-independent policy tests pass; 81 DB-backed tests were collected but blocked at setup by unavailable local `db` hostname. Python compile, Ruff, and diff-check pass. No schema/Alembic change, no `0047`, no provider/live/production action occurred.

- Telegram MTProto M4AZ1 schema-neutral production deploy: PASS. Canonical deploy harness was run with release `cbd5e7dbe5b8030a1ad100f97bcd2d12f9dccfaa`, rollback `2db36510fe884eadc40d63fed8661ed3627f1cbb`, expected Alembic `0046`. Reported: `RELEASE_HEAD=cbd5e7dbe5b8030a1ad100f97bcd2d12f9dccfaa`, `HEALTH=PASS`, `ALEMBIC=0046`, `DB_CONTAINER_UNCHANGED=true`, `DB_VOLUME_UNCHANGED=true`, `ENV_FILE_UNCHANGED=true`, `API_RECREATED=true`, `WORKER_RECREATED=true`, `DEPLOYMENT=PASS`. No migration / no `0047`. The deploy transcript does not independently expose the runtime value of `TELEGRAM_MTPROTO_AI_ENABLED`; it does prove the production `.env` file was unchanged, so no AI flag change occurred in this deploy. No Telegram/provider action was part of the deploy.

- Telegram MTProto M4BA1 one-folder preview: STOPPED SAFELY BEFORE ACTIVATION because preview is truncated. Human selected exactly one folder `Личное`, kept muted-dialog exclusion enabled, saved once, and previewed once. UI reported `В области: 28; папок: 1`, skipped `{broadcast: 12, bot: 19, unsupported: 19}`, and `Предпросмотр усечён.` No Apply Scope or Sync followed. Code review localizes the truncation to the folder-scope dialog-universe scan: scope preview calls `fetch_dialog_universe(..., DISCOVERY_DIALOG_LIMIT)` where `DISCOVERY_DIALOG_LIMIT=500`; Telethon transport iterates at most that many dialogs and reports truncated whenever `seen >= scan_limit`. Therefore the current preview cannot prove completeness for an account whose dialog universe reaches the 500-row bound, and `reconcile_scope` correctly refuses activation when truncated.

- Telegram MTProto M4BB2 scope preview probe: BUILT / LOCALLY REVIEWED ONLY. New `ops/production/manual_mtproto_scope_preview_2000_probe.sh` and bundled helper use the pinned human-shell target/host-key/release/Alembic guard protocol, read exactly one MTProto account and one configured folder, decrypt the session in memory, discover folder definitions, and use the canonical transport dialog conversion/filter path with muted dialogs excluded. The probe retains at most 2000 dialogs after a 2001-item lookahead, deduplicates scope matches by peer, reports only sanitized aggregate counts, and never reconciles, fetches history, writes DB state, or invokes AI/Bot paths. Fifteen focused tests (29 boundary subtests) pass; Bash syntax, helper compile, Ruff, and diff-check pass. Live probe was not run: production SSH, Telegram/provider calls, and production mutation are all 0.

- Telegram MTProto M4BB2R compatibility correction: BUILT / LOCALLY REVIEWED ONLY. The remote child no longer imports `TELEGRAM_MTPROTO_SCOPE_DIALOG_SCAN_LIMIT` or calls `fetch_dialog_universe`; it uses a probe-local bound of 2000, explicit `TelegramClient`/`StringSession`, production-compatible folder-definition request, separate connect/auth/iteration stages, and a 2001-item lookahead with guaranteed disconnect cleanup. Network accounting is zero before provider start and deterministic six high-level calls on success. Child-logic fake-client tests cover all boundaries, retained-window skipped counts, muted exclusion, peer dedupe, cardinality, staged auth/provider/conversion/iteration failures, disconnect cleanup, and forbidden paths. Fourteen focused tests (36 subtests) pass; helper compile, bundled compile, Bash syntax, Ruff, and diff-check pass. Live probe was not run: production SSH, Telegram/provider calls, and production mutation are all 0.

- Telegram MTProto M4BB1 scope dialog scan: BUILT / LOCALLY REVIEWED ONLY. Scope universe scans now use `TELEGRAM_MTPROTO_SCOPE_DIALOG_SCAN_LIMIT=2000`, request a provider lookahead of at most 2001 dialogs, retain/process only the first 2000, and mark truncation only when an actual extra dialog is observed. Exactly 2000 dialogs are complete; 2001+ are truncated; skipped counts are limited to the retained window. Folder discovery and manual group discovery remain bounded by `DISCOVERY_DIALOG_LIMIT=500`, and truncated scope reconciliation remains fail-closed without mutation. Seven focused fake-iterator/service tests pass; the broader A4.2/A4.4/Q1 collection is blocked by unavailable local `db` hostname (with unrelated Alembic-config failure under repository-root invocation). Python compile, Ruff, and diff-check pass. No schema/Alembic, recurring, shallow-bootstrap, AI, provider, production SSH, or mutation behavior was changed.

- Telegram MTProto M4BB1 review of `c69d2353c19c4958e1fb60aac69466fcf6ac1482`: ACCEPTED AS CODE, NOT YET AUTHORIZED FOR DEPLOY. The 2000+1 bounded lookahead semantics are correct and preserve 500-limit folder/manual discovery plus fail-closed reconciliation. A separate pre-deploy safety issue was identified in existing A4.4 behavior: the recurring MTProto worker calls `reconcile_scope()` before every history pass. Because folder `Личное` is already saved in production, deploying M4BB1 could let the recurring worker reconcile/activate that saved folder before the human can run the intended post-deploy preview or press `Apply Scope`. This is not caused by M4BB1, but it makes "deploy then preview" racy. Production remains on `cbd5e7dbe5b8030a1ad100f97bcd2d12f9dccfaa`, where the 500-dialog truncation currently keeps recurring reconciliation fail-closed. Next step is a one-shot read-only 2000+1 preview probe against the current production session before deploying M4BB1; no scope mutation or history sync.

- Telegram MTProto M4BB2 review of `a41d3e11902f55858c4960b9f6b5ddc1383e0579`: NOT READY FOR LIVE RUN. Two blockers: (1) the remote child imports `TELEGRAM_MTPROTO_SCOPE_DIALOG_SCAN_LIMIT` and calls `TelethonMtprotoTransport.fetch_dialog_universe(...)`, but the pinned production runtime is still `cbd5e7dbe5b8030a1ad100f97bcd2d12f9dccfaa`, which predates M4BB1. That runtime has no new scope-limit constant and its transport clamps dialog-universe scans to 500 with old boundary truncation semantics. Therefore the probe cannot actually exercise 2000+1 semantics on current production. (2) the child wraps all runtime failures in one broad exception handler and always reports `FAILURE_STAGE=STAGE_1_DB_SESSION` and `TELEGRAM_NETWORK_CALLS=0`, even for auth/folder/dialog failures after Telegram network activity has begun; the parser-only tests do not prove truthful live-stage/network accounting. No live probe is authorized until corrected.

- Telegram MTProto M4BB2R review of `d923db98f77fb1e389455b13b5c3bc84f83173ba`: ACCEPTED / READY FOR ONE HUMAN-SHELL LIVE RUN. Review confirms the probe is compatible with pinned production `cbd5e7dbe5b8030a1ad100f97bcd2d12f9dccfaa`: it uses probe-local `SCOPE_DIALOG_SCAN_LIMIT=2000`, explicit `TelegramClient` / `StringSession`, current-production `GetDialogFiltersRequest`, and existing canonical `_custom_filter_definitions`, `_dialog_from_dialog`, and `dialog_matches_filter`; it does not import the M4BB1-only scope constant and does not call old `fetch_dialog_universe`. The wide scan requests at most 2001 dialogs, retains at most 2000, is read-only, excludes muted dialogs, deduplicates peers, never reconciles scope or fetches history/messages, and reports sanitized staged failures with nonzero provider accounting after provider start. Current production ref remains `cbd5e7dbe5b8030a1ad100f97bcd2d12f9dccfaa`.

- Telegram MTProto M4BB3 one-shot wide scope preview: SUCCESS. All production guards passed on pinned runtime/ref `cbd5e7dbe5b8030a1ad100f97bcd2d12f9dccfaa`, Alembic `0046`, exactly one MTProto account and one configured folder, muted exclusion enabled. The read-only provider probe retained/scanned 649 dialogs, matched 28 peers in the configured folder scope, skipped 15 broadcasts, 21 bots, 67 unsupported dialogs, 0 other, and reported `TRUNCATED=false` with 6 bounded high-level Telegram network calls. This proves the account's current dialog universe is below the new 2000 bound and the selected folder scope is complete at 28 peers. The earlier 500-row preview truncation was solely a scan-boundary artifact; the matched scope count remains 28 even with the complete 649-dialog universe. No scope reconciliation, history/message fetch, DB write, Apply Scope, or Sync occurred. M4BB1 is now safe for deployment from a preview-completeness perspective, subject to explicit human deploy authorization.


- Telegram MTProto M4BC1 schema-neutral M4BB1 production deploy: PASS by human-shell canonical deploy harness. Authorized release/runtime and `origin/production` are exact `c69d2353c19c4958e1fb60aac69466fcf6ac1482`; rollback was `cbd5e7dbe5b8030a1ad100f97bcd2d12f9dccfaa`; Alembic remains `0046`; health PASS; DB container unchanged; DB volume unchanged; production `.env` unchanged; API and worker recreated; deployment PASS. No migration / no `0047`. No manual Telegram Sync, Apply Scope, provider probe, folder mutation, login/re-login, or acceptance action was performed as part of the deployment. Because the existing recurring worker reconciles saved folder scope before its history pass, post-deploy acceptance must first inspect resulting state read-only rather than manually triggering scope/history.
- Telegram MTProto M4BD1 post-deploy acceptance probe: BUILT / LOCALLY REVIEWED ONLY. New human-shell probe pins runtime `c69d2353c19c4958e1fb60aac69466fcf6ac1482` and Alembic `0046`, then performs read-only aggregate SQL for account/folder cardinality, active scope/bootstrap state, folder-only peer object distributions, MTProto inbound/outbound totals, embedding/provenance counts, and pending/running embed jobs. It performs no Telegram/provider imports or calls, no reconciliation/history/Inbox mutation, and no DB writes. Focused protocol/static tests, helper and bundled compile, Bash syntax, Ruff, and diff-check pass. Live probe was not run: production SSH, Telegram/provider calls, and production mutation are all 0.

- Telegram MTProto M4BD1 review of `5ebc22e9ab14582177b3ee1ace49eeea86894048`: ACCEPTED / READY FOR ONE HUMAN-SHELL LIVE RUN. Architect review confirms the helper is pinned to production `c69d2353c19c4958e1fb60aac69466fcf6ac1482` / Alembic `0046`, uses the committed target and strict host-key pinning, performs only aggregate DB reads in the API container, imports no Telegram transport/client code, invokes no reconciliation/history/provider path, and has no flush/commit/write calls. Protocol output is fail-closed and sanitized. `ACTIVE_PEERS_WITH_OBJECTS_GT_20` excludes the active legacy manual-selected peer and applies only to folder-only active peers; because incremental post-bootstrap messages can legitimately push a peer above 20, a nonzero value is a diagnostic signal requiring interpretation, not by itself proof of deep backfill. The live run is separately authorized as read-only; no manual Sync/Apply Scope or other production mutation is authorized.


- Telegram MTProto M4BE1 one-shot post-deploy acceptance probe: SAFE PARTIAL PASS / PROBE SQL DEFECT. Human-shell run on production runtime/ref `c69d2353c19c4958e1fb60aac69466fcf6ac1482`, Alembic `0046`, passed repo/ref/worktree/Compose/DB/API/worker/DB-health guards. Read-only state evidence before failure: `ACCOUNT_COUNT=1`, `CONFIGURED_FOLDER_COUNT=1`, `ACTIVE_SCOPE_COUNT=28`, `MANUAL_ACTIVE_COUNT=0`, `FOLDER_ONLY_ACTIVE_COUNT=28`, `LATEST_CURSOR_PRESENT_COUNT=28`, `HISTORY_COMPLETE_COUNT=28`, `BACKFILL_CURSOR_PRESENT_COUNT=0`, `HISTORY_CUTOFF_PRESENT_COUNT=0`. This confirms the saved folder reconciled to the expected 28 peers and all 28 active folder-only selections reached shallow-history state with a latest cursor, completed history, no backfill cursor, and no persisted time cutoff. The probe then failed with sanitized `STAGE_1_DB_SESSION / ProgrammingError` before object-count/embedding aggregates; `TELEGRAM_NETWORK_CALLS=0`, `DB_WRITES=0`. Architect localization: the peer-count aggregate constructs the JSON `peer_id` accessor independently in SELECT and GROUP BY, which SQLAlchemy compiles as different bind parameters; PostgreSQL can reject the aggregate as non-identical grouping expressions. This is a probe-only defect, not evidence of a production Telegram failure. No rerun is authorized until a local corrective review.
- Telegram MTProto M4BE1R corrective probe fix: the read-only post-deploy acceptance child now creates one reusable `peer_expr = Object.metadata_["peer_id"].as_string()` and reuses that exact SQLAlchemy expression in both peer-count SELECT and GROUP BY, avoiding PostgreSQL JSON-path bind mismatch. A PostgreSQL-dialect compile regression passes with one peer-path bind parameter; all existing protocol/static checks remain passing. No production/live probe or database mutation was performed.


- Telegram MTProto M4BE1R review of `1e29b64687e517e60aa8971be53ce1adb553285f`: ACCEPTED. Correction is minimal and probe-only: one reusable `peer_expr = Object.metadata_["peer_id"].as_string()` is used in both SELECT and GROUP BY; PostgreSQL-dialect compile regression proves a single JSON-path bind parameter. Existing read-only/protocol guards remain unchanged; no production SSH, provider calls, or production mutation occurred. One repeated human-shell live acceptance probe is authorized separately to collect the previously-unreached object-count and AI-quarantine aggregates.


- Telegram MTProto M4BE2 repeated post-deploy acceptance probe: PASS. Human-shell read-only run on production runtime/ref `c69d2353c19c4958e1fb60aac69466fcf6ac1482`, Alembic `0046`, passed all repo/ref/worktree/Compose/DB/API/worker/DB-health guards and completed with `M4BD1_TERMINAL=success`. Exact acceptance aggregates: account count 1; configured folder count 1; active scope 28; manual-active 0; folder-only active 28; latest cursor present 28; history complete 28; backfill cursor present 0; history cutoff present 0; active folder-only peers with >20 stored objects 0; maximum active-peer object count 20; total current-account MTProto objects 638 (440 inbound / 198 outbound); MTProto objects with embeddings 0; MTProto objects with current embedding provenance 0; pending/running embed jobs targeting MTProto objects 0; Telegram network calls 0; DB writes 0. This proves the saved folder reconciled to the expected complete 28-peer scope, every active folder-derived peer completed the intended count-bounded shallow bootstrap without evidence of deep backfill, and Q1 AI quarantine remained effective. Remaining product acceptance is only human confirmation that folder-derived Telegram items are visible through the normal Inbox UI; no Sync or Apply Scope is required.


- Telegram MTProto M4BF1 human Inbox acceptance: PASS. Human confirmed that folder-derived Telegram messages are visible in the normal Inbox UI without manual Sync or Apply Scope. Together with M4BE2, the folder-derived MTProto shallow-bootstrap phase is COMPLETE / PRODUCTION ACCEPTED: complete scope breadth is 28 peers; all 28 are active folder-only selections; all 28 completed count-bounded shallow bootstrap with max observed active-peer object count 20; no backfill cursor or persisted history cutoff exists; MTProto embedding/current-provenance/pending-running embed-job aggregates are all zero; ordinary Inbox visibility is confirmed end-to-end. Bot API remains intentionally enabled for now and its retirement is NOT authorized. Bot API and MTProto remain distinct transports with distinct canonical external-id schemes, so cross-transport duplicate representation is possible during coexistence; no deduplication or Bot API retirement change is authorized in this phase.


- Product direction after Telegram MTProto folder-scope acceptance: Bot API will be retired rather than cross-transport-deduplicated. Cross-transport duplicate representation during the temporary coexistence period is accepted and requires no corrective work. Retirement must be staged: first a local/read-only dependency inventory and removal plan, then code/config retirement under separate authorization, then production runtime disable/removal under explicit production authorization. Existing canonical Telegram objects must not be bulk-deleted merely because their ingest transport is retired unless separately authorized.
- Telegram Bot API M4BG1 retirement dependency audit: COMPLETE / AUDIT ONLY. Active Bot API ingress is `backend/app/api/telegram.py`: `POST /telegram/link` creates `TelegramLinkState`, and `POST /integrations/telegram/webhook` authenticates `X-Telegram-Bot-Api-Secret-Token` and dispatches `TelegramWebhookService`. The service handles `/start` linking, `business_connection`, `business_message`, edited/deleted business messages, and invokes `TelegramObjectMaterializer`; `backend/app/connectors/telegram/transport.py` implements Bot API `getMe`, `setWebhook`, and `sendMessage`; `backend/app/cli/telegram_webhook.py` is the setup command that performs `getMe` and `setWebhook`. `communication_external_action_service.py` still routes Telegram business replies/actions through `TelegramHttpTransport`, `TelegramAccountStore`, and Bot-specific external IDs.

- Bot API configuration remains in `backend/app/core/config.py`, `.env.example`, and both API/worker environments in `infra/compose.yaml`: `TELEGRAM_BOT_TOKEN`, `TELEGRAM_BOT_USERNAME`, `TELEGRAM_WEBHOOK_SECRET`, and `TELEGRAM_WEBHOOK_URL`. `connection_status_service.py`, `/connections`, `client/lib/account/account_screen.dart`, and `client/lib/api/secretary_api_client.dart` expose the legacy Telegram link/Business Bot status and connect control. MTProto UI/API is separate in `telegram_mtproto_account_section.dart` and `/telegram/mtproto/*`; its recurring worker path uses `TelegramMtprotoAccount` and `JOB_TYPE_SYNC_TELEGRAM_MTPROTO`, with no Bot transport dependency.

- Bot-only persistence is `TelegramAccount` (`telegram_accounts`) and `TelegramLinkState` (`telegram_link_states`), introduced by Alembic `0039_telegram_accounts.py`; `TelegramAccountStore`, `TelegramLinkStateStore`, webhook handling, connection status, and Bot action routing reference them. Migrations `0040`–`0046` do not make MTProto depend on these tables. Retain both tables/rows as historical compatibility data during runtime retirement; physical removal would require a later destructive migration and is unnecessary for disabling Bot runtime. Bot-derived object IDs use `business_connection_id|chat_id|message_id` (`normalize.py`), while MTProto objects use `mtproto|account_id|peer_id|message_id` and `metadata.transport=mtproto`; no cross-transport deduplication or ID migration is indicated.

- Bot-only tests are concentrated in `backend/tests/test_telegram_a.py`, `backend/tests/test_telegram_a_send.py`, and `client/test/account/telegram_account_test.dart`; webhook/transport setup is also covered by `app/cli/telegram_webhook.py` tests in `test_telegram_a.py`. Shared communication/assistant tests that mention `business_connection_id` exercise the still-live generic send/action contract and must be separated before deletion. No Bot API call was made during this audit.

- Retirement plan Stage A — code/runtime isolation: first release a guarded code change that stops accepting `/telegram/link` and Bot webhook ingress, disables Telegram entries in `ConnectionStatusService`/legacy account UI, and rejects Telegram Bot send/reply/action routing while leaving MTProto routes, recurring sync, history, visibility, and `TELEGRAM_MTPROTO_AI_ENABLED=false` unchanged. Keep historical Bot models/materialization readable and preserve distinct external IDs; do not perform schema migration or cross-transport deduplication.

- Retirement plan Stage B — production disable: after Stage A is deployed and verified, explicitly remove/disable the Telegram Bot webhook through the Bot administration/runtime control, then remove Bot token, username, webhook secret, and webhook URL from the production environment using the normal environment/deploy process. This task performed none of those actions. Immediate disable is not safe before Stage A because the deployed backend still exposes webhook/link ingress and Bot send/reply paths; disabling credentials first would strand reachable UI/actions and produce configuration failures for existing Bot flows.

- Retirement plan Stage C — cleanup: after a retention decision and compatibility window, remove Bot-only routes, `TelegramHttpTransport`, webhook service, link/account stores, CLI webhook setup, legacy client connect/status models and UI, Bot-only tests/docs/config entries, and only then consider a separately authorized destructive schema migration for `telegram_accounts`/`telegram_link_states`. Retain historical Bot-derived `objects`, their distinct external IDs, and readable metadata; MTProto tables, objects, recurring jobs, and AI quarantine remain independent.


- Telegram Bot API M4BG1 audit review: ACCEPTED. Architect confirms Stage A should be behavioral isolation, not destructive cleanup: legacy Bot link ingress must stop creating link state, webhook ingress must stop materializing/altering Bot-derived state, legacy Bot send/reply actions must fail closed, and legacy Bot connection UI must no longer offer connect/status workflow. MTProto auth/scope/sync/send/reply/Inbox behavior must remain unchanged. Historical Bot-derived objects and legacy Bot tables remain readable/preserved; no schema migration is needed for Stage A.

- Telegram Bot API M4BH1 Stage A behavioral isolation: BUILT / LOCALLY REVIEWED ONLY. `/telegram/link` and the Telegram webhook now fail closed with stable HTTP 410 responses before authentication, DB access, or webhook dispatch. Bot-derived send preparation and legacy Bot send routes raise a retired-transport error before any Bot transport or idempotency write; MTProto send routes remain unchanged. Connection status reports the historical Bot integration as explicitly inactive, and the generic Flutter connections screen no longer exposes Bot connect/help/status controls while retaining the MTProto section. Historical Bot models, tables, objects, and transport code remain present for compatibility; no schema/Alembic, configuration, webhook, deployment, or production change was made. Four DB-independent behavioral tests pass; Python compile and Ruff pass. Flutter formatting/analyzer/test results are recorded with the commit; no production SSH, Bot/provider calls, or production mutation occurred.


- Telegram Bot API M4BH1 review of `fe151f12f64886505253e765b82458710a949e34`: ACCEPTED / READY FOR SCHEMA-NEUTRAL PRODUCTION DEPLOY. Review confirms legacy `POST /telegram/link` and `POST /integrations/telegram/webhook` fail closed with HTTP 410 before auth/DB/provider dispatch; legacy Bot-derived Telegram send/reply preparation and canonical execution both fail closed before Bot transport/idempotency/provider writes; MTProto routes still dispatch through the existing MTProto send path; `/connections` preserves shape but reports Bot inactive; generic Flutter Bot connect/help/status UX is removed while `TelegramMtprotoAccountSection` remains. Historical Bot-derived objects/models/tables remain intact. Exact production->candidate comparison is fast-forward and contains no Alembic/migration changes. Candidate release `fe151f12f64886505253e765b82458710a949e34`; current production/rollback `c69d2353c19c4958e1fb60aac69466fcf6ac1482`; expected Alembic `0046`. No deploy is authorized until explicit human production authorization.


- Telegram Bot API M4BI1 backend deploy: HUMAN AUTHORIZED for exact release `fe151f12f64886505253e765b82458710a949e34`, rollback `c69d2353c19c4958e1fb60aac69466fcf6ac1482`, expected Alembic `0046`. This is a schema-neutral server rollout through the canonical harness only. The harness deploys API+worker, not Flutter; therefore Bot backend isolation may be deployed now, but Stage A client UX retirement still requires a separate client rollout/verification before Stage B webhook/credential removal. No Bot webhook deletion or env/credential change is authorized by this deploy.


- Telegram Bot API M4BI1 Stage A backend production deploy: PASS. Canonical schema-neutral deploy harness reported exact release `fe151f12f64886505253e765b82458710a949e34`, health PASS, Alembic `0046`, DB container unchanged, DB volume unchanged, production `.env` unchanged, API recreated, worker recreated, deployment PASS. GitHub `production` ref is exact release. No migration / no `0047`; no Bot API/provider call, webhook change, credential/env removal, or client rollout occurred. Stage A backend isolation is therefore live: legacy link/webhook/send behavior is fail-closed by release code while MTProto remains the live Telegram transport. Because old clients receive inactive Bot status and server endpoints/send paths are independently retired, Flutter artifact rollout is not a safety prerequisite for Stage B; full dead-UI cleanup may remain a later cleanup item.

- Telegram Bot API M4BJ1 Stage B retirement harness: BUILT / LOCALLY REVIEWED ONLY. Added `ops/production/retire_telegram_bot.sh` and bundled `retire_telegram_bot.py`. The fail-closed protocol validates the canonical target/host pin, exact production release `fe151f12f64886505253e765b82458710a949e34`, clean runtime/ref/health/Alembic state, credential availability, exactly one in-process `deleteWebhook?drop_pending_updates=true` call plus one bounded `getWebhookInfo` read-back, then atomically clears only the four Bot settings while preserving permissions and neutralized non-Bot content. Only API and worker are recreated; DB container/volume, MTProto credentials, credential key, DB credentials, and `TELEGRAM_MTPROTO_AI_ENABLED=false` are verified afterward. Local focused tests, helper compile, wrapper syntax, Ruff, and diff-check pass. The harness was not run: production SSH, Bot API/provider calls, and production mutation are all 0.


- Telegram Bot API M4BJ1 review of `31703dcef0f93443abe67d408fed26cd8430debe`: NOT YET ACCEPTED FOR LIVE STAGE B. The provider/env/service ordering in the implementation is directionally correct, secret values are not emitted, env mutation is atomic and constrained to the four Bot keys, and DB is excluded from service recreation. Three live-run blockers remain: (1) the remote helper checks `origin/production` without first fetching the authoritative remote ref, so a stale tracking ref could pass before irreversible provider/env mutation; (2) the shell wrapper duplicates concrete production target/pin literals instead of treating committed `target.json` as the single source of production identity; (3) the focused suite does not actually execute the destructive ordering boundary through `remote_main`—the provider-failure test calls only the provider helper, so it does not prove provider failure/read-back failure causes zero env/service mutation or that env mutation/recreate occur only after both provider calls succeed. Production remains on `fe151f12f64886505253e765b82458710a949e34`; no Stage B live action is authorized.

- Telegram Bot API M4BJ1R retirement harness hardening: BUILT / LOCALLY REVIEWED ONLY. The streamed helper now fetches `origin/production` immediately before any provider action and requires the exact production release; the wrapper fetches both `main` and `production` and obtains SSH target, port, host pin, repository path, and origin exclusively through the validated `target.json` loader. Remote control-flow tests now execute mocked `remote_main` for delete failure, read-back failure, full success ordering, fresh-ref fetch failure, and fresh-ref mismatch, proving zero env/service effects before provider success and exactly two provider calls on success. Sixteen focused tests pass; local/bundled compile, Bash syntax, Ruff, and diff-check pass. No live harness execution, SSH, Bot API/provider call, webhook deletion, environment mutation, or service recreation occurred.


- Telegram Bot API M4BJ1R review of `e7cbef5bac1a13a3dd56cd2eb3201e42f3b1b3f0`: ACCEPTED / READY FOR ONE LIVE STAGE B RETIREMENT RUN, subject to explicit human production authorization. Review confirms the remote helper fetches and verifies the authoritative `origin/production` ref before provider mutation, the wrapper derives production identity only from validated `target.json`, and mocked `remote_main` regressions prove delete failure/read-back failure stop before env/service mutation, fresh-ref failures make zero provider calls, and full success orders delete -> read-back -> atomic four-key env clear -> API/worker-only recreate with exactly two provider calls. Secret values remain out of subprocess argv/protocol output; DB container/volume, MTProto credentials, credential key, DB credentials, AI=false, health, and Alembic 0046 are postflight invariants. Current production remains `fe151f12f64886505253e765b82458710a949e34`. No live Stage B action has occurred.


- Telegram Bot API M4BK1 Stage B live retirement: EXPLICITLY HUMAN AUTHORIZED. One execution only of the architect-accepted `ops/production/retire_telegram_bot.sh` harness is authorized against production runtime/ref `fe151f12f64886505253e765b82458710a949e34`, Alembic `0046`. Authorized effects are limited to exactly one Bot `deleteWebhook(drop_pending_updates=true)`, one bounded `getWebhookInfo` verification, clearing only the four Bot environment settings, and recreating only API + worker with all accepted DB/MTProto/health invariants. No retry, direct SSH, manual Bot API action, webhook restore, Bot secret restore, BotFather/account destruction, Stage C cleanup, deploy/rollback/ref movement, MTProto change, or AI enablement is authorized.


- Telegram Bot API M4BK1 first authorized live Stage B run: SAFE PREFLIGHT FAILURE / ZERO PROVIDER MUTATION. Sanitized output reached canonical repo/target pin/exact runtime+production ref/clean worktree/Compose/DB+API+worker running markers, then stopped immediately after the current `DB_HEALTH_PASS` marker with `FAILURE_STAGE=STAGE_UNKNOWN`, `RAW_EXCEPTION_CLASS=RuntimeError`, `TELEGRAM_NETWORK_CALLS=0`. Because the failure occurred before the first provider boundary, no `deleteWebhook`, read-back, Bot env clearing, or API/worker recreation occurred; no retry is authorized. Architect root cause: the retirement harness uses an unproven Alembic check that invokes `psql` inside the API container against `127.0.0.1`, while the accepted production contract uses `compose exec -T api alembic current`. Additional review found the emitted `DB_HEALTH_PASS` is currently backed by the API HTTP health call rather than direct DB container health, deterministic four-key env-transform validation happens only after webhook deletion, and post-recreate checks verify desired Compose environment but not actual recreated API/worker container identities/environments. These are harness defects only; production remains on `fe151f12f64886505253e765b82458710a949e34`, Alembic last accepted as `0046`, Bot webhook/credentials remain unchanged by this failed run.

- Telegram Bot API M4BK1R harness hardening: BUILT / LOCALLY REVIEWED ONLY. Alembic pre/postflight now use `docker compose ... exec -T api alembic current` and require exact `0046 (head)` with explicit `STAGE_0_ALEMBIC` / `STAGE_6_ALEMBIC_POST` failures. DB health is taken from Docker container state/health metadata, separate from application HTTP health. The four-key env transform is planned and validated before provider deletion, then written only if the file remains byte-identical; preflight validates resolved API/worker credentials and AI=false. API/worker IDs and running state are checked after recreation, DB ID/volume remain unchanged, and actual container environments are inspected postflight. Twenty local control-flow/regression tests pass, including Alembic, unhealthy DB, env race, credential mismatch, provider ordering and ref failures. No live run, SSH, provider call, webhook change, env mutation, or service recreation occurred.


- Telegram Bot API M4BK1R review of `f2eb592a9f7103006d5edbbdf6c9a547b87250bb`: NOT YET ACCEPTED FOR SECOND LIVE RUN. The core hardening is correct: canonical `alembic current`, direct DB health, pre-provider four-key env planning, resolved MTProto/Bot/protected-env checks, actual API/worker recreation IDs, DB/volume preservation, and actual running-container environment inspection are implemented. One final live-run blocker remains: several expected pre/postflight failures still fall through to generic `STAGE_UNKNOWN` instead of an exact sanitized stage (notably application health preflight and multiple post-recreate DB/container/environment/health checks). After irreversible webhook deletion/env clearing, exact stage attribution is required to distinguish which retirement effects completed. Regression coverage also must prove malformed/missing/duplicate Bot env encountered through `remote_main` yields zero provider calls, and that actual new API+worker container environments are both inspected. Production remains `fe151f12f64886505253e765b82458710a949e34`; no second live run is authorized.

- Telegram Bot API M4BK1R2 failure protocol finalization: BUILT / LOCALLY REVIEWED ONLY. Expected application health, DB/container preservation, recreation identity/running, Bot-settings, Compose environment, actual container environment, protected/AI invariants, and postflight health failures now emit deterministic sanitized stages; `STAGE_UNKNOWN` remains only for unexpected exceptions. Added remote_main regressions for malformed/duplicate/missing Bot env, actual API/worker environment inspection and worker-environment failure, plus Alembic exact-head contract and DB health gating. Twenty-four focused tests pass; helper/bundled compile, Bash syntax, Ruff, and diff-check pass. No second live run, SSH, webhook action, production env mutation, or service recreation occurred.


- Telegram Bot API M4BK1R2 review of `f2e3ef441368d52b7583f0604df127504dbffdf7`: ACCEPTED / READY FOR ONE SECOND LIVE STAGE B RUN, subject to fresh explicit human production authorization. Deterministic sanitized stages now cover all expected pre/postflight boundaries; malformed/missing/duplicate Bot env is proven to fail through `remote_main` before provider calls; both newly recreated API and worker container environments are explicitly inspected; provider calls remain exactly one `deleteWebhook(drop_pending_updates=true)` plus one `getWebhookInfo`; no retry/restore path exists; DB/container/volume, non-Bot env, MTProto credentials, protected credentials, AI=false, health, and Alembic 0046 remain guarded. Production remains `fe151f12f64886505253e765b82458710a949e34`. The first live authorization was consumed by a zero-provider preflight failure, so a second live run requires new explicit human authorization.


- Telegram Bot API M4BL1 second live Stage B retirement: EXPLICITLY HUMAN AUTHORIZED. Exactly one execution of the architect-accepted `ops/production/retire_telegram_bot.sh` is authorized against production runtime/ref `fe151f12f64886505253e765b82458710a949e34`, Alembic `0046`. The accepted harness code is from `f2e3ef441368d52b7583f0604df127504dbffdf7`; later main commits only update task/state ledgers. Authorized effects are limited to one `deleteWebhook(drop_pending_updates=true)`, one bounded `getWebhookInfo`, clearing only the four Bot env settings, and recreating only API+worker with all accepted DB/MTProto/health/Alembic invariants. No retry, direct SSH, manual provider action, webhook restore, Bot-secret restore, BotFather/account destruction, Stage C cleanup, deploy/rollback/ref movement, MTProto change, or AI enablement is authorized.


- Telegram Bot API M4BL1 second live Stage B retirement: IRREVERSIBLE CORE ACTIONS COMPLETED / FINAL HEALTH UNCONFIRMED. The one-shot harness passed canonical repo/target/ref/worktree, Compose, DB/API/worker running, DB health, application pre-health, Alembic 0046, Bot/MTProto credential preflight, then completed exactly one `deleteWebhook(drop_pending_updates=true)` and one `getWebhookInfo` with `WEBHOOK_EMPTY_PASS=true`. It atomically cleared the four Bot env settings, recreated only API+worker, proved DB container and volume unchanged, proved non-Bot env unchanged, proved Bot settings empty, and preserved MTProto credentials, credential key, DB credential, and `TELEGRAM_MTPROTO_AI_ENABLED=false`. It then failed only at `STAGE_6_APP_HEALTH` with `TELEGRAM_NETWORK_CALLS=2`. Do NOT rerun the retirement harness and do NOT restore Bot secrets/webhook. Because the harness uses a single immediate post-recreate HTTP health check while the canonical deploy harness allows bounded retries, the remaining question is whether this was only an API startup race or a persistent runtime failure. Stage B is materially applied, but final production acceptance requires a separate read-only post-retirement verification.

- Telegram Bot API M4BM1 post-retirement verifier: BUILT / LOCALLY VERIFIED ONLY. Added `ops/production/verify_telegram_bot_retirement.sh` and bundled `verify_telegram_bot_retirement.py`. The read-only verifier validates the pinned target, canonical repository, exact production release/ref, clean worktree, running/healthy DB/API/worker, DB identity/volume observation, empty Bot settings in `.env`, resolved Compose, and actual API/worker environments; preserves MTProto/protected credentials and AI=false; checks exact Alembic `0046`; and retries application health up to 30 attempts with 2-second spacing without restart or mutation. The protocol is sanitized and fail-closed with zero provider/network, DB-write, environment-write, and service-recreation counters. Seventeen focused tests plus compile, bundled compile, Bash syntax, Ruff, and diff-check pass. The verifier was not run: production SSH, live verifier, Telegram/Bot API calls, webhook changes, environment/DB mutation, service recreation, and deploy are all 0.


- Telegram Bot API M4BM1 review of `aaa09236c57c780403d3096d410416330532d9aa`: NOT YET ACCEPTED FOR LIVE VERIFICATION. The verifier is read-only in its production effects, uses bounded application-health retries, checks Bot env emptiness / MTProto and protected credentials / AI=false / Alembic 0046, and contains no Telegram provider or service-recreation path. One security blocker remains in the output boundary: failure transcripts are accepted when they contain arbitrary extra unknown fields, so an unexpected line could pass validation and then be printed by the wrapper. A strict verifier must whitelist/order failure output and reject any unknown line before stdout is surfaced. Two small hardening items should be corrected in the same pass: explicitly inspect API and worker Docker running state rather than relying only on Compose `ps -q`, and restore `BatchMode=yes` in the SSH wrapper for deterministic noninteractive execution. Production remains `fe151f12f64886505253e765b82458710a949e34`; no live verifier is authorized.

- Telegram Bot API M4BR1 Stage C post-deploy verifier: BUILT / LOCALLY VERIFIED ONLY. Added a pinned, read-only verifier for runtime `bd1433921a056b8dc42bd23c6ecf0e4ce2bedf4` and Alembic `0046`. It checks canonical refs, runtime health, retired Bot routes, required MTProto route, Settings/container environment invariants, exactly one MTProto account, active scope 28, aggregate legacy Bot object preservation, and generic Inbox readability without exposing IDs/content. The bundled API child performs only aggregate/read eligibility queries; protocol counters remain zero for Telegram/provider calls, DB writes, environment writes, and service recreation. Focused tests, compile, Bash syntax, Ruff, and diff-check pass. No live verifier or production action occurred.

- Telegram Bot API M4BR1R verifier correction: BUILT / LOCALLY VERIFIED ONLY. The historical-read check now scans a deterministic bounded set of legacy non-MTProto candidates ordered by `created_at, id` and invokes canonical `RecentSourceService.get_inbox_eligible()` until any eligible object is found. If the bound is exhausted without proof, the verifier fails closed; no object identifiers or content are emitted. Added regressions for later eligible candidates, no eligible candidates, and output secrecy. Seventeen focused tests plus compile, bundled compile, Bash syntax, Ruff, and diff-check pass. No live verifier or production action occurred.

- Telegram Bot API M4BS1R authoritative ref correction: BUILT / LOCALLY VERIFIED ONLY. The Stage C wrapper and bundled remote helper now use strict `git ls-remote origin refs/heads/<branch>` parsing for authoritative `main` and `production` SHA checks, without fetching or trusting local remote-tracking refs. Malformed, duplicate, unexpected, stale, or mismatched refs fail closed before SSH/runtime inspection. Twenty-five focused tests plus compile, bundled compile, Bash syntax, Ruff, and diff-check pass. No live verifier or production action occurred.

- Telegram Bot API M4BM1R verifier hardening: BUILT / LOCALLY VERIFIED ONLY. Failure parsing now has a strict ordered whitelist: known PASS prefix plus exact sanitized failure tail; unknown, duplicate, malformed, out-of-order, or nonzero fields are rejected before wrapper stdout is surfaced. API and worker container IDs are explicitly inspected for `Status=running` before their PASS markers, with deterministic stopped-container stages. The SSH wrapper uses noninteractive `BatchMode=yes`. Twenty focused tests pass, including unknown-output, stopped API, and stopped worker regressions; no live verifier or production action occurred.


- Telegram Bot API M4BM1R review of `27f54763bd04c72a98046a5d649b703072e0369f`: ACCEPTED / READY FOR ONE LIVE READ-ONLY POST-RETIREMENT VERIFICATION, subject to fresh explicit human production authorization. Review confirms failure transcripts are strict ordered allowlist only and unknown/duplicate/malformed/nonzero fields are rejected before wrapper stdout is surfaced; API and worker Docker states are explicitly required `running`; SSH uses `BatchMode=yes`; no Telegram provider, DB/env write, service restart/recreate, deploy, or ref-movement capability was introduced. Production remains `fe151f12f64886505253e765b82458710a949e34`. Stage B irreversible core actions remain materially applied; final production acceptance now depends only on this read-only verification.


- Telegram Bot API M4BN1 live read-only post-retirement verification: EXPLICITLY HUMAN AUTHORIZED. Exactly one execution of the architect-accepted `ops/production/verify_telegram_bot_retirement.sh` is authorized against production runtime/ref `fe151f12f64886505253e765b82458710a949e34`, Alembic `0046`. The verifier is observation-only: zero Telegram/Bot API/provider calls, zero DB writes, zero env writes, zero service restart/recreate, zero deploy/ref movement. No retry is authorized if the verifier blocks or fails.


- Telegram Bot API M4BN1 live read-only post-retirement verification: PASS / STAGE B PRODUCTION ACCEPTED. The verifier confirmed exact production runtime/ref `fe151f12f64886505253e765b82458710a949e34`, clean tracked worktree, DB/API/worker running, DB healthy, Bot settings empty in production `.env`, resolved Compose env, and actual API/worker container environments; MTProto credentials, credential key, and DB credential are preserved; `TELEGRAM_MTPROTO_AI_ENABLED=false`; application health PASS; Alembic exact `0046`. The verifier reported `TELEGRAM_NETWORK_CALLS=0`, `DB_WRITES=0`, `ENV_WRITES=0`, `SERVICE_RECREATIONS=0`, and terminal success. Therefore the earlier `STAGE_6_APP_HEALTH` was a startup-timing race rather than a persistent production failure. Telegram Bot API Stage B retirement is COMPLETE / PRODUCTION ACCEPTED: webhook is deleted, Bot credentials are removed from runtime configuration, Bot ingress/send behavior is already retired in application code, and MTProto remains the sole live Telegram transport. Historical Bot-derived objects and legacy schema remain preserved. The next irreversible external step is optional human BotFather/account destruction under separate explicit authorization; Stage C code/schema cleanup remains separate.


- Telegram Bot API M4BO1 Bot account destruction: EXPLICITLY HUMAN AUTHORIZED. Stage B runtime retirement is already COMPLETE / PRODUCTION ACCEPTED; this authorization is limited to deleting the retired Telegram bot itself through the human-controlled @BotFather flow. No Secretary production/env/DB/deploy/MTProto action is authorized. Historical Bot-derived objects and legacy schema remain preserved. Telegram's current official BotFather documentation states that `/deletebot` deletes the selected bot and frees its username and cannot be undone.


- Telegram Bot API M4BO1 human BotFather destruction: COMPLETE. Human reported `TELEGRAM_BOT_ACCOUNT_DELETED` after the explicitly authorized BotFather deletion flow. Combined with accepted Stage B, the legacy Telegram Bot API integration is now fully retired externally and at runtime: webhook deleted, Bot runtime credentials cleared, application ingress/link/send paths retired, and the bot account itself destroyed. MTProto remains the sole live Telegram transport. Historical Bot-derived canonical objects and legacy Bot schema remain preserved pending separate retention/destructive-schema decisions.

- Telegram Bot API M4BP1 Stage C cleanup: BUILT / LOCALLY VERIFIED ONLY. Removed the retired Bot API router/link and webhook endpoints, Bot HTTP transport, webhook service, webhook CLI, Bot settings from active config, Bot exports from Compose and `.env.example`, and dead client Bot connection/link DTOs. Removed active legacy Bot send/route execution while preserving explicit fail-closed behavior for historical Bot anchors. Historical `TelegramAccount`, `TelegramLinkState`, canonical Bot-derived objects, normalization/materialization readers, and all migrations including `0039` remain intact; MTProto transport/session/scope/send UI remain unchanged. Added focused cleanup tests. PostgreSQL-backed suites were blocked by unavailable local PostgreSQL; DB-independent checks, Python compile, Ruff, Flutter focused test/analyzer, and diff-check were run. No production/provider/MTProto action occurred.


- Telegram Bot API M4BP1 review of `0789fc1c21b643667be0e20e328d14d14f32438f`: NOT YET ACCEPTED FOR DEPLOY. Code review found no schema/runtime blocker: production startup remains compatible with the four empty legacy Bot lines because Settings uses `extra="ignore"`; Compose no longer passes Bot settings; non-MTProto Telegram objects remain visible through the generic Inbox predicate; legacy `TelegramSendRoute` remains parseable and execution fails closed without MTProto reroute; no migration is present. Acceptance is blocked only by regression/maintenance gaps: the required historical Bot-derived generic Inbox/read regression is missing; the active-config test checks uppercase names even in Python `config.py` and would not catch lowercase `telegram_bot_*` fields; retained Bot-only stores are not explicitly marked legacy-only as required by the Stage C retention boundary; and `.env.example` has a duplicated MTProto comment. No production action is authorized.

- Telegram Bot API M4BP1R compatibility correction: BUILT / LOCALLY VERIFIED ONLY. Added a DB-independent regression proving a legacy Bot-derived Telegram object remains consumable by `ObjectOut` and generic recent-source projection helpers while retaining non-MTProto visibility. Strengthened active-config assertions to reject lowercase Bot settings in Python config and uppercase settings in Compose/example; marked retained account/link stores as legacy-only compatibility helpers; removed the duplicate MTProto example comment. Focused tests, Python compile, Ruff, and diff-check pass. No production SSH, provider calls, schema/data mutation, deploy, or MTProto behavior change occurred.


- Telegram Bot API M4BP1/M4BP1R review of cleanup candidate `bd1433921a056b8dc42bd23c6ecf0e4ce2bedf4b`: ACCEPTED / READY FOR SCHEMA-NEUTRAL PRODUCTION DEPLOY. Review confirms legacy Bot link/webhook/router/HTTP transport/send runtime and active Bot config/client DTO surfaces are removed; historical Bot-derived canonical objects remain readable through generic read projection and are not restricted by the MTProto-only visibility predicate; legacy `TelegramSendRoute` compatibility remains parseable and execution fails closed without reroute to MTProto; retained account/link-state persistence helpers are marked legacy-only; old DB models/tables/migrations including 0039 remain intact; MTProto runtime/send/mutation/account behavior remains. Production `.env` may retain the already-empty legacy Bot lines safely because Settings uses `extra="ignore"`, while candidate Compose no longer injects them into API/worker. Exact production->candidate relation is fast-forward with no migration files; expected Alembic remains `0046`. Current production/rollback is `fe151f12f64886505253e765b82458710a949e34`. No production deploy is authorized until explicit human approval.


- Telegram Bot API M4BQ1 Stage C production deploy: EXPLICITLY HUMAN AUTHORIZED for exact release `bd1433921a056b8dc42bd23c6ecf0e4ce2bedf4b`, rollback `fe151f12f64886505253e765b82458710a949e34`, expected Alembic `0046`. Exact rollback->release relation is fast-forward with no migration files. Deploy is canonical schema-neutral API+worker rollout only; production `.env`, DB container/volume, historical Bot-derived objects, legacy Bot schema, and MTProto behavior must remain unchanged.


- Telegram Bot API M4BQ1 Stage C schema-neutral production deploy: PASS. Canonical deploy harness reported exact release `bd1433921a056b8dc42bd23c6ecf0e4ce2bedf4b`, health PASS, Alembic `0046`, DB container unchanged, DB volume unchanged, production `.env` unchanged, API recreated, worker recreated, deployment PASS. GitHub `production` ref is exact release. No migration / no `0047`. Stage C server cleanup is therefore live: retired Bot routes/runtime/config execution surfaces are removed while historical Bot-derived objects/schema remain preserved and MTProto remains the sole live Telegram transport. Final Stage C acceptance should now be read-only and prove route/config absence, historical-row readability, and MTProto invariants without provider calls or mutations.


- Telegram Bot API M4BR1 review of `ababcfacf6526fcd71ccaa752278dfe268be9a1e`: NOT YET ACCEPTED FOR LIVE RUN. The verifier is read-only, strict-sanitized, pinned, and correctly checks Stage C route/config/container/MTProto invariants. One correctness blocker remains in historical-read acceptance: the child selects an arbitrary legacy Bot-derived object with `.limit(1)` and then requires that exact row to be Inbox-eligible. Legacy Bot history can legitimately contain outbound/hidden/deleted rows that generic Inbox intentionally excludes, so this can false-fail even when other preserved legacy Bot objects remain readable. The acceptance requirement is existential: at least one historical Bot-derived object must be readable. The verifier must prove existence of an eligible legacy row rather than eligibility of an arbitrary sample. No live verifier is authorized.


- Telegram Bot API M4BR1R review of `bfea4b308e7513cc743da573d7934e85ff51b393`: ACCEPTED / READY FOR ONE LIVE READ-ONLY STAGE C ACCEPTANCE RUN, subject to fresh explicit human production authorization. The verifier now checks the existential requirement correctly: it scans a deterministic bounded set of legacy non-MTProto Telegram objects ordered by `created_at, id`, applies canonical `RecentSourceService.get_inbox_eligible()` semantics to each, succeeds when any preserved legacy object is Inbox-readable, and fails closed if the candidate bound is exhausted without proof. No identifiers/content are emitted. Strict sanitized protocol, exact production pin/ref, route/config/container checks, MTProto account count/scope, AI=false, health/Alembic, and zero provider/write/recreate capabilities remain intact. Production remains `bd1433921a056b8dc42bd23c6ecf0e4ce2bedf4b`.


- Telegram Bot API M4BS1 final live read-only Stage C acceptance: EXPLICITLY HUMAN AUTHORIZED. Exactly one execution of the architect-accepted `ops/production/verify_telegram_bot_stage_c.sh` is authorized against production runtime/ref `bd1433921a056b8dc42bd23c6ecf0e4ce2bedf4b`, Alembic `0046`. The verifier is observation-only: zero Telegram/provider calls, zero DB writes, zero env writes, zero service restart/recreate, zero deploy/ref movement. No retry is authorized if the verifier blocks or fails.


- Telegram Bot API M4BS1 first final Stage C live-verifier attempt: SAFE LOCAL BLOCK / NO PRODUCTION ACCESS. Wrapper stopped at `M4BR1_BLOCKED=local_production_ref` before target parsing/SSH, so production SSH=0, provider calls=0, DB/env writes=0, and service recreation=0. GitHub confirms canonical `production` still points to exact accepted runtime `bd1433921a056b8dc42bd23c6ecf0e4ce2bedf4b`. Root cause is verifier ref-validation logic: both local wrapper and remote helper use explicit `git fetch ... production` followed by `rev-parse origin/production`; an explicit fetch ref without a destination does not reliably refresh the remote-tracking ref, so a stale `origin/production` can false-fail. Corrective direction: use authoritative read-only `git ls-remote origin refs/heads/production` (and analogous main freshness where needed) rather than relying on local tracking-ref mutation. The authorized one-shot run is consumed; no retry is authorized until corrected code is reviewed and fresh human authorization is given.


- Telegram Bot API M4BS1R review of `dbf1b14941e7582346db17205823b130dfcd59ba`: ACCEPTED / READY FOR ONE REPLACEMENT FINAL LIVE READ-ONLY STAGE C ACCEPTANCE RUN, subject to fresh explicit human production authorization. The verifier now obtains authoritative `main` and `production` SHAs through strict `git ls-remote origin refs/heads/<branch>` parsing and no longer depends on stale remote-tracking refs or fetch side effects. Local main freshness and production release are checked before SSH; the remote helper checks authoritative production before Docker/DB inspection. Malformed, duplicate, unexpected, uppercase, or mismatched ref output fails closed. Production remains exact `bd1433921a056b8dc42bd23c6ecf0e4ce2bedf4b`. The prior M4BS1 authorization was consumed by a local pre-SSH false blocker, so a replacement live run requires new explicit human authorization.


- Telegram Bot API M4BT1 replacement final live read-only Stage C acceptance: EXPLICITLY HUMAN AUTHORIZED. Exactly one execution of the architect-accepted `ops/production/verify_telegram_bot_stage_c.sh` is authorized against production runtime/ref `bd1433921a056b8dc42bd23c6ecf0e4ce2bedf4b`, Alembic `0046`. The verifier includes the accepted authoritative-ref correction from `dbf1b14941e7582346db17205823b130dfcd59ba` and remains observation-only: zero Telegram/provider calls, zero DB writes, zero env writes, zero service restart/recreate, zero deploy/ref movement. No retry is authorized if this replacement run blocks or fails.


- Telegram Bot API M4BT1 replacement final Stage C live-verifier attempt: SAFE LOCAL BLOCK / NO PRODUCTION ACCESS. Wrapper again stopped before SSH with `M4BR1_BLOCKED=local_production_ref`. GitHub independently confirms canonical `production` is still exact `bd1433921a056b8dc42bd23c6ecf0e4ce2bedf4b`. Because the wrapper reached `local_production_ref` rather than `authoritative_production`, its `git ls-remote origin refs/heads/production` call succeeded and returned a different SHA. Root cause is the remaining local trust-order bug: the wrapper queries symbolic remote name `origin` before validating that local `origin` equals the canonical repository URL. A noncanonical/mispointed local origin can therefore produce a false blocker even though canonical GitHub production is correct. No SSH/provider/DB/env/service action occurred. Corrective direction: load/validate target and canonical local origin before remote-branch checks, and perform authoritative `ls-remote` against the validated canonical URL itself rather than symbolic `origin`.

- Telegram Bot API M4BT1R canonical ref bootstrap: BUILT / LOCALLY VERIFIED ONLY. The Stage C wrapper now validates `target.json`, the local repository root, branch `main`, a clean worktree, and local `origin` against the canonical repository URL before any branch lookup. Authoritative `main` and `production` SHAs come from strict single-line `git ls-remote <canonical-url> refs/heads/<branch>` parsing. A wrong local origin fails as `wrong_local_origin` before branch comparison and before SSH. The bundled remote helper checks the remote origin, exact HEAD, and authoritative production ref before the clean-worktree check and before Docker/DB inspection. The expected release constant is the full accepted SHA `bd1433921a056b8dc42bd23c6ecf0e4ce2bedf4b`. No fetch, checkout, reset, or ref mutation is used. Thirty-nine focused tests plus compile, bundled compile, Bash syntax, Ruff, and diff-check pass. No live verifier, production SSH, provider call, or production mutation occurred.


- Telegram Bot API M4BT1R review of `3c6da496fa0d6bb5dc7d4d37b22190c473a5a0bd`: canonical Git bootstrap correction ACCEPTED, but verifier is NOT YET READY FOR LIVE RUN due a separate pre-existing protocol-consistency bug found during architect review. Root-cause correction for the two prior `M4BR1_BLOCKED=local_production_ref` attempts: the verifier's `RELEASE` constant was accidentally 39 characters (`...edf4`) instead of the full accepted 40-character SHA `bd1433921a056b8dc42bd23c6ecf0e4ce2bedf4b`. This alone deterministically explains both prior local production-ref mismatches; earlier tracking-ref and symbolic-origin diagnoses were useful hardening findings but were not the direct cause of those observed failures. Commit `3c6da496...` fixes the release constant and hardens trust ordering / explicit canonical-URL `ls-remote` checks. New blocker: `SUCCESS_FIELDS` does not match the actual emission order of `remote_main()` (notably APP_HEALTH/ALEMBIC and route-vs-environment markers), so a real success transcript — and several late failure transcripts — would be rejected by the strict local parser as `remote_protocol`. Also, Stage C requires Bot variables to be absent from Compose/API/worker runtime environments, while current `_require_empty` accepts present-but-empty keys; the production `.env` itself may retain empty legacy lines. No live verifier is authorized until these verifier-only issues are corrected and covered end-to-end.

- Telegram Bot API M4BU1 protocol consistency: BUILT / LOCALLY VERIFIED ONLY. Factual root cause of both prior `local_production_ref` blockers remains the 39-character `RELEASE` constant; commit `3c6da496fa0d6bb5dc7d4d37b22190c473a5a0bd` already restored `bd1433921a056b8dc42bd23c6ecf0e4ce2bedf4b` and the canonical-URL Git bootstrap, and that bootstrap was not regressed. `SUCCESS_FIELDS` now follows `remote_main()` emission order: Alembic before app health, then environment markers, then route/settings markers. An end-to-end mocked `remote_main()` transcript is accepted by `parse_output()`, and sanitized failures after Alembic, after health, and after environment markers are accepted. Production `.env` may omit the four Bot keys or keep them empty; Compose and actual API/worker container environments must omit them entirely, including present-but-empty keys. Forty-eight focused tests plus compile, bundled compile, Bash syntax, Ruff, and diff-check pass. No live verifier, production SSH, provider call, or production mutation occurred.


- Telegram Bot API M4BU1 review of `7a9396a502224b02fb0037628531e4df6c9ada4d`: ACCEPTED / READY FOR ONE FINAL LIVE READ-ONLY STAGE C ACCEPTANCE RUN, subject to fresh explicit human authorization. The strict success protocol now matches the real `remote_main()` emission order exactly, and end-to-end tests capture actual `remote_main()` stdout and feed it through `parse_output()`; success plus sanitized APP_HEALTH, ENVIRONMENT, and READ_ONLY_STATE failure paths are accepted, while unknown/duplicate/out-of-order fields and nonzero mutation/provider counters remain rejected. The Bot environment contract is now correct: production `.env` may omit the four retired Bot keys or retain them empty, but resolved Compose API/worker environments and actual API/worker container environments must omit them entirely. Canonical Git bootstrap from `3c6da496...` remains intact and the verifier uses the full accepted release SHA `bd1433921a056b8dc42bd23c6ecf0e4ce2bedf4b`. No production SSH/provider/write/recreate action occurred in M4BU1.


- Telegram Bot API M4BV1 final live read-only Stage C acceptance after M4BU1: EXPLICITLY HUMAN AUTHORIZED. Exactly one execution of `ops/production/verify_telegram_bot_stage_c.sh` is authorized against production runtime/ref `bd1433921a056b8dc42bd23c6ecf0e4ce2bedf4b`, expected Alembic `0046`. Accepted verifier baseline includes canonical Git bootstrap commit `3c6da496fa0d6bb5dc7d4d37b22190c473a5a0bd` and protocol/runtime-env commit `7a9396a502224b02fb0037628531e4df6c9ada4d`. The run is observation-only: Telegram/provider calls=0, DB writes=0, env writes=0, service restart/recreate=0, deploy/ref movement=0. If it blocks or fails, no retry is authorized without new review and explicit human authorization.


- Telegram Bot API M4BV1 final Stage C live read-only acceptance after M4BU1: VALID READ-ONLY REMOTE FAILURE at `STAGE_2_READ_ONLY_STATE`. The remote verifier successfully proved exact production HEAD/ref, clean worktree, DB/API/worker running, DB health, Alembic `0046`, application health, retired Bot runtime env absence, MTProto/protected credential preservation, and `TELEGRAM_MTPROTO_AI_ENABLED=false`. It then failed inside the bundled Stage 2 child before returning its structured read-state values. Sanitized terminal counters were all zero: Telegram/provider calls=0, DB writes=0, env writes=0, service recreations=0. The trailing local `M4BR1_BLOCKED=ssh_failed` is a misleading wrapper label, not an SSH transport failure: SSH succeeded and delivered a valid sanitized remote failure transcript; the remote command simply exited nonzero. Because Stage 2 currently collapses route/settings/import/query/bound failures inside one `docker compose exec ... python -c` into outer `RuntimeError`, the exact read-only cause cannot be determined from this run without guessing. No retry is authorized until Stage 2 emits deterministic sanitized substages and the wrapper distinguishes valid remote verifier failure from actual SSH transport failure.

- Telegram Bot API M4BW1 Stage 2 diagnostics: BUILT / LOCALLY VERIFIED ONLY. The production cause of the M4BV1 `STAGE_2_READ_ONLY_STATE` failure was not inferred. The child now catches each Stage 2 class internally and emits only `CHILD_STATUS` plus an allowlisted `CHILD_STAGE`, with no exception text, SQL, IDs, content, or env values. Outer failures are `STAGE_2_BOOTSTRAP`, `STAGE_2_LEGACY_ROUTES`, `STAGE_2_MTPROTO_ROUTE`, `STAGE_2_BOT_SETTINGS`, `STAGE_2_DB_SESSION`, `STAGE_2_ACCOUNT`, `STAGE_2_SCOPE`, `STAGE_2_LEGACY_AGGREGATE`, `STAGE_2_LEGACY_CANDIDATES`, `STAGE_2_INBOX_READ`, `STAGE_2_CANDIDATE_BOUND`, plus `STAGE_2_CHILD_PROTOCOL` and `STAGE_2_CHILD_EXECUTION` when the child protocol or process itself fails. A valid remote `M4BR1_TERMINAL=failure` is printed once and exits nonzero without `M4BR1_BLOCKED=ssh_failed`; an SSH transport failure without a valid transcript remains `ssh_failed` and does not surface stderr. Sixty-eight focused tests plus compile, bundled compile, Bash syntax, Ruff, and diff-check pass. No live verifier, production SSH, provider call, or production mutation occurred.


- Telegram Bot API M4BW1 review of `65309f1060990e5cb3ca639a0a1d7eb699a07881`: ACCEPTED / READY FOR ONE LIVE READ-ONLY STAGE 2 DIAGNOSTIC RUN, subject to fresh explicit human authorization. The Stage 2 child now emits a strict sanitized internal protocol and maps import/bootstrap, route, Settings, DB/session, account, scope, legacy aggregate, candidate query, Inbox-read, and bound-exhaustion failures to deterministic allowlisted `STAGE_2_*` codes without exception text, SQL, identifiers, content, or env values. Child execution failure and malformed child output are separately mapped to `STAGE_2_CHILD_EXECUTION` and `STAGE_2_CHILD_PROTOCOL`. The wrapper now distinguishes a valid remote verifier terminal failure from an SSH transport failure: a valid `M4BR1_TERMINAL=failure` transcript is emitted once and exits nonzero without the misleading `M4BR1_BLOCKED=ssh_failed`; actual SSH transport failure remains sanitized as `ssh_failed`. Strict top-level parsing and zero provider/write/recreate invariants remain intact.


- Telegram Bot API M4BX1 live read-only Stage 2 diagnostic: EXPLICITLY HUMAN AUTHORIZED. Exactly one execution of `ops/production/verify_telegram_bot_stage_c.sh` is authorized against production runtime/ref `bd1433921a056b8dc42bd23c6ecf0e4ce2bedf4b`, expected Alembic `0046`, using accepted diagnostic verifier commit `65309f1060990e5cb3ca639a0a1d7eb699a07881`. Purpose is diagnostic only: identify a fixed sanitized Stage 2 substage or complete success. Telegram/provider calls=0, DB writes=0, env writes=0, service restart/recreate=0, deploy/ref movement=0. If this run fails or blocks, no retry is authorized without new review and explicit human authorization.


- Telegram Bot API M4BX1 live read-only Stage 2 diagnostic: VALID SANITIZED FAILURE at `STAGE_2_BOOTSTRAP`. All previously proven Stage 0/1 invariants again passed: exact production HEAD/ref, clean worktree, DB/API/worker running, DB health, Alembic `0046`, application health, retired Bot runtime env absence, MTProto/protected credentials preserved, and `TELEGRAM_MTPROTO_AI_ENABLED=false`. Provider calls=0, DB writes=0, env writes=0, service recreations=0. The wrapper correctly emitted the remote failure once and did not append the former misleading `ssh_failed`. Repository review confirms every named Stage 2 symbol exists in production release `bd1433921a056b8dc42bd23c6ecf0e4ce2bedf4b`; therefore the current `STAGE_2_BOOTSTRAP` remains too broad to identify a concrete import/runtime cause and should be treated as a verifier diagnostic-design defect, not as evidence of a product-data failure. No retry is authorized until the monolithic Stage 2 bootstrap is removed or decomposed.

- Telegram Bot API M4BY1 Stage 2 bootstrap removal: BUILT / LOCALLY VERIFIED ONLY. The import that produced the M4BX1 `STAGE_2_BOOTSTRAP` failure was not inferred. The generic stage is absent from the normal path. Routes are read from the already-running API with GET `http://127.0.0.1:18080/openapi.json` and fail as `STAGE_2_OPENAPI_FETCH`, `STAGE_2_OPENAPI_PROTOCOL`, `STAGE_2_LEGACY_ROUTES`, or `STAGE_2_MTPROTO_ROUTE`. Settings is a separate minimal child that imports only `app.core.config.Settings` and fails as `STAGE_2_SETTINGS_IMPORT`, `STAGE_2_SETTINGS_EXECUTION`, `STAGE_2_SETTINGS_PROTOCOL`, or `STAGE_2_BOT_SETTINGS`. The DB child imports only SQLAlchemy helpers, models, `SessionLocal`, and `RecentSourceService` (`STAGE_2_SQLALCHEMY`, `STAGE_2_MODELS`, `STAGE_2_DB_SESSION`, `STAGE_2_RECENT_SOURCE`) and then the existing account, scope, legacy aggregate, candidate, Inbox-read, and bound substages, plus `STAGE_2_CHILD_PROTOCOL` and `STAGE_2_CHILD_EXECUTION`. It does not import `app.main` or `Settings`. Success emission order is unchanged: route PASS markers, then `BOT_SETTINGS_MODEL_ABSENT_PASS`, then the read-state counts. Seventy-one focused tests, including an actual backend-package import smoke that does not open a DB session or load `app.main`, plus compile, bundled compile, Bash syntax, Ruff, and diff-check pass. No live verifier, production SSH, provider call, or production mutation occurred. No retry is authorized.


- Telegram Bot API M4BY1 review of `02c6240bb17e7d6a353db907b49cf32e2caf5eaf`: ACCEPTED / READY FOR ONE FINAL LIVE READ-ONLY STAGE C ACCEPTANCE RUN, subject to fresh explicit human authorization. Stage 2 no longer depends on a monolithic fresh-process import of `app.main`. Route retirement/presence is checked against the already-running API's read-only `/openapi.json`; Settings is checked by a minimal isolated child; DB/read-state is checked by a narrow child importing only SQLAlchemy helpers, models, `SessionLocal`, and `RecentSourceService`. The generic `STAGE_2_BOOTSTRAP` path is removed. Actual-package smoke coverage confirms the narrow imports are valid without loading `app.main` or opening a DB session. Strict success/failure ordering, canonical Git bootstrap, runtime Bot-env absence, account=1, active scope=28, historical Bot preservation/readability, AI=false, and zero provider/write/recreate invariants remain intact. Production remains exact `bd1433921a056b8dc42bd23c6ecf0e4ce2bedf4b`, Alembic `0046`.


- Telegram Bot API M4BZ1 final live read-only Stage C acceptance: EXPLICITLY HUMAN AUTHORIZED. Exactly one execution of `ops/production/verify_telegram_bot_stage_c.sh` is authorized against production runtime/ref `bd1433921a056b8dc42bd23c6ecf0e4ce2bedf4b`, expected Alembic `0046`, using accepted Stage 2 redesign commit `02c6240bb17e7d6a353db907b49cf32e2caf5eaf`. The run is observation-only: Telegram/provider calls=0, DB writes=0, env writes=0, service restart/recreate=0, deploy/ref movement=0. If it fails or blocks, no retry is authorized without new review and explicit human authorization.


- Telegram Bot API M4BZ1 final live read-only Stage C acceptance: PASS / PRODUCTION ACCEPTED. Production runtime/ref remains exact `bd1433921a056b8dc42bd23c6ecf0e4ce2bedf4b`; worktree clean; DB/API/worker running; DB healthy; Alembic `0046`; application health PASS. Retired Bot runtime variables are absent from API/worker runtime environments; MTProto/protected credentials are preserved; `TELEGRAM_MTPROTO_AI_ENABLED=false`. Legacy Bot routes are absent; required MTProto route is present; active Settings contains no retired Bot fields. Production state confirms exactly one MTProto account, active scope count 28, 431 preserved legacy non-MTProto Telegram chat objects, and at least one preserved legacy Bot-derived object is readable through canonical Inbox eligibility. Telegram/provider calls=0, DB writes=0, env writes=0, service recreations=0. Terminal success and end markers were emitted. Telegram Bot API retirement Stage C is COMPLETE / PRODUCTION ACCEPTED. Telegram Bot account is destroyed, webhook deleted, Bot credentials cleared, Bot runtime/code/config surfaces retired, historical Bot-derived objects and legacy schema intentionally preserved, and MTProto is the sole live Telegram transport.


- Client acceptance after Telegram Bot retirement Stage C: MANUAL HUMAN ACCEPTANCE NEXT. Backend Stage C remains production accepted. The next verification is client-only/local: build the current Flutter client from canonical main, install/upgrade it on two Android devices, run the Linux desktop client locally, confirm all three can connect/authenticate to the accepted production backend, and verify a fresh inbound MTProto Telegram message from at least one configured folder is visible in the ordinary Inbox. No backend deploy, production mutation, schema/data change, Telegram provider configuration change, or AI enablement is implied. Current folder UX semantics: checking a folder changes local selection; `Сохранить папки` persists configured folder names; `Применить область` immediately reconciles the active scope; recurring MTProto sync also reconciles configured folders on each run, so manual Apply is deterministic/immediate rather than required for eventual reconciliation. `Предпросмотр области` is optional. The current production backend requires muted dialogs to remain excluded, so leave `Исключать заглушенные чаты` enabled during acceptance.


- Client acceptance execution delegated to Executor. Local workstation-only authorization now permits clean Flutter toolchain diagnosis, Linux debug build/launch, one Android debug APK build, and upgrade installation/launch on exactly two already-connected authorized Android devices. Android uninstall/clear-data is forbidden so existing local bearer token/settings are preserved. The previous Linux build emitted garbled binary-looking terminal output and was manually interrupted; this is not yet classified as an application defect. Executor must obtain the first meaningful textual build error if it recurs and must not patch product code under this task. Human visual Telegram Inbox acceptance remains separate after clients are running.


- Telegram MTProto client acceptance: DELIVERY PASS / UX ACCEPTANCE FAIL. A fresh inbound MTProto message is reaching Secretary and is visible in the Inbox screen, but each ordinary new message is currently materialized twice at the presentation level: as the canonical `chat_message` source object eligible for ordinary `Последние входящие`, and as an unresolved Telegram `transport_event/message_created` Notification rendered under `Требует внимания` with `Готово / Пропустить`. This behavior comes from the previously accepted C2B/C3B transport-notification design, not from delivery failure. Real client acceptance shows that routine inbound message creation is not an attention/action item and this prior product decision is revised. New canonical direction: ordinary inbound MTProto `message_created` must surface only through the normal Inbox/source feed, not through the unresolved attention notification surface. Existing historical `message_created` Notification rows must be retained (no destructive cleanup) but excluded from Inbox attention presentation. Telegram `message_edited` and `message_deleted` transport-event notifications remain unchanged for now. No production change is yet authorized.

- Telegram MTProto ordinary Inbox attention: BUILT / LOCALLY VERIFIED ONLY. The history import path no longer creates a Notification for a routine inbound MTProto `message_created`; the canonical `chat_message` remains the Inbox source object. `GET /inbox` unresolved attention now uses `NotificationService.list_inbox_attention`, which keeps historical rows stored and excludes only proposals with `type=transport_event`, `provider=telegram`, `transport=mtproto`, and `event_type=message_created`. Generic notification listing still returns those historical rows. `message_edited` and `message_deleted` transport notifications are unchanged. No migration, Flutter redesign, production SSH, provider call, or production mutation. Twenty-three focused C2B tests pass, including materialization without a created-event notification, replay, edit/delete retention, and Inbox source visibility with the historical created-event hidden from attention.


- Telegram MTProto ordinary Inbox UX correction review of `f31f8f5b704159b7ec903da4c26a590d23f86e78`: ACCEPTED / READY FOR SCHEMA-NEUTRAL PRODUCTION DEPLOY, subject to explicit human authorization. Review confirms the forward history sync no longer creates routine inbound `message_created` Notifications; canonical MTProto `chat_message` materialization and Inbox eligibility remain intact; replay stays idempotent; edit/delete deterministic transport notifications remain unchanged. `GET /inbox` now uses a dedicated Inbox-attention selector that excludes only historical unresolved Telegram MTProto `transport_event/message_created` rows while leaving the generic Notification API and all stored rows untouched. Exact production rollback/current runtime is `bd1433921a056b8dc42bd23c6ecf0e4ce2bedf4b`; exact candidate is `f31f8f5b704159b7ec903da4c26a590d23f86e78`. Candidate is a fast-forward 56 commits ahead, but the only production runtime code delta is three backend files (`api/inbox.py`, `services/notification_service.py`, `services/telegram_mtproto_history_service.py`); no Alembic/migration or infra files changed, so expected Alembic remains `0046`. No client rebuild is required for this backend-only correction.


- Telegram MTProto ordinary Inbox UX production deploy: EXPLICITLY HUMAN AUTHORIZED. Canonical `production` was fast-forwarded non-force from `bd1433921a056b8dc42bd23c6ecf0e4ce2bedf4b` to exact release `f31f8f5b704159b7ec903da4c26a590d23f86e78`. Expected Alembic remains `0046`. Authorized deployment path is the canonical schema-neutral `ops/production/deploy.py` only, with rollback `bd1433921a056b8dc42bd23c6ecf0e4ce2bedf4b`. No bespoke verifier is required after deploy; acceptance is the normal deploy invariants plus human UI check that routine Telegram created events no longer appear under `Требует внимания` and fresh inbound messages appear under ordinary Inbox.


- Telegram MTProto ordinary Inbox UX production deploy: SUCCESS. Production runtime/ref is exact `f31f8f5b704159b7ec903da4c26a590d23f86e78`; health PASS; Alembic `0046`; DB container unchanged; DB volume unchanged; `.env` unchanged; API and worker recreated as expected. No schema/data cleanup occurred. Backend correction is now live: routine inbound MTProto `message_created` no longer creates a new actionable Notification, while historical created-event Notification rows remain stored and are excluded from Inbox attention. Human UI acceptance remains to confirm old routine Telegram attention cards disappear and a fresh inbound message appears under ordinary Inbox / `Последние входящие` without creating a new attention card.


- Telegram MTProto post-transport closure audit: ordinary transport/communication capability is substantially complete (receive/sync, compose send, reply via message anchor, outbound edit, exact-message delete, mark-read; external writes remain approval/pending-action-plan gated). The AI quarantine remains correctly OFF in production with `TELEGRAM_MTPROTO_AI_ENABLED=false`. Audit of the intended future false->true path found that embedding catch-up, auto-label gating, temporal eligibility, retrieval/context gating, and conversation-summary gating already exist, but full downstream parity is NOT YET PROVEN. Two concrete implementation gaps were found: (1) current MTProto objects materialize metadata as `peer_id/peer_title/sender_peer_id/topic_id`, while the generic Telegram conversation projection expects legacy `chat_id/chat_display_name/from_user_id`, so MTProto messages may stay singleton and miss conversation-stack summarization; (2) `chat_message` is absent from correlation trigger kinds, so an MTProto message does not currently initiate automatic task/object correlation even when the Telegram AI gate is enabled. A local synthetic false->true integration proof is required before the Telegram topic is considered development-complete. No production AI enablement is authorized.

- Telegram MTProto full downstream readiness: BUILT / LOCALLY VERIFIED ONLY. Production `TELEGRAM_MTPROTO_AI_ENABLED` remains false. Native MTProto conversation projection now uses `peer_id`, account id, `peer_title`/`group_title`/username, `sender_peer_id`, `reply_to_message_id`, and non-null `topic_id` without rewriting stored metadata or adding legacy Bot aliases. Correlation may start from a canonical MTProto `chat_message` only when the AI gate and active scope allow it; Teams and other chat providers stay outside the trigger set. A synthetic false->true test, with fake embedding/classifier/temporal extractor/correlation judge/summarizer and no Telegram or OpenAI calls, proves Inbox visibility, deterministic stacking, zero AI jobs while false, then bounded existing catch-up, embedding, label, temporal hint, proposed correlation edge, stack summary, and assistant/context visibility after the in-test flag flip. Voice stays on `/assistant/transcribe` plus `/assistant/message`, so it inherits the same AI gate. Future production activation remains flag true, API/worker reload, and the existing recurring catch-up. No migration, production SSH, provider call, or production mutation.


- Architect review of `10cb4a3179e2a4c1acdfe94c3814a8aaf9ec7694`: conversation projection, narrow MTProto correlation trigger, false->true catch-up, label path, assistant/context gate, stack summary and CRUD proof are accepted. One blocking semantic defect remains before final Telegram development closure: MTProto participation currently labels every inbound message from a non-self sender as `direct_recipient`, including group/supergroup peers. That overstates personal relevance and can incorrectly promote group temporal statements into the user's consolidated calendar. Required correction: only private inbound may infer direct-recipient from direction; group/supergroup must use non-direct group/channel semantics, and MTProto `sender_peer_id`/`peer_kind` must participate in temporal source signatures. No production change is authorized.

- Telegram MTProto temporal participation correction: BUILT / LOCALLY VERIFIED ONLY. Outbound MTProto from the connected user remains `sender`. Only inbound `peer_kind=private` from another sender is `direct_recipient`. Group and supergroup inbound are not personal addressing and use the existing non-direct channel policy, so a high-confidence temporal statement stays `possible` and is not promoted to `expected`. Missing or unknown peer kind does not infer personal addressing. MTProto temporal source signatures include `sender_peer_id`, `peer_kind`, and `direction`. Production `TELEGRAM_MTPROTO_AI_ENABLED` remains false. No production SSH, provider call, or production mutation.


- Architect review of `8ad52f0653f9f90e1932c49532dc4f993ea1a9cc`: ACCEPTED. MTProto temporal participation now distinguishes private direct-recipient semantics from group/supergroup non-direct semantics; unknown peer kind fails conservatively; `sender_peer_id` and `peer_kind` are included in MTProto temporal source signatures. The previously accepted full-pipeline work from `10cb4a3179e2a4c1acdfe94c3814a8aaf9ec7694` remains accepted. The two reported `test_temporal_signals_a.py` failures concern the pre-existing repeated `already_evidenced` extractor-call contract and also reproduce for Teams; they are outside the MTProto semantic correction and are not a Telegram release blocker. Production remains `f31f8f5b704159b7ec903da4c26a590d23f86e78` with Telegram AI false. The fast-forward production-to-main delta contains no Alembic/migration or infra files. To achieve the future one-toggle activation goal, the accepted downstream code should be deployed now while the production Telegram AI flag stays false, followed by one explicitly authorized production synthetic ML rehearsal using process-local AI=true, synthetic content only, real production DB/config/ML adapters, and zero Telegram/provider transport calls.


- Telegram final closure production authorization: HUMAN AUTHORIZED deploy of exact downstream candidate `8ad52f0653f9f90e1932c49532dc4f993ea1a9cc` while long-running production API/worker remain `TELEGRAM_MTPROTO_AI_ENABLED=false`, plus one production synthetic ML rehearsal with real production ML/LLM adapters and zero Telegram transport calls. Canonical `production` was fast-forwarded non-force from `f31f8f5b704159b7ec903da4c26a590d23f86e78` to `8ad52f0653f9f90e1932c49532dc4f993ea1a9cc`. Expected Alembic remains `0046`; no migration/infra delta. No existing production rehearsal helper was found. The authorized rehearsal therefore requires one reviewable local helper/wrapper implementation first; the helper must execute process-local AI=true only for the one-shot rehearsal process, keep long-running API/worker false, use synthetic Telegram-shaped content only, use real ML/LLM adapters, make zero Telegram transport calls, and emit sanitized pass/fail evidence. This helper implementation itself is code/test-only until architect review. The user's existing authorization covers the single subsequent live rehearsal once the helper is reviewed and the deploy has passed; no second human authorization is required unless scope changes.


- Telegram downstream readiness deploy to production: SUCCESS. Production runtime/ref is exact `8ad52f0653f9f90e1932c49532dc4f993ea1a9cc`; health PASS; Alembic `0046`; DB container unchanged; DB volume unchanged; `.env` unchanged; API/worker recreated normally. This rollout deploys the accepted MTProto conversation projection, narrow task correlation trigger, false->true catch-up wiring, temporal participation semantics, and related downstream-readiness code while the production Telegram AI gate remains operationally quarantined. No schema migration or data cleanup occurred. The only remaining Telegram closure work is the already-authorized production synthetic ML rehearsal after architect review of the one-shot helper; no further product-code deploy is expected for that rehearsal.

- Telegram production rehearsal helper: BUILT / LOCALLY VERIFIED WITH FAKE PROVIDERS ONLY. `ops/production/telegram_production_rehearsal.py` creates one synthetic private MTProto conversation, parks AI jobs as running before commit, and executes the normal handlers synchronously in-process. Process-local `telegram_mtproto_ai_enabled=true` does not write the service environment. Startup requires a long-running API/worker probe that stays false. Telegram transport import and MTProto session decrypt are sealed. Repeated run ids fail closed. Live mode stays blocked until architect review. No live rehearsal, production SSH, provider call, or production mutation in this build.


- Architect review of rehearsal helper `02ea29f58c36ff629cb5c6fc00f225d65f035ede`: fake-provider/local design accepted, live execution NOT YET ACCEPTED. Blocking findings: live CLI currently discards `execute_live()` return code and always exits 2; broad provider/runtime exceptions are not sanitized and can leave already-committed synthetic jobs parked `running`; helper exists only on main while production checkout intentionally remains `8ad52f06...`, so a canonical streamed one-shot remote wrapper is required instead of a second product deploy or ad-hoc SSH. Clarification: passing `embedding_service=None` in live mode is correct because the canonical embed handler resolves the configured real service. One focused correction task is authorized; no live rehearsal has run.

- Telegram production rehearsal live path: BUILT / NOT EXECUTED. Live CLI returns the real `execute_live()` code. Provider failures emit only `REHEARSAL_FAILED=rehearsal_aborted`, roll back the open transaction, and mark leftover synthetic pending/running jobs failed without deleting artifacts. `ops/production/telegram_production_rehearsal_remote.py` streams the helper into a one-shot `docker compose run --rm --no-deps --no-build` container after pinned-SSH checks and a host-side false AI probe. Production checkout stays `8ad52f0653f9f90e1932c49532dc4f993ea1a9cc`. No second deploy, live rehearsal, production SSH, or production mutation.


- Architect review of production rehearsal live path `23d2521bcd5a2e774be657cc47dd5625d840822f`: ACCEPTED FOR THE ONE PREVIOUSLY HUMAN-AUTHORIZED LIVE SYNTHETIC REHEARSAL. Live CLI propagates actual exit code; broad provider/runtime failures are sanitized; failed rehearsal jobs are converted from pending/running to failed with fixed safe error; process-local AI and env are restored; Telegram transport/session decrypt are blocked; the wrapper streams reviewed helper code into an isolated `docker compose run --rm --no-deps --no-build` one-shot container and does not deploy/restart/recreate long-running services. Production ref independently remains exact `8ad52f0653f9f90e1932c49532dc4f993ea1a9cc`; API/worker must remain AI=false. Known non-safety reliability note: wrapper local bootstrap still consults remote-tracking refs after an explicit branch fetch; the live command therefore performs a full `git fetch --prune origin` immediately beforehand to refresh tracking refs and avoid the previously observed stale-ref false blocker. Exactly one run id `tgprod0922a` is authorized. No retry is authorized without review.


- Telegram production synthetic rehearsal `tgprod0922a`: ONE AUTHORIZED INVOCATION WAS ATTEMPTED. The local wrapper returned to the shell with completely empty stdout. This is not an accepted rehearsal result. Given reviewed wrapper semantics, local checkout/target/host-key blocks would emit `REHEARSAL_REMOTE_BLOCKED=...`; the streamed remote program likewise emits a public marker for production/ref/worktree/long-running-AI/oneshot failures, and a successful helper emits a non-empty report. Therefore empty stdout strongly indicates the outer SSH/remote-command subprocess failed before producing remote stdout; its stderr is intentionally suppressed by `public_output()`. No retry is authorized. The only allowed next observation is the shell exit status of the already-completed invocation, captured immediately with no intervening command.


- Telegram rehearsal attempt `tgprod0922a` exit-status capture: `REHEARSAL_WRAPPER_EXIT=1`; the user pressed an additional empty Enter before the capture, which does not normally execute a shell command and therefore does not invalidate the prior status. Architect identified a deterministic wrapper defect that exactly explains empty stdout + exit 1: `build_remote_program()` inlines `_flag_enabled()`, but the generated program omits the referenced module constant `TRUE_FLAGS`. On normal remote-state assessment this raises `NameError`; the traceback is stderr-only and intentionally suppressed by the local wrapper, while SSH propagates remote exit 1. The failure happens before `Path(HELPER_PATH).write_text(...)` and before the one-shot `docker compose run`, so the synthetic rehearsal itself did not execute: no synthetic objects, real ML/LLM calls, Telegram transport calls, or production mutation occurred. A single local wrapper/test correction is required before the already-authorized rehearsal can be attempted.

- Telegram rehearsal remote program: FIXED / NOT EXECUTED. The streamed program now defines `TRUE_FLAGS` itself, so normal API/worker flag assessment no longer raises `NameError`. An unexpected exception inside the generated program emits only `REHEARSAL_REMOTE_BLOCKED=remote_program` and a nonzero exit, instead of an empty stdout. Production release remains `8ad52f0653f9f90e1932c49532dc4f993ea1a9cc`. Run id `tgprod0922a` was not replayed. No live rehearsal, production SSH, provider call, or production mutation.


- Architect review of remote-program fix `700ec26a22485b52fb5cafab0f2de8b35d45e3e9`: ACCEPTED. The generated remote program is self-contained, includes its own TRUE_FLAGS data, and execution-level tests prove the normal production-state path reaches the one-shot child and propagates stdout/exit status. Unexpected generated-program exceptions now emit only `REHEARSAL_REMOTE_BLOCKED=remote_program` with nonzero exit, so the prior empty-stdout class is closed. The earlier `tgprod0922a` invocation is classified as wrapper-bootstrap crash before helper write/one-shot execution and therefore did not consume the authorized synthetic rehearsal: no synthetic DB artifacts, real ML/LLM calls, Telegram calls, or production mutation occurred. Production remains exact `8ad52f0653f9f90e1932c49532dc4f993ea1a9cc`; long-running API/worker AI must remain false. Deployment-level OpenAI API key fallback is a supported canonical credential path for a synthetic user, so no credential-copy mutation is required before the rehearsal. The same run id `tgprod0922a` remains authorized for one actual rehearsal execution; no further deploy is needed.


- Telegram production rehearsal second invocation reached the isolated one-shot launch but returned `REHEARSAL_REMOTE_BLOCKED=oneshot_failed`; no retry performed. Static compatibility review against exact production release `8ad52f0653f9f90e1932c49532dc4f993ea1a9cc` confirms every symbol imported by the streamed helper exists in production code. A deterministic container-layout defect was identified: production image canonical Python import root is `/app` (`WORKDIR /app`, application code at `/app/app`), while the wrapper mounts and directly executes the helper from `/opt/rehearsal`. A top-level `from app...` can therefore fail before helper `main()`, yielding stderr-only child failure and the observed `oneshot_failed`. One final local correction is authorized: execute the helper from a unique read-only path under `/app`, add a sanitized startup marker, and preflight only the boolean availability of the deployment OpenAI fallback required by the separate synthetic user. No live retry, provider call, Telegram call, or production mutation is authorized during this correction.

- Telegram rehearsal one-shot startup: FIXED / NOT EXECUTED. The isolated container now executes the read-only helper at `/app/telegram_production_rehearsal.py`, so `sys.path[0]` is the production import root. Live startup emits `REHEARSAL_STARTUP=PASS` only after the helper imports, live confirmation, and false long-running AI values. A missing deployment `OPENAI_API_KEY` emits `REHEARSAL_REMOTE_BLOCKED=provider_config` before any synthetic fixture write and does not print the key. Import/startup failure without a rehearsal marker still becomes `REHEARSAL_REMOTE_BLOCKED=oneshot_failed`. Production release remains `8ad52f0653f9f90e1932c49532dc4f993ea1a9cc`. Run id `tgprod0922a` was not replayed. No live rehearsal, production SSH, provider call, or production mutation.


- Architect review of one-shot startup fix `659d49d16c36370f4fab1884b4805a6cedb1a8e4`: ACCEPTED. The reviewed helper is mounted read-only at `/app/telegram_production_rehearsal.py` and executed from the production Python import root, with no PYTHONPATH mutation, Docker socket, deploy, or long-running service change. The live provider preflight checks only whether deployment `OPENAI_API_KEY` is non-empty and does so before run-id lookup/fixture creation; missing fallback emits only `REHEARSAL_REMOTE_BLOCKED=provider_config` and no credential material. Startup emits `REHEARSAL_STARTUP=PASS` after successful imports/live confirmation/false long-running AI probes. The prior `oneshot_failed` occurred before helper business execution and therefore created no synthetic state. Production remains exact `8ad52f0653f9f90e1932c49532dc4f993ea1a9cc`, Alembic 0046, API/worker AI=false. The same run id `tgprod0922a` remains authorized for exactly one actual live synthetic rehearsal.


- Telegram acceptance strategy changed by Architect: synthetic production rehearsal is abandoned after repeated wrapper/container startup friction. No further execution or repair of the synthetic rehearsal path is planned for acceptance. New canonical acceptance direction: use real canonical MTProto objects produced by normal sync from a brand-new private Telegram solo group containing only the connected user, with explicit marker `TG_SELF_E2E_0922A`. The executor is authorized code/test-only to build a narrow self-authored E2E harness that fail-closes unless every selected message is canonical MTProto, active-scope, outbound, and `sender_peer_id == TelegramMtprotoAccount.telegram_user_id`; conversation summarization must additionally prove every Telegram message in the summary cohort is self-authored before any provider call. Global production Telegram AI remains false; future live acceptance will use process-local true only for the proven cohort, canonical handlers, real providers, no catch-up/backlog, no Telegram transport/session decrypt, and zero unrelated Telegram content sent to ML/LLM. Production remains `8ad52f0653f9f90e1932c49532dc4f993ea1a9cc`, Alembic 0046.


- Telegram self-authored E2E fixture policy refined: no dedicated test label is required or desired. Acceptance uses the user's existing Secretary label vocabulary. Auto-label pipeline PASS means the normal real classifier/handler demonstrably executed for a selected self-authored Telegram object, proven via the normal `auto_label_result` AI-audit/job evidence; a legitimate zero-assignment result is acceptable and must not be treated as pipeline failure. Existing-label assignments, if any, are observational output, not a deterministic prerequisite. The marker-matched normal Secretary task remains useful for deterministic correlation testing.


- Communication presentation parity decision: current `RecentSourceService._not_outbound_chat_clause()` suppresses outbound `chat_message` rows only for Telegram and Teams, while Mattermost is not covered by that suppression. This explains the observed provider inconsistency. Architect decision: canonical conversation history/detail must be bidirectional for all chat providers, including the user's own outbound messages; attention/unread semantics remain a separate inbound-driven concern. A follow-up parity correction is required after the active Telegram self-authored E2E harness task; do not interrupt the current executor task unless this defect blocks it.


- Architect review of `9d4ed0ee25fbb2bbd9bba51888b63417f1a05957`: core real-object cohort proof, self-authorship checks, active-scope validation, conservative summary privacy guard, queue parking/drain concept, and Telegram transport/session barrier are accepted. Live acceptance is NOT yet ready. Blocking findings: (1) auto-label PASS currently counts all historical user `auto_label_result` audit events instead of new events tied to selected cohort objects; (2) standard correlation candidate generation can include arbitrary user objects within a 72-hour time window, including unrelated/third-party Telegram messages, so their content could reach the real correlation judge under process-local AI=true unless an acceptance-only provider-boundary privacy barrier is added; (3) current report proves handler execution more than actual embedding/temporal/correlation/summary/context/retrieval/idempotency outcomes; (4) helper `--live` is intentionally blocked and the remote module is only a compose-command constructor, not yet the canonical SSH/trust wrapper. One consolidated code/test-only correction task is active. No production live E2E is authorized yet.

- Telegram self-authored E2E live readiness: BUILT / NOT EXECUTED. Auto-label PASS counts only new `auto_label_result` events joined to traces whose object is in the selected marker cohort. Correlation calls the inner judge only after an acceptance-only privacy fence rejects Telegram candidates outside the approved self-authored set. The local fake report proves embedding, temporal hint, proposed marker-task correlation, current stack summary, AI-only context/retrieval, and idempotent repeat. `--live` uses production providers behind `SELF_E2E_CONFIRM=reviewed` and fixed false long-running probes, with sanitized failure cleanup. The remote wrapper streams the helper read-only under `/app` and does not set `TELEGRAM_MTPROTO_AI_ENABLED=true`. Production release remains `8ad52f0653f9f90e1932c49532dc4f993ea1a9cc`. No live E2E, production SSH, provider call, or production mutation.


- Architect final review of `32eb444d6df318e779b540acee8103bfd5870616`: major live-readiness work accepted, but one true privacy gap and two false-failure risks remain. Privacy gap: `PrivacyCorrelationJudge` currently blocks only canonical MTProto Telegram candidates outside the approved set, so retained legacy Bot-derived `provider=telegram` objects could still enter the real correlation judge payload. Final rule must block every Telegram-provider candidate unless its id is in the approved self-authored set. Reliability corrections: correlation PASS should accept any canonical allowed proposed relation to the exact marker task rather than only `related_to`; temporal PASS should use new cohort-scoped `temporal_signal_result` audit evidence and accept canonical successful existing-anchor outcomes such as calendar_first_match/hint_merged/already_evidenced, not require creation of a brand-new temporal_hint. One narrow code/test-only correction is active. Live production E2E remains unauthorized.

- Telegram self-authored E2E final readiness: BUILT / NOT EXECUTED. Correlation privacy now blocks every `provider=telegram` candidate whose id is outside the approved self-authored set, including legacy Bot objects and unknown Telegram transport/kind, before the inner judge. Correlation PASS accepts a new proposed edge from a selected marker message to the exact marker task when the type is in `CORRELATION_ALLOWED_TYPES`. Temporal PASS uses only new cohort-scoped `temporal_signal_result` events and accepts `hint_created`, `calendar_first_match`, `hint_merged`, and `already_evidenced` when that anchor is still present for the selected source revision. Production release remains `8ad52f0653f9f90e1932c49532dc4f993ea1a9cc`. No live E2E, production SSH, provider call, or production mutation.


- Architect final acceptance of Telegram self-authored E2E harness `00a653a11f44f4122fb2689cbbffb06c133049e3`: ACCEPTED / LIVE-READY / NOT EXECUTED. Final checks confirm: every Telegram-provider correlation candidate outside the approved self-authored set is blocked before the inner judge; correlation evidence accepts only a new proposed edge to the exact marker task with a type in canonical `CORRELATION_ALLOWED_TYPES`; temporal evidence is based on new cohort-scoped `temporal_signal_result` audit events and matches canonical service outcomes `hint_created`, `calendar_first_match`, `hint_merged`, and validated `already_evidenced`; the evidence-edge direction/signature check matches the canonical temporal service contract (anchor -> source). No code blockers remain. Production stays `8ad52f0653f9f90e1932c49532dc4f993ea1a9cc`, Alembic 0046, long-running Telegram AI=false. One live E2E with real providers is awaiting explicit human authorization because it will send the user's self-authored Telegram test content to production ML/LLM providers and persist normal downstream artifacts.


- Human explicitly authorized exactly one live self-authored Telegram E2E on marker `TG_SELF_E2E_0922A` with real production ML/LLM providers while long-running Telegram AI remains false. Accepted harness remains `00a653a11f44f4122fb2689cbbffb06c133049e3`; production runtime remains exact `8ad52f0653f9f90e1932c49532dc4f993ea1a9cc`, Alembic 0046. One canonical remote-wrapper invocation is now authorized. Any BLOCKED/FAILED/incomplete result consumes the invocation attempt operationally and must not be retried without architect review.

- Telegram self-authored E2E first live acceptance attempt: SAFE ONE-SHOT STARTUP BLOCK / NO ACCEPTED E2E RESULT. Exactly one authorized invocation returned only `SELF_E2E_REMOTE_BLOCKED=oneshot_failed` with exit status `1`; no retry, direct SSH, or manual correction was performed. The wrapper reached the isolated `docker compose run` boundary after production/ref/worktree/origin/long-running-AI guards, but the child produced no harness-owned stdout marker before nonzero exit and raw stderr is intentionally suppressed. Therefore this is not evidence of a Telegram/ML pipeline failure; it is currently localized only to container/interpreter/helper startup. The live authorization is consumed. Next work is code/test-only startup observability hardening; no production retry is authorized.

- Telegram self-authored E2E one-shot startup observability: BUILT / NOT EXECUTED. The isolated container now starts a standard-library bootstrap that emits `SELF_E2E_STARTUP=bootstrap`, compiles the helper, emits `SELF_E2E_STARTUP=compiled`, imports it, emits `SELF_E2E_STARTUP=imported`, then calls `main(["--live"])` once. Compile failure emits `SELF_E2E_REMOTE_BLOCKED=compile_failed`. Import failure emits `SELF_E2E_REMOTE_BLOCKED=import_failed`. A child failure with no `SELF_E2E_` stdout marker remains `SELF_E2E_REMOTE_BLOCKED=oneshot_failed`. Harness stdout and exit code are forwarded once the imported stage is reached. Raw stderr, tracebacks, and exception text are not forwarded. The consumed live attempt was not repeated. Production release remains `8ad52f0653f9f90e1932c49532dc4f993ea1a9cc`, Alembic 0046, long-running Telegram AI=false. No production SSH, docker compose run, provider call, or production mutation.

- Architect review of startup-observability commit `86e856b4e8ab54a2bf535eae45a002692e94ee4c`: compile/import diagnostics are accepted, but one post-import protocol blind spot remains. Because startup markers use the `SELF_E2E_` prefix and the remote wrapper currently treats any `SELF_E2E_` stdout as harness-owned evidence, a failure after import but before `SELF_E2E_BLOCKED/FAILED` or a complete success report can bypass the generic one-shot fallback; startup-only stdout can therefore be mistaken for sufficient protocol evidence. One narrow code/test-only correction is active to distinguish startup markers from terminal harness outcomes and fail closed on post-import exception/nonterminal exits. No replacement live E2E is authorized.

- Telegram self-authored E2E post-import protocol gate: BUILT / NOT EXECUTED. `SELF_E2E_STARTUP=...` is no longer treated as a terminal harness result. After import, `main(["--live"])` must emit `SELF_E2E_BLOCKED`, `SELF_E2E_FAILED`, or a report line `SELF_AUTHORED=PASS`; otherwise the child and the remote wrapper fail closed with `SELF_E2E_REMOTE_BLOCKED=harness_protocol` and do not forward stderr, traceback, or exception text. Compile and import blockers remain `compile_failed` and `import_failed`. A child with no startup marker remains `oneshot_failed`. The consumed live attempt was not repeated. Production release remains `8ad52f0653f9f90e1932c49532dc4f993ea1a9cc`, Alembic 0046, long-running Telegram AI=false. No production SSH, Docker, provider call, or production mutation.

- Architect final review of `4fc6ff3b12f5e393be2ce3a46ab9b78b4280d86c`: ACCEPTED / LIVE-READY AGAIN / NOT AUTHORIZED TO RUN. Startup markers are progress-only; compile/import blockers remain fixed and sanitized; post-import exceptions or nonterminal exits fail closed as `SELF_E2E_REMOTE_BLOCKED=harness_protocol`; legitimate blocked/failed/success harness outcomes retain their stdout and child exit code; partial nonterminal output and stderr/traceback are suppressed. Existing checkout/ref/origin/host-key/long-running-AI guards remain intact. No code blocker remains in the one-shot observability path. The previous live authorization remains consumed, so a replacement live E2E requires new explicit human authorization before `CURRENT_TASK.md` may contain an executable live task.

- Human explicitly authorized exactly one replacement live self-authored Telegram E2E on marker `TG_SELF_E2E_0922A` with real production ML/LLM providers while long-running Telegram AI remains false. The earlier authorization remains consumed by the startup-only attempt; this is one new replacement authorization. Accepted harness baseline remains `00a653a11f44f4122fb2689cbbffb06c133049e3`; accepted startup/protocol hardening is `4fc6ff3b12f5e393be2ce3a46ab9b78b4280d86c`; production runtime remains exact `8ad52f0653f9f90e1932c49532dc4f993ea1a9cc`, Alembic 0046. Exactly one canonical remote-wrapper invocation is authorized. Any BLOCKED/FAILED/incomplete or nonterminal result consumes this replacement attempt operationally and must not be retried without Architect review and new human authorization.

- Telegram self-authored E2E replacement attempt #2: SAFE PRE-CONTAINER BLOCK / NO ACCEPTED E2E RESULT. Exactly one authorized replacement invocation returned only `SELF_E2E_REMOTE_BLOCKED=oneshot_failed` with exit status `1`; no retry/direct SSH/manual Docker/correction followed. Because the hardened bootstrap emitted no `SELF_E2E_STARTUP=bootstrap`, failure is before Python child startup. Architect static review found the canonical one-shot command uses unsupported `docker compose run --no-build`. Official Compose `run` exposes `--build` but not `--no-build`; CLI rejection before container creation exactly matches the observed empty child stdout collapsed to `oneshot_failed`. Root cause is therefore the acceptance wrapper CLI contract, not evidence of Telegram/ML pipeline failure. The replacement live authorization is consumed. One code/test-only correction is active; no further live run is authorized.

- Telegram self-authored E2E one-shot Compose contract: BUILT / NOT EXECUTED. The `docker compose run` command no longer passes unsupported `--no-build` and does not pass `--build`. It keeps `--rm` and `--no-deps` and adds `--pull never` before the `api` service. Before the one-shot, the remote program reads the running `api` container image id and fail-closes with `SELF_E2E_REMOTE_BLOCKED=image_missing` when that id is absent, without building or pulling. Raw Docker stderr is not forwarded. The consumed live attempts were not repeated. Production release remains `8ad52f0653f9f90e1932c49532dc4f993ea1a9cc`, Alembic 0046, long-running Telegram AI=false. No production SSH, Docker, provider call, or production mutation.

- Architect review of `7d571f410a31e4fc322f90ae0ac5e976b300b4fb`: PARTIAL ACCEPT / NOT LIVE-READY. Removing unsupported `compose run --no-build` is correct and `--pull never` prevents registry pulls, but it does not establish a hard no-build guarantee for a service defined with `build:`. Current Compose `run` internals retain a build-capable create path unless an internal no-build condition is set, and `run` exposes no public `--no-build` option. The running-container image-id check does not bind the subsequent `compose run api` to that exact image. The active corrective task therefore replaces one-shot container creation with stdin-fed `docker compose exec -T ... api python3 -B -` inside the already-running API container, which launches a separate process with exec-only environment overrides and requires no image create/build/pull path. No new live authorization exists.

- Telegram self-authored E2E acceptance path: BUILT / NOT EXECUTED. Acceptance now uses `docker compose exec -T -w /app` in the already-running `api` container, with exec-only `SELF_E2E_CONFIRM=reviewed` and false long-running AI probes, and `python3 -B -`. The helper is embedded in the stdin bootstrap, compiled and executed in memory under a module name other than `__main__`, and `main(["--live"])` is called once. No helper file is written to the production checkout or container. The command does not use `run`, `create`, `up`, `build`, or `pull`. Startup, compile, import, and harness-protocol markers remain. The consumed live attempts were not repeated. Production release remains `8ad52f0653f9f90e1932c49532dc4f993ea1a9cc`, Alembic 0046, long-running Telegram AI=false. No production SSH, Docker, provider call, or production mutation.

- Architect review of `88e9055d901a931f4df2dc66b6e8b5a1011d8142`: EXEC PATH ACCEPTED / NOT YET LIVE-READY. The acceptance path now correctly uses stdin-fed `docker compose exec -T -w /app ... api python3 -B -` in the already-running API container, with no run/create/build/pull/filesystem-write path. One privacy blocker remains: helper top-level module execution currently runs outside stdout redirection, so import-time stdout can escape before the terminal harness protocol owns output; the focused test explicitly demonstrates this with `HELPER_IMPORT`. A narrow code/test-only correction is active to discard helper import-time stdout/stderr while preserving bootstrap markers and normal terminal harness output. No new live authorization exists.

- Telegram self-authored E2E live attempt after import-output acceptance: BLOCKED AT SUMMARY COHORT / NO ACCEPTED E2E RESULT. Exactly one authorized invocation emitted startup markers through `SELF_E2E_STARTUP=imported` and then `SELF_E2E_BLOCKED=summary_cohort` with exit status `2`. No retry followed. The block is before process-local Telegram AI and before a provider call. The consumed authorization is not reused.

- Telegram self-authored E2E summary privacy: BUILT / NOT EXECUTED. Selected E2E messages still require marker `TG_SELF_E2E_0922A`. Covered summary neighbors are accepted only when they are canonical outbound MTProto messages for the same account whose sender equals the connected Telegram user id; the marker is not required for those neighbors. Inbound, foreign sender, unknown direction, missing identity, and legacy Telegram objects still fail closed as `summary_cohort` before any provider call. The accepted `docker compose exec` path is unchanged. No live E2E, production SSH, Docker, provider call, or production mutation. Production release remains `8ad52f0653f9f90e1932c49532dc4f993ea1a9cc`, Alembic 0046, long-running Telegram AI=false.

- Telegram self-authored E2E import-output privacy: BUILT / NOT EXECUTED. In-memory `exec` of the helper now runs under redirected stdout and stderr. Import-time output is discarded on both success and failure. Public output before `main(["--live"])` is limited to bootstrap markers and fixed `compile_failed` or `import_failed`. After `SELF_E2E_STARTUP=imported`, the existing captured protocol still propagates `SELF_E2E_BLOCKED`, `SELF_E2E_FAILED`, and `SELF_AUTHORED=PASS`, and nonterminal main results remain `harness_protocol`. The accepted `docker compose exec -T -w /app ... api python3 -B -` command is unchanged. No live E2E, production SSH, Docker, provider call, or production mutation. Production release remains `8ad52f0653f9f90e1932c49532dc4f993ea1a9cc`, Alembic 0046, long-running Telegram AI=false.

- Architect final review of `a4c5225550aa56b204043380a02280507f9a9976`: ACCEPTED / LIVE-READY / NOT EXECUTED. Helper top-level/import-time stdout and stderr are now discarded under redirected streams on both success and failure; bootstrap-owned startup/fixed failure markers remain outside redirection; after successful import, `main(["--live"])` is called exactly once under the existing captured terminal protocol. The previously accepted stdin-fed `docker compose exec -T -w /app ... api python3 -B -` path is unchanged and contains no run/create/up/build/pull or helper filesystem-write path. Ref/origin/host-key/long-running-AI guards remain intact. No code/privacy blocker remains in the acceptance wrapper. Both prior live authorizations are consumed; any new production acceptance attempt requires fresh explicit human authorization and a new exact one-shot task.

- Human explicitly authorized exactly one new live self-authored Telegram E2E to verify the complete downstream Telegram pipeline using marker `TG_SELF_E2E_0922A` and real production ML/LLM providers. Architect-accepted live-ready wrapper/privacy baseline is `a4c5225550aa56b204043380a02280507f9a9976`. Acceptance executes only through stdin-fed `docker compose exec` inside the already-running API container; no run/create/build/pull path is authorized. Production runtime remains exact `8ad52f0653f9f90e1932c49532dc4f993ea1a9cc`, Alembic 0046, and long-running API/worker Telegram AI must remain false. Exactly one canonical remote-wrapper invocation is authorized. Any remote/harness BLOCKED, FAILED, nonzero exit, or incomplete PASS report consumes the attempt and requires STOP with no retry.

- Telegram full-pipeline live E2E attempt #3: SAFE HARNESS PRIVACY BLOCK / NO PROVIDER CALL. Exactly one authorized invocation reached `SELF_E2E_STARTUP=bootstrap`, `compiled`, and `imported`, then stopped with `SELF_E2E_BLOCKED=summary_cohort`, exit 2. No retry/direct SSH/manual Docker/correction followed. Because `prove_summary_cohort()` runs before process-local Telegram AI is enabled and before provider setup, this attempt sent no selected Telegram content to ML/LLM providers. Architect review found a test gap and likely predicate conflation: `message_identity_block()` proves canonical outbound account/sender identity but also requires the E2E marker; `prove_summary_cohort()` applies that same marker-bearing predicate to every Telegram member in the covered conversation group. A safe self-authored outbound neighbor without the marker can therefore fail the summary privacy proof. The active task is code/test-only separation of marker membership from base self-authorship. The live authorization is consumed; no retry is authorized.

- Architect static review of `8ae75dc9e2b6b05e8caa4b966caafc7bbcb63574`: CODE SHAPE ACCEPTED / LOCAL CHECK EVIDENCE PENDING. The refactor correctly separates base canonical self-authorship from marker membership: selected E2E messages still require `TG_SELF_E2E_0922A`, while covered summary neighbors may be marker-free only when canonical outbound MTProto for the same account and authored by the connected Telegram user. Added tests cover marker preservation, a self-authored outbound non-marker neighbor, foreign sender, unknown direction, missing sender, legacy/noncanonical exclusion, and the pre-existing inbound-before-provider regression. The accepted remote exec path is unchanged. This Architect environment cannot independently execute the repository's focused pytest/Ruff suite, and no GitHub Actions workflow is configured, so local check evidence is required before live readiness is restored. No live authorization exists.

- Architect final review of `8ae75dc9e2b6b05e8caa4b966caafc7bbcb63574`: ACCEPTED / LIVE-READY / NOT EXECUTED. Local verification was completed on the exact commit with a clean worktree: focused `tests/test_telegram_self_authored_e2e.py` 29 passed, `py_compile` passed, Ruff check passed, Ruff format --check reported both files already formatted, and `git diff --check` was clean. The marker/self-authorship split is therefore accepted: selected E2E messages still require `TG_SELF_E2E_0922A`, while summary neighbors may be marker-free only when canonical outbound MTProto for the same selected account and authored by the connected Telegram user. Inbound/foreign/wrong-account/unknown/missing/legacy cases remain fail-closed. The accepted stdin-fed `docker compose exec` path is unchanged. No new live authorization exists; any further production E2E requires fresh explicit human authorization.

- Human explicitly authorized exactly one new live self-authored Telegram E2E to verify the complete downstream Telegram pipeline after acceptance of `8ae75dc9e2b6b05e8caa4b966caafc7bbcb63574`. That exact code baseline was locally verified with 29 focused tests passing, `py_compile`, Ruff check/format, and `git diff --check` clean. The live path remains stdin-fed `docker compose exec` inside the already-running API container with marker-vs-self-authorship privacy separation. Production runtime remains exact `8ad52f0653f9f90e1932c49532dc4f993ea1a9cc`, Alembic 0046, and long-running API/worker Telegram AI must remain false. Exactly one canonical remote-wrapper invocation is authorized. Any remote/harness BLOCKED, FAILED, nonzero exit, or incomplete PASS report consumes the attempt and requires STOP with no retry.

- Human explicitly authorized termination of the currently running Telegram live E2E attempt only, with no retry. Read-only local inspection showed wrapper PID `2940596` and child SSH PID `2940620` still alive after roughly 1h44m, both waiting in `do_sys_poll`, while the SSH TCP connection remained ESTABLISHED. Termination authorization is narrowly scoped to those existing processes: close the current SSH child first with targeted signals, then allow or terminate the wrapper if necessary. No new live invocation, direct production SSH, manual Docker/Compose, provider diagnostics, production mutation, deploy, env change, or ref movement is authorized.

- Architecture correction from human: the desired Telegram acceptance is the real normal backend pipeline, not an isolated E2E harness. Production should keep `TELEGRAM_MTPROTO_AI_ENABLED=false` globally while allowing only provably self-authored canonical MTProto messages (active scope, outbound, sender_peer_id == owned account.telegram_user_id) through the ordinary AI pipeline. Inbound/foreign Telegram remains excluded. No marker is part of production policy. The approximately two-hour harness attempt was terminated by explicit human authorization; it produced no harness verdict and local wrapper exit 255 after the SSH channel was closed. No retry is authorized. The code/test-only implementation of the central self-authored eligibility exception is recorded below. Deployment/live verification will require separate explicit authorization.

- Telegram self-authored production AI policy: BUILT / NOT DEPLOYED. While `TELEGRAM_MTPROTO_AI_ENABLED=false`, Python eligibility, the SQLAlchemy predicate, and the raw SQL fragment allow a canonical MTProto chat message only when the account is owned by the same user, the peer selection is `scope_active`, direction is outbound, and `sender_peer_id` equals `account.telegram_user_id`. Inbound, foreign sender, missing or unknown direction, missing sender, malformed or wrong account, inactive scope, and noncanonical Telegram stay outside that exception. Global true still admits every canonical message in active scope. The ordinary materializer enqueues embedding for a new self-authored message and enqueues nothing for inbound or foreign messages. Worker embedding, correlation, auto-label, temporal enqueue, conversation-stack summary, and AI-only object/context/search/correlation-candidate paths use the same policy. Recurring embedding catch-up still returns zero jobs while the global flag is false. Production eligibility does not read the E2E marker or the harness. Focused policy, Q1, and full-pipeline tests: 25 passed. `py_compile` passed. Ruff check passed on the edited files. Ruff format --check passed for the new policy module and the new test. `git diff --check` was clean. No production SSH, Docker, provider call, or live Telegram test.

- Architect review of `51b6b2eb2c227c00af680fd7e001289169f11dca`: PARTIAL ACCEPT / NOT DEPLOY-READY. The canonical self-authored MTProto exception, normal materializer path, worker/downstream gates, retrieval parity, and global-false no-catch-up behavior are directionally correct. One policy blocker remains: false-mode eligibility still treats every noncanonical Telegram object as eligible because Python returns true for noncanonical objects and ORM/raw SQL use a `NOT canonical` allow branch. The new test explicitly expects legacy Bot Telegram to be eligible. Under the approved architecture, while global Telegram AI is false every provider=telegram object must remain AI-ineligible except the narrow canonical active-scope outbound sender==owned-account Telegram user exception. A code/test-only correction is recorded below; no deploy/live authorization exists.

- Telegram global-false AI gate: BUILT / NOT DEPLOYED. While `TELEGRAM_MTPROTO_AI_ENABLED=false`, non-Telegram objects stay eligible and every `provider=telegram` object is ineligible unless it is a canonical MTProto `chat_message` in an owned `scope_active` chat, direction outbound, with non-empty `sender_peer_id` equal to `account.telegram_user_id`. Legacy Bot, unknown transport, and non-chat Telegram are false on Python eligibility, the SQLAlchemy predicate, and the raw SQL fragment. Global-true active-scope behavior is unchanged. Recurring catch-up under global false remains zero. Policy, Q1, full-pipeline, and C1A/C1B/C2A tests: 92 passed. `py_compile` passed. Ruff check passed on the edited files. Ruff format --check passed for the policy module and the policy test. `git diff --check` was clean. No production SSH, Docker, deploy, provider call, or live Telegram test.

- Architect final review of `681c0e04df5881124ab8d72a4c05e5a2c7977296`: ACCEPTED / DEPLOY-READY / NOT DEPLOYED. False-mode semantics now match the approved production policy: non-Telegram behavior is unchanged, while every provider=telegram object is AI-ineligible unless it is a canonical MTProto chat message in an owned active scope, direction outbound, with a non-empty sender_peer_id equal to the owned account telegram_user_id. Legacy Bot, unknown transport, non-chat Telegram, inbound/foreign/malformed/inactive cases fail closed across Python eligibility, SQLAlchemy predicate, raw SQL, direct context access, and normal downstream surfaces. Global-true behavior remains unchanged. Global-false recurring catch-up still enqueues zero jobs. Reported focused Telegram suites total 92 passed; py_compile, Ruff check/format, and git diff --check passed. No deploy/live authorization exists. The intended next acceptance after an explicitly authorized deploy is a brand-new ordinary self-authored Telegram message flowing through the real materializer/worker pipeline; the old E2E harness is no longer the acceptance mechanism.

- Human explicitly authorized schema-neutral production deployment of exact release `681c0e04df5881124ab8d72a4c05e5a2c7977296` for later live verification of the normal Telegram pipeline. Architect fast-forwarded canonical `origin/production` non-force from `8ad52f0653f9f90e1932c49532dc4f993ea1a9cc` to the exact authorized release, matching the normal deploy harness precondition. Authorized deploy parameters are release `681c0e04df5881124ab8d72a4c05e5a2c7977296`, rollback `8ad52f0653f9f90e1932c49532dc4f993ea1a9cc`, Alembic `0046`, via unchanged `ops/production/deploy.py` only. No live Telegram message test, old E2E harness, direct SSH, manual Docker/Compose, env change, provider diagnostic, manual job enqueue, or historical catch-up is authorized by this deploy task.

- Schema-neutral production deploy of `681c0e04df5881124ab8d72a4c05e5a2c7977296`: PASS through unchanged `ops/production/deploy.py`. Exit status 0. Harness reported `RELEASE_HEAD=681c0e04df5881124ab8d72a4c05e5a2c7977296`, `HEALTH=PASS`, `ALEMBIC=0046`, `DB_CONTAINER_UNCHANGED=true`, `DB_VOLUME_UNCHANGED=true`, `ENV_FILE_UNCHANGED=true`, `API_RECREATED=true`, `WORKER_RECREATED=true`, `DEPLOYMENT=PASS`. Rollback SHA remains `8ad52f0653f9f90e1932c49532dc4f993ea1a9cc` and was not used. No retry, direct SSH, manual Docker/Compose, env change, E2E harness, provider diagnostic, job enqueue, or Telegram catch-up. Live Telegram message verification is not done.

- Architect accepted the reported deployment result for `681c0e04df5881124ab8d72a4c05e5a2c7977296`: production deployment PASS, exit 0, health PASS, Alembic 0046, DB container/volume and env unchanged, API/worker recreated, rollback not used. Canonical production ref independently resolves to the exact release. The next acceptance is the real normal Telegram pipeline: the human will send a brand-new ordinary self-authored message in an already active-scope chat. No E2E harness, manual sync, manual enqueue, global AI enablement, provider diagnostic, or production mutation is authorized. After the human reports the message was sent, a separate read-only verification task is required.

- Human live observation after production release `681c0e04df5881124ab8d72a4c05e5a2c7977296`: a newly sent ordinary self-authored Telegram message in an active-scope solo group is not visible in the Inbox/feed, while downstream temporal/task evidence and graph/message evidence are visible. Architect code review identifies a concrete presentation cause: `RecentSourceService._not_outbound_chat_clause()` suppresses outbound Telegram/Teams chat messages from the feed. This is separate from the AI pipeline. Architect also reviewed the deployed AI gate: false mode uses the narrow self-authored exception, while true mode still delegates to the pre-existing active-scope predicate; focused tests already cover true-mode inbound+outbound and false->true bounded/idempotent catch-up. Current task authorizes one narrowly scoped BREAK-GLASS READ-ONLY production verification of the newest recent self-authored object using a PostgreSQL read-only transaction with sanitized booleans/counts only, followed by code/test-only removal of outbound feed suppression and strengthened false->true->false regression. No deploy, production AI toggle, manual sync/enqueue, provider call, transport/session access, production DB write, or service mutation is authorized.

- BREAK-GLASS read-only production inspection of the newest post-deploy self-authored canonical MTProto object: PASS with exit 0. Transaction used `SET TRANSACTION READ ONLY` and rolled back. Markers: `PRODUCTION_RELEASE=PASS`, `GLOBAL_TELEGRAM_AI_FALSE=PASS`, `RECENT_SELF_AUTHORED_FOUND=PASS`, `SELF_AUTHORED_POLICY=PASS`, `AI_ELIGIBLE=PASS`, `EMBEDDING_PRESENT=PASS`, `EMBED_JOB_PRESENT=PASS`, `EMBED_JOB_DONE=PASS`, `DOWNSTREAM_JOB_TYPES_PRESENT=auto_label_object,correlate_object,extract_temporal_signal`, `DOWNSTREAM_FAILED_JOBS=0`, `AI_TRACE_SUCCESS_COUNT=4`, `AI_TRACE_FAILED_COUNT=0`, `TEMPORAL_EVIDENCE_PRESENT=FAIL`, `CORRELATION_EVIDENCE_PRESENT=PASS`, `AI_ONLY_OBJECT_VISIBLE=PASS`, `CONTEXT_VISIBLE=PASS`, `INBOX_FEED_VISIBLE=FAIL`, `OUTBOUND_FEED_SUPPRESSION_CONFIRMED=PASS`. No provider call, Telegram transport/session decrypt, sync, enqueue, DB write, restart, or recreate. Production runtime/ref remains `681c0e04df5881124ab8d72a4c05e5a2c7977296`. Production `TELEGRAM_MTPROTO_AI_ENABLED` remains false. No second production query.

- Bidirectional Inbox/feed parity: BUILT / NOT DEPLOYED. `RecentSourceService` no longer suppresses outbound Telegram or Teams chat messages, so Inbox/feed and conversation detail include both directions. Mattermost, Gmail, and Yandex feed rules are unchanged, including Gmail noise exclusion. Ordinary Telegram active-scope visibility, rejected/deleted filtering, and child-email attachment filtering are unchanged. Visible outbound messages do not become inbox attention: ordinary Telegram MTProto `message_created` notifications stay excluded from attention. Telegram AI eligibility is unchanged. Same-data regression covers `TELEGRAM_MTPROTO_AI_ENABLED=false -> true -> false`: false admits only self-authored outbound, true admits active-scope inbound and outbound without metadata writes and catch-up is bounded then idempotent, and returning to false closes inbound again while self-authored outbound stays eligible. Python, ORM, and raw SQL agree at each stage. Focused policy, full-pipeline, bidirectional feed, Teams, and conversation tests: 74 passed. `py_compile` passed. Ruff check passed on the edited files. Ruff format --check passed for the new feed test and the policy test. Historical files were not wholesale-reformatted. `git diff --check` was clean. No deploy, production ref movement, production env change, or production AI toggle.

- Architect final review of `d22c6cf78945c8f92934a46431b2bcc1887fd8c8`: ACCEPTED / DEPLOY-READY / NOT DEPLOYED. Production remains exact `681c0e04df5881124ab8d72a4c05e5a2c7977296`, Alembic 0046, Telegram AI false. Read-only production evidence confirms the real self-authored Telegram object passed materialization, AI eligibility, embedding, normal downstream job creation/execution, correlation, AI-only visibility and context; the deployed feed hid it specifically because of the legacy outbound Telegram/Teams suppression. The accepted change removes only that feed suppression, preserves attention semantics and all other feed/visibility filters, and adds bidirectional Telegram/Teams conversation-detail coverage. The temporal extraction job succeeded but the sanitized selected source had no temporal evidence edge; because accepted exact temporal outcomes necessarily create/update such an edge, this indicates no accepted exact temporal anchor for that exact selected source rather than a pipeline/provider failure. The same-data false->true->false regression proves the self-authored false-mode exception does not modify or constrain the existing true-mode active-scope policy: both directions become eligible under true without metadata rewrite, catch-up is bounded/idempotent, and returning false restores the narrow gate. Focused suite 74 passed plus py_compile/Ruff/diff checks. No deploy or production true-mode toggle is authorized.

- Human explicitly authorized schema-neutral production deployment of exact parity release `d22c6cf78945c8f92934a46431b2bcc1887fd8c8`. Architect verified it is a non-force fast-forward from current production `681c0e04df5881124ab8d72a4c05e5a2c7977296` and advanced canonical `origin/production` to the exact release. Authorized deploy parameters: release `d22c6cf78945c8f92934a46431b2bcc1887fd8c8`, rollback `681c0e04df5881124ab8d72a4c05e5a2c7977296`, Alembic `0046`, via unchanged `ops/production/deploy.py` only. This deploy does not authorize enabling Telegram AI, live Telegram/provider testing, manual sync/enqueue, backlog processing, direct SSH, manual Docker/Compose, env changes, DB writes, or old E2E harness execution.

- Schema-neutral production deploy of `d22c6cf78945c8f92934a46431b2bcc1887fd8c8`: PASS through unchanged `ops/production/deploy.py`. Exit status 0. Stderr empty. Harness reported `RELEASE_HEAD=d22c6cf78945c8f92934a46431b2bcc1887fd8c8`, `HEALTH=PASS`, `ALEMBIC=0046`, `DB_CONTAINER_UNCHANGED=true`, `DB_VOLUME_UNCHANGED=true`, `ENV_FILE_UNCHANGED=true`, `API_RECREATED=true`, `WORKER_RECREATED=true`, `DEPLOYMENT=PASS`. Rollback SHA `681c0e04df5881124ab8d72a4c05e5a2c7977296` was not used. No retry, direct SSH, manual Docker/Compose, env change, Telegram AI toggle, manual sync/enqueue, backlog processing, provider test, or old E2E harness.

- Architect post-deploy acceptance of `d22c6cf78945c8f92934a46431b2bcc1887fd8c8`: DEPLOYED / PASS. Canonical production ref is exact release; health PASS; Alembic 0046; DB container/volume and production env unchanged; API/worker recreated; rollback unused. Bidirectional Telegram/Teams feed parity is now live in production. Telegram AI remains false; no live provider test or full false->true production activation occurred. Repository is in HOLD. Any future production activation of `TELEGRAM_MTPROTO_AI_ENABLED=true` is a separate explicit authorization boundary and must use its own rollout/preflight/rollback plan.

- New bounded UX bugfix pass authorized by human; production actions remain unauthorized. Scope: (A) preserve existing automatic Google access-token refresh while retaining safe OAuth provider error codes such as invalid_grant and documenting the operational requirement that Testing publishing status cannot provide persistent non-basic OAuth authorization; (B) make voice/hands-free Assistant failures visible and emit one local failure cue instead of silently returning to idle, including existing assistant_round_limit reason; (C) add desktop Inbox card delete quick action reusing the existing universal delete API/confirmation/local-removal path; (D) replace ambiguous Assistant "seen objects" references with provenance-safe exact object citations/opening so explicitly cited results survive the generic 8-reference cap and can open exact Secretary objects. Google Cloud publishing-status changes, real OAuth reauthorization, deploy, provider calls, and production mutation are outside this task. Deferred backlog remains only the temporal already_evidenced/idempotency nuance and eventual PROJECT_STATE history cleanup.

- UX bugfix pack: BUILT / NOT DEPLOYED. Google access-token refresh is unchanged. Non-retryable refresh failures such as `invalid_grant` keep only the sanitized OAuth error code, classify as authentication, and tell the user to reconnect; transient OAuth errors stay retryable. User-visible diagnostics do not include tokens or client secrets. `docs/google_oauth.md` records that Testing-mode expiry cannot be bypassed in code and that continuous use needs Google OAuth Publishing status In Production or an appropriate Internal mode. Hands-free Assistant failures stay in the voice error state with the existing message, including the round-limit text, and play one local failure cue; typed failures do not play that cue and do not become chat messages. Desktop Inbox cards have a trash action after Ask Secretary and Open in Graph, reusing the existing delete/confirmation/local-removal path; Android swipe is unchanged. Assistant answers may cite `secretary://object/<uuid>` only when that id was exposed this turn; unseen, invented, and foreign ids are not clickable. Exact citations are kept ahead of the generic 8-reference cap and open through `openObjectDetail()`. http/https links stay external. Focused backend citation/OAuth/sync tests: 25 passed. `py_compile` passed. Ruff check passed on the edited modules. Ruff format --check passed for the new citation module. Historical files were not wholesale-reformatted. Flutter voice, markdown, source-sync, and inbox delete tests passed. `flutter analyze` on the touched libraries reported no new errors. `git diff --check` was clean. No production SSH, Docker, deploy, Google Cloud mutation, OAuth reauthorization, or provider call.

- Architect final review of `2a5d76ae80d53c13a6581852ef1a0362ad1a3e38`: ACCEPTED / DEPLOY-READY / NOT DEPLOYED. Package is schema-neutral relative to production `d22c6cf78945c8f92934a46431b2bcc1887fd8c8` / Alembic 0046. Google automatic access-token refresh is preserved; permanent OAuth refresh failures retain only a sanitized provider code such as invalid_grant, remain authentication failures, and direct the user to reconnect, while transient failures remain retryable and secrets/raw provider bodies are not surfaced. docs/google_oauth.md records that Testing-mode authorization expiry cannot be bypassed in application code and continuous use requires In Production or an appropriate Internal publishing mode. Voice-origin Assistant failures now remain visibly errored and emit one local failure cue while typed failures do not; desktop Inbox cards gain a trash quick action reusing existing delete semantics; provenance-safe secretary://object citations are server-validated against current-turn exposure, exact cited objects survive the generic reference cap, and the client opens them through openObjectDetail while external http/https behavior is preserved. Reported backend focused tests 25 passed plus py_compile/Ruff/diff checks; focused Flutter tests passed and analyze reported no new errors. The currently expired Google refresh token is not repaired by this code and will require one fresh OAuth authorization after the Google Cloud publishing mode is corrected. No deploy, Google Cloud mutation, real OAuth reauthorization, provider call, or production mutation is authorized.

- Human explicitly authorized normal schema-neutral production deployment of exact release `2a5d76ae80d53c13a6581852ef1a0362ad1a3e38`. Architect verified it is a clean fast-forward from production `d22c6cf78945c8f92934a46431b2bcc1887fd8c8`, with no migration files in the delta and Alembic remaining 0046, then fast-forwarded canonical `production` ref non-force to the exact authorized release. Authorized execution path is only `ops/production/deploy.py --release-sha 2a5d76ae80d53c13a6581852ef1a0362ad1a3e38 --rollback-sha d22c6cf78945c8f92934a46431b2bcc1887fd8c8 --expected-alembic 0046`. No Google Cloud change, OAuth reauthorization, Telegram AI toggle, provider diagnostic, direct SSH/manual Docker, env change, or schema migration is authorized.

- Schema-neutral production deploy of `2a5d76ae80d53c13a6581852ef1a0362ad1a3e38`: PASS through unchanged `ops/production/deploy.py`. Exit status 0. Stderr empty. Harness reported `RELEASE_HEAD=2a5d76ae80d53c13a6581852ef1a0362ad1a3e38`, `HEALTH=PASS`, `ALEMBIC=0046`, `DB_CONTAINER_UNCHANGED=true`, `DB_VOLUME_UNCHANGED=true`, `ENV_FILE_UNCHANGED=true`, `API_RECREATED=true`, `WORKER_RECREATED=true`, `DEPLOYMENT=PASS`. Rollback SHA `d22c6cf78945c8f92934a46431b2bcc1887fd8c8` was not used. No retry, Google Cloud change, OAuth reauthorization, Telegram AI toggle, provider diagnostic, direct SSH, manual Docker/Compose, or env change.

- Architect post-deploy acceptance of `2a5d76ae80d53c13a6581852ef1a0362ad1a3e38`: DEPLOYED / PASS. Canonical production ref equals the exact release; health PASS; Alembic 0046; DB container, DB volume, and production env unchanged; API and worker recreated; rollback unused; exit 0 and stderr empty. Quick UX bugfixes are now live: safe Google OAuth refresh diagnostics, persistent hands-free failure state plus local failure cue, desktop Inbox trash quick action, and provenance-safe exact Assistant object citations. The release does not revive an already expired/revoked Google refresh token. Repository returned to HOLD. Google Cloud OAuth publishing-mode inspection/change and one fresh Google OAuth authorization remain separate operator actions requiring explicit authorization. Deferred backlog remains temporal already_evidenced/idempotency and eventual PROJECT_STATE history cleanup.
- Human requested a self-contained public Google OAuth branding site on the Secretary server. Architect selected a repository-managed static HTTPS surface at `https://web-itx.duckdns.org`: static homepage/privacy/terms under `infra/public`, served by a minimal Caddy `public_web` Compose service that exposes only those pages and does not proxy/expose the Secretary API. Current task is code/test-only and also requires dedicated fail-closed one-shot rollout tooling that reuses the existing pinned production target/SSH trust contract and can start only `public_web` while proving DB/API/worker/.env unchanged. Exact Google Branding URLs after rollout will be `/`, `/privacy`, and `/terms`. Google Cloud publishing change, Search Console verification, real OAuth reauthorization, DNS/firewall changes, production ref movement, and live deployment remain unauthorized pending Architect review.

- Public OAuth branding site: BUILT / NOT DEPLOYED. Static pages at `infra/public/` identify Personal Secretary, describe Gmail, Google Calendar, and Google Drive use, and link `/privacy` and `/terms`. The privacy page states imported storage, encrypted OAuth tokens, no sale or advertising use, configured AI-provider disclosure, and revocation without inventing a retention period. `infra/Caddyfile` serves only `/`, `/privacy`, `/terms` (optional trailing slashes) over automatic HTTPS and returns 404 for other paths, with no API reverse proxy. Compose service `public_web` uses pinned `caddy:2.10.2`, publishes TCP 80 and 443 only, and has no Secretary secret environment or database dependency. `ops/production/public_web_rollout.py` and `ops/production/remote_public_web.py` are fail-closed and were not executed. They can recreate only `public_web` after the production checkout and `origin/production` already match an exact release SHA, and they leave db, api, worker, and `.env` unchanged. `docs/google_oauth.md` records the branding URLs for use after a later successful rollout. Focused tests: 9 passed. `py_compile` passed. Ruff check and format --check passed on the new helper and tests. `docker compose config` resolved `public_web` as `caddy:2.10.2` on TCP 80/443 with no environment and no dependency. `caddy validate` reported a valid configuration. `git diff --check` was clean. No Alembic files changed. No production SSH, public_web start, DNS/firewall change, Google Cloud mutation, or OAuth reauthorization. Production runtime remains `2a5d76ae80d53c13a6581852ef1a0362ad1a3e38`.

- Architect review of `54050ea52efb5234428dfa1fd047cbaa2766680c`: static branding pages, Caddy configuration, isolated `public_web` Compose service, documentation, and schema-neutral scope accepted, but package is NOT YET DEPLOY-READY due to one rollout-helper reliability defect. In `remote_public_web.py`, initial curl connection/TLS failure escapes `collect_statuses()` before its intended 45-second retry window, and because `collect_statuses()` is called before the existing cleanup try/except, a failed first rollout can leave the newly-created `public_web` service running without successful verification. Active correction is code/test-only: tolerate temporary HTTPS startup failures within a bounded poll and guarantee narrow first-rollout cleanup for any post-start failure while preserving pre-existing public_web on failed updates. No production/public_web/Google Cloud/OAuth action is authorized.

- Public web rollout helper correction: BUILT / NOT DEPLOYED. `collect_statuses()` treats a temporary connection or TLS `PublicWebError` as not ready and sleeps until the 45-second deadline before failing. It does not use `curl -k` or another TLS bypass. Any failure after the `public_web` start attempt, including persistent verification failure, an unexpected exception, an identity mismatch, or a missing or stopped `public_web`, stop/removes only that new service. A `public_web` that already existed is left in place. Cleanup commands cannot target db, api, or worker and do not remove Caddy volumes. db, api, worker, and `.env` stay untouched. Focused branding/rollout tests: 19 passed. `py_compile` passed. Ruff check and format --check passed. `docker compose config --quiet` passed. `git diff --check` was clean. No production SSH, public_web start, deploy, DNS/firewall change, Google Cloud mutation, or OAuth reauthorization. Production runtime remains `2a5d76ae80d53c13a6581852ef1a0362ad1a3e38`.

- Architect review of `157fbbbd7545192cd65639426becea90999e1ef7`: prior TLS readiness and first-rollout cleanup blocker is fixed; bounded retry, narrow cleanup, pre-existing-service preservation branch, and no-insecure-TLS behavior are accepted. One final narrow rollout-helper issue remains before DEPLOY-READY: `existed_before` is derived from `docker compose ps -q public_web`, which Docker documents as running-only by default, so a previously-created but stopped/exited public_web would be misclassified as a first rollout and could be removed after failure. Active correction is code/test-only: use an all-state Compose existence query for the pre-rollout existence decision while retaining a strict running-container check after start. No production/public_web/Google Cloud/OAuth action is authorized.

- Public web existence check: BUILT / NOT DEPLOYED. `existed_before` now comes only from `docker compose ps --all -q public_web`, so a stopped or exited container still counts as pre-existing and a failed update does not stop or remove it. After start, `service_id()` still uses running-only `ps -q` and `require_running()` still rejects a container that is not running. Bounded HTTPS polling, narrow first-rollout cleanup, and the prohibition on insecure TLS are unchanged. Focused branding/rollout tests: 24 passed. `py_compile` passed. Ruff check and format --check passed. `docker compose config --quiet` passed. `git diff --check` was clean. No production SSH, public_web start, deploy, DNS/firewall change, Google Cloud mutation, or OAuth reauthorization. Production runtime remains `2a5d76ae80d53c13a6581852ef1a0362ad1a3e38`.

- Architect final review of `3930c4b4c026a2e1fc9acb31c0d8fea1b9422b5c`: ACCEPTED / DEPLOY-READY / NOT DEPLOYED. Static branding pages, Caddy configuration, isolated `public_web` Compose service, and dedicated rollout tooling are accepted. Final stopped-container edge case is closed: pre-rollout existence uses all-state `docker compose ps --all -q public_web`, so stopped/exited pre-existing containers are preserved on failed update, while post-start validation remains running-only and requires an actually running container. Prior bounded HTTPS/TLS retry and first-rollout narrow cleanup semantics remain intact. Focused tests 24 passed; py_compile, Ruff check/format, compose config validation, and diff check passed. No migration files changed. Production remains `2a5d76ae80d53c13a6581852ef1a0362ad1a3e38` / Alembic 0046. Live public_web rollout, Google Cloud branding/publishing changes, and fresh OAuth authorization remain separate explicit authorization steps.

- Human explicitly authorized schema-neutral production deploy of `3930c4b4c026a2e1fc9acb31c0d8fea1b9422b5c` with rollback `2a5d76ae80d53c13a6581852ef1a0362ad1a3e38`, Alembic `0046`, API/worker recreation through the normal deploy harness, canonical production-ref movement to the release, and then the first `public_web` rollout for the same release. Google Cloud/OAuth, DNS/firewall, env changes, direct SSH/manual Docker remain unauthorized.

- Authorized production rollout execution attempt from Architect environment: canonical `production` ref was successfully moved non-force from `2a5d76ae80d53c13a6581852ef1a0362ad1a3e38` to authorized release `3930c4b4c026a2e1fc9acb31c0d8fea1b9422b5c`. The approved `ops/production/deploy.py` harness was NOT started against production because the Architect execution container cannot resolve `github.com`, so a required fresh canonical local checkout could not be created. No production SSH, API/worker recreation, public_web start, DB/env change, Google Cloud/OAuth action, DNS/firewall action, or manual Docker/Compose occurred. Production runtime therefore remains `2a5d76ae80d53c13a6581852ef1a0362ad1a3e38` while canonical production ref is already `3930c4b4c026a2e1fc9acb31c0d8fea1b9422b5c`. Current authorized task remains active and should be continued by an Executor environment that can run the repository harness from a canonical clean up-to-date main checkout; no direct-SSH workaround is authorized.


- Public OAuth branding production deploy continuation: DEPLOY PASS / PUBLIC_WEB BLOCKED BEFORE START. Canonical deploy harness completed exact release `3930c4b4c026a2e1fc9acb31c0d8fea1b9422b5c` with `HEALTH=PASS`, Alembic `0046`, DB container and volume unchanged, production env unchanged, and API/worker recreated. Preflight confirmed authoritative `origin/production` already equals the release, the release is a clean fast-forward from rollback `2a5d76ae80d53c13a6581852ef1a0362ad1a3e38`, and no migration files changed. The subsequent first `public_web` rollout exited 2 with sanitized blocker `PUBLIC_WEB_BLOCKED=public web ports already occupied` before starting `public_web`; no branding HTTPS verification ran and no public_web mutation occurred. Production application runtime is therefore accepted at `3930c4b4c026a2e1fc9acb31c0d8fea1b9422b5c`; next work is read-only ownership diagnosis of existing listeners on TCP 80/443. No stop/restart/reload/removal/reconfiguration of the occupying service is authorized.

- BREAK-GLASS read-only port diagnosis: PASS. Production checkout HEAD matched `3930c4b4c026a2e1fc9acb31c0d8fea1b9422b5c` before observation. TCP 80 listens on `0.0.0.0` and `[::]`, process `nginx`, PID `3604234`. TCP 443 listens on `0.0.0.0` and `[::]`, same process `nginx`, PID `3604234`. No running Docker container publishes port 80 or 443. Compose `ps --all -q public_web` is absent. No process, container, firewall, DNS, or file was changed. `public_web` was not started. Deploy was not rerun.


- Architect acceptance of BREAK-GLASS port diagnosis `a3e3986b95847fd0e83c372c2cc7cc5c4931f7f8`: ACCEPTED. The blocker is conclusively host-level nginx, not Docker or an existing `public_web` container. TCP 80 and 443 are owned by the same nginx process; no running container publishes those ports; Compose has no `public_web` in any state. Therefore the original standalone Caddy host-port design cannot be rolled out unchanged without conflicting with the existing production front door. No existing listener may be stopped or reconfigured until its active routing role is inspected and reviewed.

- BREAK-GLASS read-only nginx routing inspection: PASS. Production checkout HEAD matched `3930c4b4c026a2e1fc9acb31c0d8fea1b9422b5c`. nginx version `1.24.0`. Hostname `web-itx.duckdns.org` is served by two public server blocks, with no HTTP-to-HTTPS redirect. Port 80 is `/etc/nginx/sites-enabled/web-itx.conf`, `listen 80` and `listen [::]:80`, server root `/var/www/web-itx`. `location /.well-known/acme-challenge/` uses `root /var/www/web-itx`. `location /` is static `try_files` and has no `proxy_pass`, `alias`, or `return`. Port 443 is `/etc/nginx/sites-enabled/web-itx-ssl.conf`, `listen 443 ssl http2` and `listen [::]:443 ssl http2`, same server root `/var/www/web-itx`. TLS certificate path `/etc/letsencrypt/live/web-itx.duckdns.org/fullchain.pem`; key path `/etc/letsencrypt/live/web-itx.duckdns.org/privkey.pem`; contents were not read. `location /secretary` is `return 301` to the path `/secretary/`. `location /secretary/` is `proxy_pass` to `127.0.0.1:18080`. `location /` on 443 is static `try_files` with no `proxy_pass`, `alias`, or `return`. The same SSL file also has a loopback-only server for this hostname on `127.0.0.1:18443` with static `try_files` and no Secretary proxy. No nginx reload, edit, Docker change, deploy, firewall/DNS change, or `public_web` start occurred.


- Architect acceptance of nginx routing inspection `3d828587fb30f6140e65f85c45b6020e6b6a56f8`: ACCEPTED. Existing host nginx is the production front door for `web-itx.duckdns.org`; public HTTP/HTTPS static content is rooted at `/var/www/web-itx`, while only `/secretary/` proxies to the Secretary API at `127.0.0.1:18080`. The standalone Caddy/`public_web` host-port design is superseded and must not be deployed. Planned replacement is an nginx-preserving static branding rollout that changes only bounded files under the existing static root and does not edit/reload nginx or touch the Secretary proxy. One final read-only target-root/route-shape preflight is required before implementing that rollout.

- BREAK-GLASS read-only static-root preflight: PASS. Authoritative `origin/production` and production checkout HEAD remained `3930c4b4c026a2e1fc9acb31c0d8fea1b9422b5c`. Public port-80 and port-443 `location /` both use exactly `try_files $uri $uri/ =404;`. `/var/www/web-itx` resolves to itself, owner `www-data:www-data`, mode `0755`. `index.html` exists as a regular file, same owner, mode `0644`, 2923 bytes, SHA-256 `0983a566e6f4db3bb18eabcb3cb51165ec8e60d54f8968041b51f90134379a27`. `privacy`, `privacy.html`, `privacy/index.html`, `terms`, `terms.html`, and `terms/index.html` are absent. File contents were not printed. nginx master account `root` and worker account `www-data` can both traverse and read the static root; `www-data` can read `index.html`. Status codes only: `http://web-itx.duckdns.org/` 200, `https://web-itx.duckdns.org/` 200, `https://web-itx.duckdns.org/privacy` 404, `https://web-itx.duckdns.org/privacy/` 404, `https://web-itx.duckdns.org/terms` 404, `https://web-itx.duckdns.org/terms/` 404, `https://web-itx.duckdns.org/secretary/` 404. No file, permission, nginx, Docker, deploy, firewall, DNS, or `public_web` change occurred.


- Architect acceptance of static-root preflight `976e19d8b385c015aca957b3a381b0f29514d0f1`: ACCEPTED. Existing nginx public root is exactly `/var/www/web-itx` (`www-data:www-data`, 0755); only `index.html` currently exists among the bounded branding candidates, while privacy/terms paths are absent. Public port-80 and port-443 `location /` both use `try_files $uri $uri/ =404;`. Therefore the nginx-preserving branding layout is fixed as `index.html`, `privacy/index.html`, and `terms/index.html`; this shape supports both slash and non-slash privacy/terms URLs without editing nginx. Standalone Caddy/Compose `public_web` remains retired. Next work is code/test-only replacement of the rollout tooling; no production mutation is authorized by this acceptance.

- Nginx-root branding publisher: BUILT / NOT EXECUTED. Canonical pages remain `infra/public/index.html`, `privacy.html`, and `terms.html`. Compose no longer defines `public_web` or Caddy volumes, and `infra/Caddyfile` is removed, so no repository service binds ports 80 or 443. `ops/production/public_web_rollout.py` still uses the pinned production target, but its remote helper now publishes only `/var/www/web-itx/index.html`, `privacy/index.html`, and `terms/index.html` by same-directory atomic replace. It checks source hashes, preserves pre-existing target bytes and mode for rollback, creates `privacy` and `terms` only when missing, and on identity drift or public verification failure restores those paths and removes only directories this rollout created when they are empty. Verification requires HTTPS 200 for `/`, `/privacy`, `/privacy/`, `/terms`, and `/terms/` with the repository page hash, and 404 for an unrelated path. It does not edit, reload, or restart nginx and does not mutate Docker or Compose. Focused tests: 16 passed. `py_compile` passed. Ruff check and format --check passed. `docker compose config --quiet` passed. `git diff --check` was clean. The publisher was not run against production. Production runtime remains `3930c4b4c026a2e1fc9acb31c0d8fea1b9422b5c`.


- Architect review of nginx-root publisher `60d7a42c0f406921c7e2298c12e4e909f40ecdc9`: NOT YET DEPLOY-READY. The Caddy retirement, bounded target paths, rollback structure, runtime-identity guard, and no-nginx/no-Docker mutation direction are accepted. Two correctness/trust blockers remain. First, the package models both `/privacy` and `/privacy/` (and terms equivalents) as direct 200 responses with identical body hashes. With the observed production nginx `try_files $uri $uri/ =404;` and directory targets such as `privacy/index.html`, the no-trailing-slash directory request is expected to redirect to the slash form rather than return the page body directly; the focused test currently fabricates 200 responses and therefore does not exercise real nginx route semantics. Second, the local launcher requires local HEAD to equal `origin/main` but does not bind that HEAD/helper source to the supplied exact `--release-sha`, allowing a future newer main helper to be streamed while production is pinned to an older release. A narrow code/test correction is required before production authorization. No production mutation is authorized.

- Branding publisher route and release binding: BUILT / NOT EXECUTED. Canonical page links and `docs/google_oauth.md` now use `https://web-itx.duckdns.org/privacy/` and `https://web-itx.duckdns.org/terms/`. Verification requires HTTPS 200 plus the exact page hash for `/`, `/privacy/`, and `/terms/`; 301 from `/privacy` and `/terms` to those same-host slash URLs; and 404 for an unrelated path. It does not follow redirects. The local launcher requires `HEAD == --release-sha` as well as `HEAD == origin/main`, so a newer checkout cannot stream the helper for an older release. Before any target read or write, `/var/www/web-itx` must be a real directory whose resolved path is exactly that root, and existing `privacy` or `terms` parents must be real directories rather than symlinks. Focused tests: 18 passed. `py_compile` passed. Ruff check and format --check passed. `docker compose config --quiet` passed. `git diff --check` was clean. The publisher was not run against production. Production runtime remains `3930c4b4c026a2e1fc9acb31c0d8fea1b9422b5c`.


- Architect review of branding publisher correction `5dff00d482eafc7982d7ea2a6c763fc806d6f3d0`: NOT YET DEPLOY-READY. The nginx redirect semantics, canonical trailing-slash URLs, exact local HEAD/release binding, and static-root/parent symlink guards are accepted. One remaining release-integrity blocker exists: the remote helper currently reads canonical HTML from the mutable production working tree and derives the manifest from those same bytes. `HEAD == release` alone does not prove tracked files are unmodified, so a dirty `infra/public/*.html` could be self-consistently hashed and published. The remote execution must fail closed on tracked-worktree drift and bind page bytes to the exact authorized Git commit object before any production target write. No production mutation is authorized.

- Branding page bytes bound to the release object: BUILT / NOT EXECUTED. Before any static-root read or write, the helper rejects a dirty tracked worktree from `git status --porcelain --untracked-files=no` with a sanitized error that does not list paths. Untracked files are not part of that check. The three HTML sources are read with `git cat-file blob <release_sha>:<path>` and checked against `git rev-parse` object ids. A missing blob or an object-id mismatch fails before any target write. Redirect, rollback, symlink, and runtime-identity behavior is unchanged. Focused tests: 22 passed. `py_compile` passed. Ruff check and format --check passed. `docker compose config --quiet` passed. `git diff --check` was clean. The publisher was not run against production. Production runtime remains `3930c4b4c026a2e1fc9acb31c0d8fea1b9422b5c`.


- Architect final review of nginx-root branding publisher `3486c830b3b20cfd8f8d7c66c17e9872b6b575fc`: ACCEPTED / DEPLOY-READY / NOT DEPLOYED. The release closes the tracked-worktree and exact-Git-object integrity gap before any static-root operation: tracked drift fails closed with untracked files excluded from enumeration, and the three branding page byte streams are loaded from exact `release_sha` Git blobs and checked against their object ids. Previously accepted safeguards remain intact: canonical/pinned SSH target, local and remote exact-release binding, static-root/parent/final-path symlink rejection, ownership limited to `index.html`, `privacy/index.html`, and `terms/index.html`, rollback of owned paths on runtime identity or public verification failure, exact page-body verification for slash URLs, exact same-host 301 verification for non-slash privacy/terms URLs, unrelated-path 404, and no nginx or Docker mutation. Focused tests 22 passed; py_compile, Ruff check/format, Compose config validation, and diff check passed. Relative to current production release `3930c4b4c026a2e1fc9acb31c0d8fea1b9422b5c`, the candidate is a clean forward history with no Alembic/migration files changed. Production remains on `3930c4b4c026a2e1fc9acb31c0d8fea1b9422b5c` / Alembic 0046. Deploying `3486c830b3b20cfd8f8d7c66c17e9872b6b575fc`, moving the production ref, and running the branding publisher require a new explicit human authorization.

- Authorized production branding rollout of `caab6f1825f69635f88724207ccf173b808dafa4`: PASS. Local main matched `origin/main`, parent was `339bab7a8d541c29133e843adbf38abb33e6a00a`, and the only path changed from that parent was `CURRENT_TASK.md`. `origin/production` was fast-forwarded non-force from `3930c4b4c026a2e1fc9acb31c0d8fea1b9422b5c` to the release. No Alembic files changed. `ops/production/deploy.py` exited 0 with `RELEASE_HEAD=caab6f1825f69635f88724207ccf173b808dafa4`, `HEALTH=PASS`, `ALEMBIC=0046`, `DB_CONTAINER_UNCHANGED=true`, `DB_VOLUME_UNCHANGED=true`, `ENV_FILE_UNCHANGED=true`, `API_RECREATED=true`, `WORKER_RECREATED=true`, `DEPLOYMENT=PASS`. Rollback was not used. The nginx-root publisher then exited 0 with `PUBLIC_WEB=PASS`: home 200, `/privacy` 301, `/privacy/` 200, `/terms` 301, `/terms/` 200, unrelated path 404, and DB container, DB volume, env, API, and worker unchanged. No manual SSH, manual Docker, nginx reload, DNS/firewall change, or Google Cloud/OAuth action.


- Architect final acceptance of production branding rollout ledger `26a9f76097dd11d73c437a56c9932582ab653c92`: ACCEPTED / COMPLETE. Production runtime and canonical `production` ref are aligned at exact release `caab6f1825f69635f88724207ccf173b808dafa4`, Alembic `0046`. The schema-neutral deploy passed with health PASS, unchanged DB container/volume and production env, API/worker recreated, and rollback unused. The nginx-root branding publisher passed with the accepted route semantics: home 200, non-slash privacy/terms 301 to same-host slash URLs, slash privacy/terms 200 with authorized page verification, unrelated path 404, and unchanged DB/container/env/API/worker identities during publication. No nginx reload/edit, manual SSH/Docker workaround, DNS/firewall change, or Google Cloud/OAuth mutation occurred. Branding rollout is closed; repository remains HOLD.

- Homepage Search Console verification tag: BUILT / NOT DEPLOYED. Commit `f334fd69ba70df152a5ccdfeaa015017f5a336e2` adds one `google-site-verification` meta tag to `infra/public/index.html` with content `hTkY_ZxfZrzsygn-3oU4wxH6zraiRWo28hGXZF_U1Is`. Privacy and terms pages do not contain the tag. Focused branding tests: 23 passed. Ruff check and format --check passed. `git diff --check` was clean. Production was not touched and remains `caab6f1825f69635f88724207ccf173b808dafa4`.


- Architect review of Search Console verification tag implementation `f334fd69ba70df152a5ccdfeaa015017f5a336e2` / ledger `445a9fab0ca71229d6b5e0855f0850f07b67fe58`: ACCEPTED / DEPLOY-READY / NOT DEPLOYED. The homepage contains exactly one `google-site-verification` meta tag with the supplied public token; privacy and terms pages do not contain it. Focused branding tests 23 passed; Ruff check/format and `git diff --check` passed. The current main delta from production touches only task/state ledger files, `infra/public/index.html`, and the focused branding test; no Alembic/migration files are present. Production remains exact `caab6f1825f69635f88724207ccf173b808dafa4`, Alembic 0046. Publishing this verification tag requires a separate explicit production authorization.

- Authorized Search Console verification rollout of `54888ed5797b4b84fa663ff8be11b8b74c64e2a6`: PASS. Local main matched `origin/main`, parent was `6d37e146b789aa940e8a738c6dc5aeb4f80d6870`, and the only path changed from that parent was `CURRENT_TASK.md`. `origin/production` was fast-forwarded non-force from `caab6f1825f69635f88724207ccf173b808dafa4`. No Alembic files changed. `ops/production/deploy.py` exited 0 with `RELEASE_HEAD=54888ed5797b4b84fa663ff8be11b8b74c64e2a6`, `HEALTH=PASS`, `ALEMBIC=0046`, `DB_CONTAINER_UNCHANGED=true`, `DB_VOLUME_UNCHANGED=true`, `ENV_FILE_UNCHANGED=true`, `API_RECREATED=true`, `WORKER_RECREATED=true`, `DEPLOYMENT=PASS`. Rollback was not used. The branding publisher exited 0 with `PUBLIC_WEB=PASS`: home 200, `/privacy` 301, `/privacy/` 200, `/terms` 301, `/terms/` 200, unrelated path 404, and DB container, DB volume, env, API, and worker unchanged during publication. Live `https://web-itx.duckdns.org/` returned 200 with exactly one `google-site-verification` meta tag whose content matched the authorized token. Page body was not printed. No Google Cloud/Search Console mutation, nginx action, DNS/firewall change, or manual SSH/Docker workaround.


- Architect final acceptance of Search Console verification rollout ledger `11b999dcfc2a5bc4b55e29cae1d91d8297441ec4`: ACCEPTED / COMPLETE. Production runtime and canonical `production` ref are aligned at exact release `54888ed5797b4b84fa663ff8be11b8b74c64e2a6`, Alembic `0046`. Schema-neutral deploy PASS: health PASS, DB container/volume and production env unchanged, API/worker recreated, rollback unused. Branding publisher PASS with accepted 200/301/404 route semantics and unchanged DB/API/worker/env identities during publication. The live homepage returns HTTP 200 and contains exactly one `google-site-verification` meta tag with the authorized token. No Google Cloud/Search Console mutation, nginx action, DNS/firewall change, or manual SSH/Docker workaround occurred. The repository remains HOLD. The next step is the human Search Console ownership verification action.


- Google OAuth operator closure: COMPLETE. Branding verification is confirmed by the human as passed in Google Auth Platform, Publishing status is `In production`, a fresh Google OAuth reconnect was completed, and the prior warning banners disappeared. The previously expired/revoked refresh token is no longer the active credential path. No additional Google OAuth operator action is currently required for the personal Secretary workflow. The temporary read-only live-meta diagnostic is superseded by the successful live-tag rollout plus completed Google-side verification and reconnect.


- Architect model-routing decision: the prior GPT-6 selector task is superseded before implementation. Secretary will use one canonical per-user generative model selection (existing `assistant_model` field) across interactive Assistant and all background/reasoning/generative OpenAI workloads. The legacy `OPENAI_MODEL`/global Terra path is to be retired from runtime. Specialized embeddings, STT, and TTS remain separate. Current official GPT-6 family has Luna, Sol, and Astra; there is no `gpt-6-terra`, so the selector target is GPT-6 Luna/Sol/Astra plus GPT-5.6 Luna/Terra/Sol. Production is not authorized to change in this task.

- Unified generative model selector: BUILT / NOT DEPLOYED. Commit `6b22bdc94e5c993a44d6d9b719a1dbbbcbfea529`. `assistant_model` stays the API/DB field. Deployment default and allowlist are `gpt-6-luna`, `gpt-6-sol`, `gpt-6-astra`, `gpt-5.6-luna`, `gpt-5.6-terra`, `gpt-5.6-sol`. Stored allowlisted GPT-5.6 choices stay effective; no override resolves to `gpt-6-luna`. `gpt-6-astra` plus reasoning `none` is rejected on write and in deployment validation; other listed models still accept `none|low|medium|high`. Flutter label is `Модель ИИ`; switching to Astra while reasoning is `none` sends one Astra + `low` update, and Astra hides `none`. Audit: interactive Assistant, proactive review, summarization, conversation-stack summary, auto-label, correlation judge, temporal extractor, and temporal match judge already took `EffectiveUserSettings.assistant_model`. Global `create_openai_summarizer` and `create_correlation_judge` had no runtime callers and were removed. Legacy Secretary construction had no runtime caller; the remaining factory takes the effective per-user model. `settings.openai_model` is gone. Compose forwards the canonical Assistant settings to api and worker and no longer forwards `OPENAI_MODEL`. Embedding, transcription, and TTS defaults are unchanged. Focused backend tests: 9 passed in `test_generative_model_selector.py`; related background and secretary tests passed in the same run (40 total). Flutter account model tests passed. Flutter analyze of the touched account files found no issues. Ruff check passed on the touched Python modules and the new test. `docker compose config --quiet` passed. `git diff --check` was clean. Historical files that were already outside Ruff format were not reformatted. No Alembic migration. Production was not touched and remains `54888ed5797b4b84fa663ff8be11b8b74c64e2a6`.


- Architect review of unified generative model implementation `6b22bdc94e5c993a44d6d9b719a1dbbbcbfea529`: FUNCTIONALLY ACCEPTABLE, verification incomplete. Code review confirms the canonical per-user `assistant_model` is used by the interactive Assistant runtime and the audited background generative factories (proactive review, summarization/conversation-stack summary, auto-label, correlation judge, temporal extractor/match judge, and the remaining legacy Secretary provider factory). `OPENAI_MODEL` is removed from Settings/runtime Compose wiring; api and worker now receive the canonical Assistant model controls. Flutter uses the existing per-user selector labeled `Модель ИИ`; Astra + reasoning `none` is prevented coherently. Official OpenAI API documentation confirms `gpt-6-luna`, `gpt-6-sol`, and `gpt-6-astra` are valid API model IDs, with Astra not supporting `reasoning=none`. No migration files are present in the production-to-main delta. Final DEPLOY-READY acceptance is withheld only because the executor ledger does not explicitly record the task-required `py_compile` and `ruff format --check` PASS results and no CI exists for the implementation SHA. Production remains unchanged.

- Unified generative model verification: RECORDED / NOT DEPLOYED. Worktree was clean. `6b22bdc94e5c993a44d6d9b719a1dbbbcbfea529` is an ancestor of main, and the only paths changed after it are `CURRENT_TASK.md` and `PROJECT_STATE.md`. `py_compile` PASS for all 11 Python files in `6481afec6794206e71d6882b163208b87622a976..6b22bdc94e5c993a44d6d9b719a1dbbbcbfea529`. `ruff format --check` PASS for `backend/app/llm/openai_summarizer.py`, `backend/app/services/secretary_service.py`, and new `backend/tests/test_generative_model_selector.py`. The same check FAIL for `backend/app/api/me.py`, `backend/app/core/assistant_openai_config.py`, `backend/app/core/config.py`, `backend/app/llm/correlation_judge.py`, `backend/app/services/effective_user_settings_service.py`, `backend/tests/test_assistant_cost_config.py`, `backend/tests/test_secretary.py`, and `backend/tests/test_secretary_live.py`. Each of those eight files also failed `ruff format --check` at baseline `6481afec6794206e71d6882b163208b87622a976`, and the reformat body is unchanged aside from line-number shifts, so the implementation did not add a format failure. No formatter rewrite was made. `git diff --check` was clean. `docker compose config --quiet` PASS. No live OpenAI call and no production change. Production remains `54888ed5797b4b84fa663ff8be11b8b74c64e2a6`.


- Architect final acceptance of unified generative model selector: DEPLOY-READY / NOT DEPLOYED. Accepted implementation SHA `6b22bdc94e5c993a44d6d9b719a1dbbbcbfea529`. Verification completion SHA `34d7dcc91210d0e4d26b3d608faf65a086e76c4c` confirms `py_compile` PASS for all 11 changed Python files, `git diff --check` clean, and `docker compose config --quiet` PASS. Ruff format status is accepted as non-regression: the three newly formatted/new files pass, while the eight remaining failures reproduce exactly at baseline `6481afec6794206e71d6882b163208b87622a976` and were not introduced or worsened by this implementation. Post-implementation history changes only `CURRENT_TASK.md` and `PROJECT_STATE.md`; no implementation code changed after `6b22bdc94e5c993a44d6d9b719a1dbbbcbfea529`. Production remains exact `54888ed5797b4b84fa663ff8be11b8b74c64e2a6`, Alembic `0046`. No deployment is authorized by this acceptance.

- Authorized generative model rollout of `36c2ce9f43e56a9554688f50c60a79d56e469fbe`: PASS. Parent was `c455d8caf1d9be18e2d8e37f4e446fc96d9d4e3b`. Read-only preflight before the production ref moved: runtime HEAD `54888ed5797b4b84fa663ff8be11b8b74c64e2a6`, Alembic `0046`, health PASS. `OPENAI_ASSISTANT_MODEL`, `OPENAI_ALLOWED_ASSISTANT_MODELS`, `OPENAI_ASSISTANT_REASONING_EFFORT`, `OPENAI_ASSISTANT_VERBOSITY`, and `OPENAI_ASSISTANT_MAX_OUTPUT_TOKENS` were unset, so the model/allowlist blocker did not fire. `.env` was not edited. `origin/production` was then fast-forwarded non-force from `54888ed5797b4b84fa663ff8be11b8b74c64e2a6` to the release. `ops/production/deploy.py` exited 0 with `RELEASE_HEAD=36c2ce9f43e56a9554688f50c60a79d56e469fbe`, `HEALTH=PASS`, `ALEMBIC=0046`, `DB_CONTAINER_UNCHANGED=true`, `DB_VOLUME_UNCHANGED=true`, `ENV_FILE_UNCHANGED=true`, `API_RECREATED=true`, `WORKER_RECREATED=true`, `DEPLOYMENT=PASS`. Rollback was not used. `GET /me/settings` is NOT VERIFIED: no existing authenticated production harness exposes it, and credentials were not improvised. No live OpenAI call.


- Architect final acceptance of unified generative model production rollout: ACCEPTED / LIVE. Production ref and runtime are exact `36c2ce9f43e56a9554688f50c60a79d56e469fbe`, Alembic `0046`. The authorized rollout passed the normal `ops/production/deploy.py` path with health PASS, DB container unchanged, DB volume unchanged, production `.env` unchanged, API and worker recreated, and rollback unused. Preflight confirmed the production `.env` did not override the canonical Assistant model/allowlist controls, so Compose defaults from the release are active unless a future env change is explicitly made. The implementation therefore ships the unified per-user generative-model routing and six-model selector. Authenticated `GET /me/settings` remains NOT VERIFIED because no existing production harness provides safe authenticated access; this is an observability/test-harness gap, not evidence of a runtime failure. Current task remains HOLD; no further production action is authorized.


- Human-selected pre-next-feature fix round (2026-09-24): add the following to the existing deferred temporal-idempotency and PROJECT_STATE-cleanup backlog. (1) Inbox rapid deletion UX: mobile swipe-to-remove and desktop inline trash-after-Ask-Secretary/Open-in-Graph are already implemented/live, so treat them as regression verification and fix only any current mismatch. (2) Hands-free negative-result feedback: voice-origin failures already have a persistent error state plus local error cue; explicitly reproduce the round-limit/silent-return scenario on mobile/hardware-voice paths and fix only if any terminal failure can still return to listening/idle without one local failure cue. (3) Prompt-injection defense-in-depth: do not build a new filter/security subsystem. Interactive Assistant/finalization, proactive review, and temporal extractor/matcher already classify stored/external content as untrusted data/evidence and forbid following embedded instructions. Extend the same short invariant only to remaining background generative prompts that consume external/object text (notably resource/conversation summarization, correlation judge, and auto-label object payload); preserve all existing approval/tool authorization gates and do not strip source content. (4) Assistant reference-chip presentation: preserve current clickable exact-object references, but replace textual kind prefixes with the shared object-kind icon plus shared provider/source icon, keep the title, and append a subdued compact date `dd.MM.yy` when available. Extend Assistant reference metadata with `provider` plus canonical `primary_at` using the same `object_primary_search_datetime` semantics as Inbox; no date means no date suffix. Existing reference provenance/citation cap rules remain unchanged. (5) Deferred temporal `already_evidenced` nuance is now concretized as a pre-model idempotency/cost issue: repeated jobs for an exact already-active source revision currently reach extraction before returning `already_evidenced`; fix so a provably identical active revision can short-circuit before paid extraction while changed/stale/superseded revisions still run normally. (6) PROJECT_STATE cleanup must be conservative: keep historical ledger facts, repair the current-state summary/header and obvious contradictory/stale current-status duplicates only; no bulk history deletion or archival redesign. This entire round is code/test/docs only; production remains exact `36c2ce9f43e56a9554688f50c60a79d56e469fbe`, Alembic `0046`.


- Clarification to the active pre-next-feature fix round: Inbox Scope B is a real desktop list-card UI fix, not merely regression verification. Mobile swipe-to-remove remains as-is, and the existing delete control inside opened object/detail UI is already correct. On desktop Linux/macOS/Windows, the Inbox LIST card itself must expose a visible trash action in its action row after Ask Secretary and Open in Graph, so multiple messages can be deleted directly while scanning the list. Reuse the existing delete/confirmation API and local-list removal behavior; do not add desktop swipe or a duplicate always-visible mobile trash control. The user-provided Inbox screenshot confirms the current desktop list card lacks this trash action.


- Architect review of pre-next-feature fix round implementation `82b27f9f93af489d086344fc443337d23aecd6a0`: PARTIALLY ACCEPTED, NOT DEPLOY-READY pending one narrow corrective pass. Scope C (hands-free failure cue regression), D (prompt-injection consistency hardening), and F (conservative PROJECT_STATE cleanup) are acceptable as reported. Scope A's pre-model `already_evidenced` short-circuit correctly avoids extractor/judge calls, but its new early return drops the previously returned anchor `hint_id/calendar_id`, which also causes `temporal_signal_result.chosen_hint_id/chosen_calendar_id` to become null on the repeated-revision path; preserve the old outcome metadata while keeping zero paid calls. Scope B correctly adds the desktop Inbox list-card trash, but implementation changed shared `ObjectMetaActionRow` wide layout globally from Row/right-aligned labels to Wrap; that widget is also used by Search and Today, so the fix must be narrowed to Inbox (or be behind an opt-in default-off layout option) rather than silently changing other surfaces. Scope E's provider/date chips are directionally correct, but reference provenance currently opens a separate `SessionLocal()` and performs a second object query even though the exact `get_object` payload already contains provider/date inputs; remove that extra session/query and derive provenance/date from the same referenced-object snapshot using the canonical primary-date rule. Add a narrow-width/long-title chip test so the added icons/date do not create a per-chip overflow. The red Telegram parameter in `test_communication_providers_use_common_temporal_job_path_and_dedup` predates this fix and fails before extraction because current Telegram AI quarantine blocks that legacy Telegram test object; nevertheless, update the stale test to encode the current quarantine invariant so the relevant focused suite is green. Production remains exact `36c2ce9f43e56a9554688f50c60a79d56e469fbe`, Alembic `0046`; no production action is authorized.
- Narrow corrective pass for `82b27f9f93af489d086344fc443337d23aecd6a0` is implemented at `a2a2e85ed5255cdf9fcf57fad15b222d89e946f6`. Early `already_evidenced` again carries `hint_id` or `calendar_id`, and `temporal_signal_result` keeps `chosen_hint_id`/`chosen_calendar_id`. Inbox list cards opt into action wrapping; Search/Today keep the shared wide row with right-aligned labels. Assistant reference provider/date come from the same `get_object` snapshot via the canonical primary-date rule, with no second session. At baseline `73457fec226e113f7f8ddf8768497cafa8e003f1` the Telegram parameter of `test_communication_providers_use_common_temporal_job_path_and_dedup` already failed before extraction with zero jobs; the suite now asserts that quarantined Telegram does not enqueue and keeps the common path green for Mattermost and Teams. Production ref remains `36c2ce9f43e56a9554688f50c60a79d56e469fbe`. Not deployed.


- Architect final acceptance of the pre-next-feature fix round plus corrective pass: DEPLOY-READY / NOT DEPLOYED. Accepted base implementation `82b27f9f93af489d086344fc443337d23aecd6a0` with corrective implementation `a2a2e85ed5255cdf9fcf57fad15b222d89e946f6`. Final review confirms: (A) exact active temporal revisions short-circuit before extractor/match judge while preserving prior `hint_id/calendar_id` outcome and `temporal_signal_result` anchor metadata; (B) desktop Inbox list cards expose the inline trash after Ask Secretary/Open in Graph, mobile retains swipe-only rapid deletion, and wrapping is Inbox opt-in while shared Search/Today wide-row semantics remain unchanged; (C) hands-free terminal failures retain visible error state and exactly one local failure cue with no voice-state rewrite required; (D) background generative prompts consistently treat stored/external/object text as untrusted data/evidence without introducing a second filtering/authorization subsystem; (E) Assistant reference chips preserve exact-object citations and now show kind icon, provider icon, title, and compact date using canonical primary-date semantics from the same `get_object` snapshot, with no second DB session/query and narrow-width overflow covered; (F) PROJECT_STATE current-summary cleanup is conservative. Telegram temporal coverage now encodes the existing MTProto AI quarantine and keeps Mattermost/Teams common-path tests green without enabling Telegram AI. Focused backend/Flutter tests, Python compile, Ruff check, Flutter analyze for touched files, and `git diff --check` passed as reported; remaining Ruff-format/analyze findings are documented pre-existing baseline items. No Alembic migration. Production remains exact `36c2ce9f43e56a9554688f50c60a79d56e469fbe`, Alembic `0046 / 0046`. No deployment is authorized by this acceptance. CURRENT_TASK remains HOLD.
- Pre-next-feature fix-round production rollout: SUCCESS. `origin/production` fast-forwarded non-force from `36c2ce9f43e56a9554688f50c60a79d56e469fbe` to exact release `fe81a13c8887da73b743f5f5c9a4f8830aafa943`. `ops/production/deploy.py` exit 0: RELEASE_HEAD=`fe81a13c8887da73b743f5f5c9a4f8830aafa943`, HEALTH=PASS, ALEMBIC=`0046`, DB_CONTAINER_UNCHANGED=true, DB_VOLUME_UNCHANGED=true, ENV_FILE_UNCHANGED=true, API_RECREATED=true, WORKER_RECREATED=true, DEPLOYMENT=PASS. Rollback unused. Preflight and post-check: runtime HEAD matched the production ref before (`36c2ce9`) and after (`fe81a13`); health HTTP 200; Alembic `0046 (head)`; DB container `2078e90d6202498e809963e5a888604b7043d5b9455cc57b43a86eaedd49e76b` unchanged; DB volume `volume` / `infra_db_data` / `/var/lib/postgresql/data` unchanged; `.env` SHA-256 `6a5132faef6d9a32c19265ab87b1f0f3dce5e273cebcd038f555b29d78fa5ae3` unchanged; API container `650da44f8cc37372ff8f3c3406cca84362184a297d17af5af20e1b385c919b8c` recreated as `da9ba24ef3dc53b8492edfbf3eb6b83c757354f31a19c7b1f517a947409ec904`; worker container `afd3ca87509133c98fec3f28a23e982b04a632402de0db64007c548bc6f2d9af` recreated as `b196712b939dd139f27634e7db8c4dfa0be4b94d562c7a98e8de7d7f15aade3e`. `TELEGRAM_MTPROTO_AI_ENABLED` remained UNSET. No live OpenAI/provider call. No canonical Flutter client distribution path exists in the deploy harness; client UI in this release is source/release-ready and was not installed or distributed to devices.


- Architect acceptance of production rollout `fe81a13c8887da73b743f5f5c9a4f8830aafa943`: ACCEPTED / LIVE. Normal `ops/production/deploy.py` path passed, runtime/ref equal the exact release, health PASS, Alembic `0046 / 0046`, DB container/volume and production `.env` remained unchanged, API/worker were recreated, rollback was unused, Telegram MTProto AI remained UNSET/off, and no live provider calls were made. Canonical deploy does not distribute Flutter clients, so the client-side Inbox/chip/hands-free UI changes in that release are source/release-ready but were not installed on devices by the server rollout.

- Immediate post-rollout micro-fix authorized next: collapsed/expanded Inbox Conversation Stack cards must surface bookmark presence from the exact child messages represented by that rendered stack. This is a Flutter-only derived presentation signal: reuse already-loaded child bookmark state, do not create/persist a bookmark for the stack itself. One unique child bookmark color => one filled bookmark glyph; multiple distinct colors => compact unique-color glyph summary (bounded to avoid layout growth) plus overflow indication/semantics as needed. The aggregate is read-only and must update immediately when a child bookmark is added/changed/cleared.

- Future backlog, not part of the bookmark micro-fix: Conversation Stack label summary. Preferred first implementation is a deterministic deduplicated union of labels already assigned to the represented child messages, rendered through the existing `ObjectLabelStrip` (two visible labels plus existing `+N` overflow behavior). Do not introduce a separate agent/LLM classification for the stack unless later product evidence shows the union semantics are insufficient. The group-level label presentation should mean “this conversation contains messages with these labels”, not “the stack itself was independently classified”.


- Architect final acceptance of Inbox Conversation Stack bookmark visibility micro-fix: ACCEPTED / CLIENT SOURCE READY / NOT INSTALLED ON DEVICES. Accepted implementation SHA `e730f8819854b8ace5a92ce686380cf2c6001a86`. Review confirms the stack indicator is a derived read-only aggregate of bookmark colors from the exact rendered child objects, shows up to three distinct colors plus `+N`, preserves first-occurrence order from the rendered children, remains visible in collapsed and expanded states, updates from the existing bookmark controller without Inbox reload, and does not create/write a bookmark for `stackId`. Focused tests cover no-bookmark, single/multiple/duplicate colors, overflow, live color changes/clear, narrow mobile/desktop widths, and tap behavior; related compaction/bookmark-controller tests passed. No backend/API/DB files changed. Post-implementation history changes only `CURRENT_TASK.md` and the encrypted architect context. Production server remains exact `fe81a13c8887da73b743f5f5c9a4f8830aafa943`, Alembic `0046 / 0046`. Because the canonical server deploy does not distribute Flutter clients, this acceptance does not imply that the UI fix is installed on any phone/desktop device. CURRENT_TASK remains HOLD; no production or client-distribution action is authorized.


- Human desktop acceptance / stage closure: CLOSED. After rebuilding/restarting the current Flutter desktop client from current main, the user manually confirmed the intended client-visible results are present and satisfactory: desktop Inbox inline trash is visible on list cards, Assistant reference-chip presentation changes are visible, and Conversation Stack bookmark aggregation is visible on grouped conversations. The user considers this fix stage complete and approved to close. Server production remains exact `fe81a13c8887da73b743f5f5c9a4f8830aafa943`, Alembic `0046 / 0046`; no new server deploy was required for this manual client verification. Current main is `0f640a11b1818cd8f460fde72367715ca90304f3`. Future backlog remains separate, including Conversation Stack label aggregation, voice-note STT ingestion/transcription, and persistent/resumable Assistant conversations/history.


- Reply-to-sender regression identified after the closed client-fix stage. The imported email data is intact: Gmail/Yandex normalizers persist `metadata.sender`, `metadata.headers["reply-to"]` when present, recipients, subject, message-id/thread metadata. The regression is at the Assistant visibility/routing boundary: `assistant/tool_output.py::_bounded_object()` intentionally strips arbitrary object metadata, so the model can read an email body while not seeing its canonical sender envelope. This must NOT be fixed by re-exposing arbitrary metadata or weakening the untrusted-data/prompt-injection invariant.
- Architecture decision for reply parity: use **reply by exact Object** across communication providers. Existing Mattermost/Telegram/Teams `send_message(reply_to_object_id=...)` remains canonical and server-resolved. Email must gain symmetric object-anchored reply preparation: the model supplies the exact email `reply_to_object_id` plus reply body; backend resolves and freezes recipient using RFC envelope semantics (`Reply-To` first, otherwise `From`), source provider/account, reply subject, and threading headers before creating the approval plan. Never infer a recipient from body/signature text. Compose-new-email remains supported separately.
- Email source-account provenance gap: newly materialized Gmail/Yandex email objects should record their connected source account identity in metadata (no schema migration). Legacy objects should resolve the source account deterministically from connected account vs To/Cc when possible, or a sole eligible account; genuine ambiguity must fail/clarify rather than guess. Selectively expose bounded normalized email sender/reply-to identity in Assistant read output for informational questions, but not provider routing secrets/arbitrary metadata.
- Telegram MTProto AI activation/quarantine policy is unchanged by this work. No provider gate may be relaxed as part of reply parity.


- Architect clarification/correction on prompt-injection handling: **semantic source data must not be hidden merely as an injection defense**. An injection can appear in any source field (body, subject, sender display name, address-like text, filename, label, metadata, URL, etc.), so field stripping is not the security boundary. The universal invariant is control/data separation: every field originating from stored/external/UI/tool object data is untrusted evidence/data only and never an instruction, regardless of how imperative it looks. Only actual system/developer/user instruction channels define intent and authorization. Assistant-visible object projections should preserve normalized user-relevant semantic fields broadly. Exclusions are only for genuine secrets/credentials/auth material, non-user-facing internal sensitive state, or bounded size/cost concerns; those exclusions must not be described as prompt-injection protection.
- Exact-object routing remains the mutation safety boundary: for replies, the model may inspect source metadata but should anchor the action to the exact communication Object; backend deterministically resolves/freeze the canonical route. Thus source content cannot redirect the action even if it contains attacker-controlled addresses/instructions. Approval/action-plan rules remain unchanged.
- Exact-object reply parity implemented at `036e34db44024ffd1fa536409bbc1a75a1ffb98f`. Email reply uses `send_email(reply_to_object_id, body)`; the backend freezes recipient from Reply-To or else From, source account, `Re:` subject, In-Reply-To/References, and Gmail thread id. New Gmail/Yandex objects store `source_account_email` without a migration. Assistant object output keeps bounded semantic envelope fields and hides credential-like metadata. No live provider send. Production remains `fe81a13c8887da73b743f5f5c9a4f8830aafa943`. Telegram MTProto AI gate unchanged.
- Exact-object reply production rollout: SUCCESS. `origin/production` fast-forwarded non-force from `fe81a13c8887da73b743f5f5c9a4f8830aafa943` to exact release `42db393be50a4c3f20ce86dadc280d77bada3959`. `ops/production/deploy.py` exit 0: RELEASE_HEAD=`42db393be50a4c3f20ce86dadc280d77bada3959`, HEALTH=PASS, ALEMBIC=`0046`, DB_CONTAINER_UNCHANGED=true, DB_VOLUME_UNCHANGED=true, ENV_FILE_UNCHANGED=true, API_RECREATED=true, WORKER_RECREATED=true, DEPLOYMENT=PASS. Rollback unused. Runtime HEAD matches the release. Health HTTP 200. Alembic `0046 (head)`. DB container `2078e90d6202498e809963e5a888604b7043d5b9455cc57b43a86eaedd49e76b` unchanged. DB volume `volume` / `infra_db_data` / `/var/lib/postgresql/data` unchanged. `.env` SHA-256 `6a5132faef6d9a32c19265ab87b1f0f3dce5e273cebcd038f555b29d78fa5ae3` unchanged. API container `da9ba24ef3dc53b8492edfbf3eb6b83c757354f31a19c7b1f517a947409ec904` recreated as `be7376bb2b0cb6e8606551d68488f3916dfa0db2e40e27389ca134662ea29abc`. Worker container `b196712b939dd139f27634e7db8c4dfa0be4b94d562c7a98e8de7d7f15aade3e` recreated as `b58bfef7095c614d4e99ca4b9d92bef3047c7b11f8d731ca6d0f50c535b6cc45`. `TELEGRAM_MTPROTO_AI_ENABLED` remained UNSET. No live provider or LLM call. The server deploy does not distribute Flutter clients, so client-source changes in this release history were not installed on devices.


- Architect final acceptance of exact-object reply parity + semantic object visibility: ACCEPTED / DEPLOY-READY / NOT DEPLOYED. Accepted implementation SHA `036e34db44024ffd1fa536409bbc1a75a1ffb98f`; Executor closure/HOLD SHA `c2634d523f67be16e29f3c17475763e366c3c555`. Review confirms `send_email` now has mutually exclusive compose vs exact-object reply modes; email reply preparation loads the exact active user-owned email, resolves recipient only from canonical Reply-To then From/sender, deterministically resolves the source account (including fail-closed legacy multi-account ambiguity), freezes provider/account/recipient/`Re:` subject/`In-Reply-To`/`References`/Gmail thread id before approval, and execution consumes only the frozen canonical payload. Assistant bounded object output now preserves bounded user-relevant semantic metadata while filtering credential/token-like material; system instructions explicitly treat the entire stored object as DATA/evidence rather than instructions. Email reply anchors use the per-turn exact-object allowlist, matching chat reply safety semantics. New Gmail/Yandex materialization records `source_account_email`; no DB migration/schema change was added. Focused reply tests, existing Gmail compose tests, Mattermost/Telegram/Teams reply tests, source-account materialization tests, Ruff/compile, and `git diff --check` were reported PASS. One pre-existing baseline failure remains in `test_safe_external_actions_yandex_parity.py::test_yandex_mail_sent_copy_failure_keeps_smtp_success` (`sent_copy_status` expectation); Executor reports it reproduces on unmodified baseline and this task did not modify that test. Track separately; do not represent the full suite as all-green. No live provider send occurred. Production remains exact `fe81a13c8887da73b743f5f5c9a4f8830aafa943`, Alembic `0046 / 0046`; Telegram MTProto AI activation/quarantine unchanged. CURRENT_TASK remains HOLD; deployment requires separate human authorization.


- Architect final acceptance of exact-object reply production rollout: ACCEPTED / LIVE. Exact server release `42db393be50a4c3f20ce86dadc280d77bada3959` is both `origin/production` and runtime HEAD. Normal `ops/production/deploy.py` completed with DEPLOYMENT=PASS and HEALTH=PASS; Alembic remains `0046 / 0046`; DB container/volume and production `.env` hash were unchanged; API/worker were recreated; rollback was unused; `TELEGRAM_MTPROTO_AI_ENABLED` remained UNSET; no live provider or LLM call was made. Canonical server deploy did not install/distribute Flutter clients. This rollout is closed.
- Next authorized client-only micro-fix is GitHub issue #9: desktop inline delete confirmation, restore wide Inbox labels as a far-right anchor independent of the action wrap, and shorten shared action labels to `Секретарь`, `Граф`, `Удалить`. Mobile deliberate full-swipe delete behavior remains unchanged. No backend/API/DB or production work is part of that task.
- Flutter Inbox safe-delete and compact pinned action-row fix implemented at `8ed63a32cb6017a73064852ac93676aab7112ba0`. Desktop inline delete always confirms through `confirmAndDeleteObject`; mobile deliberate full-swipe is unchanged. Wide Inbox/Today/Search rows pin labels to the right while actions wrap only in the left cluster. Shared visible labels are `Секретарь`, `Граф`, and `Удалить`, with the previous phrases kept as tooltips. Files: `client/lib/inbox/inbox_screen.dart`, `client/lib/objects/object_delete_actions.dart`, `client/lib/ui/object_actions.dart`, `client/lib/ui/object_label_strip.dart`, `client/test/ui/design_quality_pass_a_test.dart`, `client/test/ui/inbox_quick_actions_swipe_remove_a_test.dart`, `client/test/ui/object_meta_action_row_test.dart`. Focused Flutter tests 96 passed, including Inbox delete/swipe, object-delete policy, and Today/Search shared-row coverage; Flutter analyze of touched Dart files reported no issues; `git diff --check` was clean. No backend/API/DB change. Production remains `42db393be50a4c3f20ce86dadc280d77bada3959`, Alembic `0046 / 0046`. No client installation or distribution.


- Architect final acceptance of GitHub issue #9 (Flutter Inbox safe delete + compact pinned action row): ACCEPTED / CLIENT SOURCE READY / NOT INSTALLED ON DEVICES. Accepted implementation SHA `8ed63a32cb6017a73064852ac93676aab7112ba0`; Executor HOLD SHA `0a5e329f990edf922657e0a435ff0ab3e878bd76`. Review confirms desktop inline delete always enters the existing confirmation flow before any DELETE request; Cancel preserves the card/state; Confirm performs exactly the existing Secretary-only delete; supported mobile deliberate full-swipe behavior remains unchanged. Wide `ObjectMetaActionRow` keeps actions in the left expandable/wrapping cluster and labels independently at the far right; shared visible action labels are `Секретарь`, `Граф`, `Удалить`, with previous long wording retained in tooltips. Focused Flutter coverage reports 96 passing tests across Inbox delete/swipe, shared action-row, Today/Search/design-quality coverage; touched-file Flutter analyze has no issues; `git diff --check` is clean. No backend/API/DB files changed. Post-implementation history changes only `CURRENT_TASK.md` and `PROJECT_STATE.md`. Server production remains exact `42db393be50a4c3f20ce86dadc280d77bada3959`, Alembic `0046 / 0046`. Canonical server deploy does not distribute Flutter clients, so this acceptance does not imply installation on a phone/desktop device. CURRENT_TASK remains HOLD.


- Human visual acceptance / stage closure after issue #9: CLOSED. The user rebuilt/restarted the desktop Flutter client and manually confirmed the current visual/interaction package is satisfactory, including the safe desktop delete confirmation, compact action labels, right-pinned label chips, previously accepted Inbox trash/reference-chip changes, and Conversation Stack bookmark visibility. This closes the current stabilization/UI stage. Server production remains exact `42db393be50a4c3f20ce86dadc280d77bada3959`, Alembic `0046 / 0046`. Client-source acceptance does not imply automatic device distribution.
- Architect selected the next major product stage: GitHub issue #8, persistent/resumable Assistant conversations with an explicit New dialog and lightweight conversation history. This is preferred ahead of Telegram voice-note STT because it is a foundational Assistant state model, is not blocked by Telegram AI activation/consent, and will later provide the canonical conversation target for both typed and voice interaction. Conversation Stack label aggregation remains a smaller backlog item.
- Persistent/resumable Assistant conversations MVP implemented at `6acea4d49a175b43b0bb4a187d17e748dfa08a8b`. Migration `0047` adds `assistant_conversations` and `assistant_messages` (additive; downgrade returns to `0046`; not applied in production). One current conversation per user. API: `GET/POST /assistant/conversations`, `GET /assistant/conversations/current`, `POST /assistant/conversations/{id}/select`, `GET /assistant/conversations/{id}/messages`, and persistent `POST /assistant/message` with `conversation_id` + `client_turn_id`. Server-owned history is the last 12 messages; legacy requests without those ids still send client history. Titles are a deterministic 72-character first-user-message truncation, displayed as `Новый диалог` when empty. Unresolved pending action plans block new/select; executed/rejected/expired do not. Resume summaries persist once on the linked conversation. Plan cards rehydrate from the canonical plan row. Flutter restores or creates the current conversation, sends persistent turns with a stable retry id, and shows a wide history panel or a narrow drawer. If the conversation routes are absent, the client keeps the legacy in-memory history path. Focused backend `tests/test_assistant_conversations.py`: 9 passed, including 0046↔0047. Cost-config and cost-guard A (excluding the live test): 30 passed. Existing Flutter `assistant_test.dart`: 6 passed. New Flutter `assistant_conversations_test.dart`: 7 passed. `git diff --check` clean. Ruff/compile of touched Python clean. Flutter analyze of touched Dart files reports one pre-existing warning in `assistant_screen.dart` drop-target null check. `tests/test_assistant_action_plans.py` fails on `ai_traces` foreign key for uncommitted fixture users; the same failure reproduces with the previous `assistant.py` because trace recording uses a separate database session. No live provider call. Production remains `42db393be50a4c3f20ce86dadc280d77bada3959`, Alembic `0046 / 0046`. Telegram MTProto AI gate unchanged.
- Persistent Assistant conversations corrective pass implemented at `890ee7ae449d0b009a029d8c6479072552fa3ee7`. Flutter message loading now passes `before_id` and keeps `has_more`; the Assistant screen shows `Ранее` and prepends the older page in chronological order without duplicates. A transcript of 55 stored messages returns the newest 50, then the older 5 via `before_id`, with no overlap. The following persistent turn still sends the model only the last 12 server messages (`m-043` through `m-054`). A transient bootstrap failure stays retryable and does not emit a legacy `POST /assistant/message`; the next attempt restores the persistent conversation. Route-absent 404 responses still use the legacy send path. `tests/test_assistant_conversations.py`: 10 passed. Flutter `assistant_conversations_test.dart` and `assistant_test.dart`: 16 passed. Ruff/compile of touched Python passed. `git diff --check` clean. Flutter analyze of touched Dart files still reports the pre-existing `assistant_screen.dart` drop-target null-check warning. No live provider call. Migration `0047` was not applied in production. Production remains `42db393be50a4c3f20ce86dadc280d77bada3959`, Alembic `0046 / 0046`. Telegram MTProto AI gate unchanged.
- Assistant persistent-session isolation fix implemented at `5711f47f63b0a3c7222a618833554327756a5b8e`. `resetSession()` clears conversation id, list, transcript, older-page flags, persistent mode, bootstrap/legacy latches, and the switch notice, and bumps a session generation. Bootstrap, paging, new/select, send, and action-plan continuations ignore completions from a previous generation, so a late response cannot restore the previous user's conversation. Flutter `assistant_conversations_test.dart` and `assistant_test.dart`: 19 passed, including logout clearing persistent state, a fresh bootstrap of conversation B after reset, and stale bootstrap/send completions that do not restore conversation A. `git diff --check` clean. Flutter analyze of touched Dart files still reports only the pre-existing `assistant_screen.dart` drop-target null-check warning. No backend change. No live provider call. Migration `0047` was not applied in production. Production remains `42db393be50a4c3f20ce86dadc280d77bada3959`, Alembic `0046 / 0046`. Telegram MTProto AI gate unchanged.
- Assistant stale voice transcription session fix implemented at `b1331596144354aaea3a8c31e69869b50410aab4`. `VoiceTranscriptionController.reset()` bumps a transcription generation, so a `transcribeAudio` completion from the previous auth session does not change voice state, set an error, call the transcript consumer, or report an authentication failure. `AssistantController.resetSession()` also drops the in-flight voice-stop future, so the next session can record and transcribe without waiting for that completion. A late transcript does not call `POST /assistant/message` and does not append the previous user's text. Flutter `voice_recorder_session_test.dart`, `voice_timeout_test.dart`, `assistant_conversations_test.dart`, and `assistant_test.dart`: 32 passed. Flutter `voice_assistant_a_test.dart`, `voice_short_wav_transcription_test.dart`, `assistant_voice_test.dart`, and `hardware_voice_shell_test.dart`: 53 passed. `git diff --check` clean. Flutter analyze of the touched Dart files found no issues. No backend change. No live provider call. Migration `0047` was not applied in production. Production remains `42db393be50a4c3f20ce86dadc280d77bada3959`, Alembic `0046 / 0046`. Telegram MTProto AI gate unchanged.

- Architect final acceptance of persistent/resumable Assistant conversations: ACCEPTED / DEPLOY-READY / NOT DEPLOYED. Accepted implementation chain: MVP `6acea4d49a175b43b0bb4a187d17e748dfa08a8b`, pagination/bootstrap corrective pass `890ee7ae449d0b009a029d8c6479072552fa3ee7`, auth-session isolation `5711f47f63b0a3c7222a618833554327756a5b8e`, stale voice-transcription isolation `b1331596144354aaea3a8c31e69869b50410aab4`; Executor HOLD SHA `36285295dbbc7946e694854e3d95a3da177be30b`. Review confirms server-owned persistent per-user conversations/messages, bounded 12-message model context, paginated UI transcript, stable turn idempotency, New dialog/history switching, pending-plan safety, typed/voice parity, retryable transient bootstrap, old-server compatibility, strict client auth-session reset, and stale bootstrap/send/action-plan/voice-transcription completions not crossing into a later auth session. Focused backend conversation coverage previously passed 10 tests; latest Flutter voice/session/conversation coverage reports 32 + 53 passing tests; touched-file Flutter analyze is clean; `git diff --check` is clean. Migration `0047` remains unapplied in production. Production remains exact `42db393be50a4c3f20ce86dadc280d77bada3959`, Alembic `0046 / 0046`. No live provider call occurred. Telegram MTProto AI activation/quarantine is unchanged. Production rollout of migration `0047` and the feature requires separate explicit human authorization; until then CURRENT_TASK remains HOLD and issue #8 remains open as the rollout/acceptance tracker.
- Assistant `0046 -> 0047` migration harness implemented at `748949f5816c3eb6f1ec031d42b887a73c205a65`. Dedicated entrypoints `ops/production/migrate_assistant_0047.py` and `ops/production/remote_migrate_assistant_0047.py` accept only release `296b4735f9473ea60ef22f1827ed94260603128e`, rollback `42db393be50a4c3f20ce86dadc280d77bada3959`, and Alembic `0046 -> 0047`. The migration delta must be exactly added `backend/alembic/versions/0047_assistant_conversations.py` with unchanged Alembic infrastructure. The historical `0041 -> 0046` harness is unchanged. Post-cutover downgrade is allowed only when `assistant_messages` and `assistant_conversations` are both proven empty; otherwise the harness stops api/worker, does not downgrade, and emits `BREAK_GLASS_REQUIRED=true`. `tests/test_assistant_0047_migration_harness.py`: 23 passed. Ruff and compile of touched Python passed. `git diff --check` clean. No production connection, no `origin/production` move, no production migration, and no deploy. Production remains `42db393be50a4c3f20ce86dadc280d77bada3959`, Alembic `0046 / 0046`. Telegram MTProto AI gate unchanged.
- Assistant conversations production rollout: PASS. `origin/production` fast-forwarded from `42db393be50a4c3f20ce86dadc280d77bada3959` to exact release `296b4735f9473ea60ef22f1827ed94260603128e`. Dedicated harness `ops/production/migrate_assistant_0047.py` exited 0 with `ASSISTANT_MIGRATION_DEPLOYMENT=PASS`, `ALEMBIC=0047`, `DB_CONTAINER_UNCHANGED=true`, `DB_VOLUME_UNCHANGED=true`, `ENV_FILE_UNCHANGED=true`, `API_RECREATED=true`, `WORKER_RECREATED=true`. Rollback was unused. No `BREAK_GLASS_REQUIRED`. No live provider/LLM call. Telegram MTProto AI activation/quarantine was not changed. Production runtime/ref is `296b4735f9473ea60ef22f1827ed94260603128e`, Alembic `0047 / 0047`.

- Human acceptance of persistent/resumable Assistant conversations: CLOSED / LIVE. After the production `0046 -> 0047` rollout and rebuilding/installing the current Flutter desktop client, the user manually verified that Assistant chats persist, receive deterministic titles, remain reopenable from history, survive full application restart, and continue with the prior thread context after reopening. The user explicitly considers the feature working well. This closes GitHub issue #8 as a delivered product stage. Production remains exact `296b4735f9473ea60ef22f1827ed94260603128e`, Alembic `0047 / 0047`, health PASS; no additional server deploy is required for this acceptance. CURRENT_TASK remains HOLD while the next major product stage is selected with the user.
- Graph Refined P1 Person identity foundation implemented at `3c773db48b8bc0f27a0f6896f94295bfbc5781b1`. Migration `0048` adds `person_identities` (additive; downgrade returns to `0047`; not applied in production). A Person is an Object with `kind="person"`. Exact identities cover case-folded email, Mattermost server-scoped user id and username, Teams tenant-scoped user id, and Telegram MTProto account-scoped numeric sender peer id. The same active identity cannot attach to two People of one user; another Secretary user may hold the same external identifier independently. Display-name equality does not resolve or merge. Malformed values fail closed. Metadata extractors return evidence only and do not mutate source Objects. Detach/reassign is explicit; rejecting or deleting a source message does not cascade-delete the identity. `tests/test_person_identity.py`: 10 passed, including `0047 -> 0048 -> 0047` downgrade/upgrade. Ruff and compile of touched Python passed. `git diff --check` clean. No fuzzy matching, UI, send-by-person, or live provider/LLM call. Production remains `296b4735f9473ea60ef22f1827ed94260603128e`, Alembic `0047 / 0047`. Telegram MTProto AI gate unchanged.
- Graph Refined P1R person-evidence type safety implemented at `92aef7e6c9fb2e5acd6afc7056b26ea67ea99d07`. `telegram_user_id` accepts only a strictly positive user id; zero and negative Telegram sender peers fail closed and emit no Person evidence. Newly normalized Teams messages persist `sender_kind` (`user` or `application`); Person evidence emits a Teams identity only when `sender_kind == "user"`. Extraction is anchored to the source Object provider and kind, so coincidental metadata cannot create a cross-provider identity. MTProto storage and Teams presentation/send/reply behavior are unchanged. `tests/test_person_identity.py`: 12 passed. `tests/test_communication_temporal_parity_a.py`: 5 passed. Ruff and compile of touched Python passed. `git diff --check` clean. Migration `0048` was not applied in production. Production remains `296b4735f9473ea60ef22f1827ed94260603128e`, Alembic `0047 / 0047`. Telegram MTProto AI gate unchanged.

- Architect final acceptance of Graph Refined P1/P1R: ACCEPTED / FOUNDATION READY / NOT DEPLOYED. Accepted implementation SHAs: P1 `3c773db48b8bc0f27a0f6896f94295bfbc5781b1`, P1R `92aef7e6c9fb2e5acd6afc7056b26ea67ea99d07`; Executor HOLD `4662a7eeeb3e98dde0c8a5ff9eff5ff609561cf1`. Canonical Person remains an existing user-owned graph Object with `kind="person"`; exact provider identities are normalized and uniquely scoped per user/provider/type/realm; display names do not merge people; cross-user isolation and explicit detach/reassign remain intact. P1R closes provider-type safety: Telegram Person evidence accepts only strictly positive user peer ids; Teams messages persist `sender_kind` and only `user` may yield Person evidence; evidence extraction is anchored to canonical source Object provider/kind, preventing cross-provider identities from coincidental metadata. Reported focused checks: 17 passed; Ruff/compile and `git diff --check` clean. Migration `0048` remains unapplied in production. Production remains exact `296b4735f9473ea60ef22f1827ed94260603128e`, Alembic `0047 / 0047`. Telegram MTProto AI gate unchanged.
- Graph Refined P2 explainable identity evidence and candidate scoring implemented at `c74571132b643a816a2069d0ce6708c128a8dba4`. Migration `0049` adds `person_identity_evidence` (additive; downgrade returns to `0048`; not applied in production). Active evidence rows derive a bounded score. `user_confirmed` yields confirmed, `user_rejected` vetoes weaker positives, and route choice stays below confirmed. Identical provenance does not inflate the score. Retraction keeps the prior row. Name similarity only proposes candidates. The new service does not attach identities or merge People. Deleting a source object clears `source_object_id` and keeps the evidence row. `tests/test_person_evidence_ledger.py`: 4 passed. `tests/test_person_identity.py`: 12 passed. Combined focused run: 16 passed. Ruff and compile of touched Python passed. `git diff --check` clean. No UI, send-by-person, or live provider/LLM call. Production remains `296b4735f9473ea60ef22f1827ed94260603128e`, Alembic `0047 / 0047`. Telegram MTProto AI gate unchanged.
- Graph Refined P2R evidence ownership and active-candidate safety implemented at `feaa7becb4b006057f2276a53b22a357c9a34891`. A linked `person_identity_id` must be an active identity of the same target Person. A later detach leaves the evidence row readable and blocks a new link to that rejected identity. Exact resolution and name candidates skip rejected or deleted People and rejected identity display values. Migration `0049` is unchanged. `tests/test_person_evidence_ledger.py`: 6 passed. `tests/test_person_identity.py`: 12 passed. Combined focused run: 18 passed. Ruff and compile of touched Python passed. `git diff --check` clean. No P3, salience, UI, auto-merge, or live provider/LLM call. Production remains `296b4735f9473ea60ef22f1827ed94260603128e`, Alembic `0047 / 0047`. Telegram MTProto AI gate unchanged.

- Architect final acceptance of Graph Refined P2/P2R: ACCEPTED / EVIDENCE FOUNDATION READY / NOT DEPLOYED. Accepted implementation SHAs: P2 `c74571132b643a816a2069d0ce6708c128a8dba4`, P2R `feaa7becb4b006057f2276a53b22a357c9a34891`; Executor HOLD `7c6a51bcab6746e1a6fe8b78fb9c15b96ccc7891`. Migration `0049` adds the append/history-friendly `person_identity_evidence` ledger. Candidate confidence is derived from active explainable rows; explicit confirm/reject, route-choice, replay dedupe, retraction/history, and contradictory evidence semantics are preserved without auto-attach or merge. P2R enforces that a linked `person_identity_id` is an active identity of the same target Person and same tuple, while historical evidence remains readable after later detach. Exact and name candidate resolution now excludes rejected/deleted People and rejected identities. Reported focused checks: `tests/test_person_evidence_ledger.py` 6 passed; `tests/test_person_identity.py` 12 passed; combined 18 passed; Ruff/compile and `git diff --check` clean. Migrations `0048/0049` remain unapplied in production. Production remains exact `296b4735f9473ea60ef22f1827ed94260603128e`, Alembic `0047 / 0047`.
- Graph Refined P3 Person salience foundation implemented at `3402cb2f450a036efc6ce4a6415441b43947a31e`. Salience is derived at read time for existing active People and their active exact identities. Components are directness, reciprocity, capped frequency, recency decay, capped public exposure, P2 route-choice/confirmation attention, and an existing task or calendar edge. A noisy public author stays below a less frequent direct reciprocal contact. Unknown senders do not create People. Rejected, deleted, and cross-user People contribute nothing. The result records that salience does not decide object importance. No migration. `tests/test_person_salience.py`: 4 passed. `tests/test_person_evidence_ledger.py`: 6 passed. `tests/test_person_identity.py`: 12 passed. Combined focused run: 22 passed. Ruff and compile of touched Python passed. `git diff --check` clean. No UI, proactive, Assistant, or Task Graph integration, and no live provider/LLM call. Production remains `296b4735f9473ea60ef22f1827ed94260603128e`, Alembic `0047 / 0047`. Telegram MTProto AI gate unchanged.
- Graph Refined P3R salience attribution and noise-starvation safety implemented at `52928587b8ff4f5586b216e8ffb989cc28a50c3d`. Gmail and Yandex direction come from SENT/INBOX or sent/inbox folder metadata; an inbound recipient is not treated as an outbound interaction, and ambiguous mail does not create reciprocity. Multi-recipient or CC mail is not 1:1. The read scans up to 200 recent messages in the 90-day window and keeps direct and public hits under separate per-Person caps, so newer unrelated traffic does not erase a direct contact. Ranking uses People who have interaction, attention, or a task/calendar link, not the oldest created People. Teams outbound and application senders fail closed. Mattermost direct and public inbound stay exact-identity only. No migration. `tests/test_person_salience.py`: 7 passed. `tests/test_person_evidence_ledger.py`: 6 passed. `tests/test_person_identity.py`: 12 passed. Combined focused run: 25 passed. Ruff and compile of touched Python passed. `git diff --check` clean. No P4, UI, or live provider/LLM call. Production remains `296b4735f9473ea60ef22f1827ed94260603128e`, Alembic `0047 / 0047`. Telegram MTProto AI gate unchanged.
- Graph Refined P3R2 salience ranking-pool integrity implemented at `37130c3468418fefa2231094d7b28ced5415bd42`. `rank()` builds its pool from three bounded sources: exact identities referenced by the 90-day communication scan, explicit confirmations ahead of newer route choices, and newest active Person-task/calendar edges. Older identities, older route choices, and unrelated edges no longer occupy those slots. Truncation stays explicit. Rejected, deleted, and cross-user People stay out. `evaluate()` scan and per-Person caps are unchanged. No migration. `tests/test_person_salience.py`: 8 passed. `tests/test_person_evidence_ledger.py`: 6 passed. `tests/test_person_identity.py`: 12 passed. Combined focused run: 26 passed. Ruff and compile of touched Python passed. `git diff --check` clean. No P4, UI, or live provider/LLM call. Production remains `296b4735f9473ea60ef22f1827ed94260603128e`, Alembic `0047 / 0047`. Telegram MTProto AI gate unchanged.
- Graph Refined P3R3 salience candidate dedupe and scoring consistency implemented at `34d96e4b66b4eb92aceb8b0aebac96d635fd9c5c`. Attention and task/calendar candidate bounds count distinct People. Each Person is represented by its strongest newest feedback or newest relevant edge. Duplicate rows for one Person do not hide another Person, and truncation means more distinct People than the bound. Scoring reads existence of attention and task/calendar links for every Person already in the pool, so the reason for admission remains visible in the components. No migration. `tests/test_person_salience.py`: 9 passed. `tests/test_person_evidence_ledger.py`: 6 passed. `tests/test_person_identity.py`: 12 passed. Combined focused run: 27 passed. Ruff and compile of touched Python passed. `git diff --check` clean. No P4, UI, or live provider/LLM call. Production remains `296b4735f9473ea60ef22f1827ed94260603128e`, Alembic `0047 / 0047`. Telegram MTProto AI gate unchanged.

- Architect final acceptance of Graph Refined P3/P3R/P3R2/P3R3: ACCEPTED / PERSON SALIENCE FOUNDATION READY / NOT DEPLOYED. Accepted implementation chain: P3 `3402cb2f450a036efc6ce4a6415441b43947a31e`, P3R `52928587b8ff4f5586b216e8ffb989cc28a50c3d`, P3R2 `37130c3468418fefa2231094d7b28ced5415bd42`, P3R3 `34d96e4b66b4eb92aceb8b0aebac96d635fd9c5c`; Executor HOLD `e6a67a48d70f1d25e8719ff67f818e229afa5eec`. Salience is derived at read time for existing active People only, from exact identities and bounded recent interaction/attention/task-calendar evidence. Direction-safe email semantics, direct/public distinction, reciprocity, recency/frequency caps, noisy-channel protection, bounded 90-day communication scan, distinct-Person candidate pools, deterministic attention/task ordering, and pool/scoring consistency are in place. Unknown senders do not create People; rejected/deleted/cross-user People and identities are excluded. Salience remains a processing/ranking feature only and does not claim object importance. No migration was added. Latest reported focused checks: `tests/test_person_salience.py` 9 passed; `tests/test_person_evidence_ledger.py` 6 passed; `tests/test_person_identity.py` 12 passed; combined 27 passed; Ruff/compile and `git diff --check` clean. Production remains exact `296b4735f9473ea60ef22f1827ed94260603128e`, Alembic `0047 / 0047`; development migrations `0048/0049` remain unapplied.
- Graph Refined P4 bounded identity enrichment orchestration implemented at `a0bfa2bc256cae628d492bce0a3562f624e48b76`. The planner ranks already-known People by salience and explicit confirmation, records idempotent exact evidence from stored communication, and reports missing provider categories without inventing identities. Exact attach is limited to an identity that already belongs to the Person or to explicit `user_confirmed` evidence. Display-name similarity stays a confirmation need. An identity owned by another Person fails closed. Unknown public authors do not become People. Scans and candidate lists are bounded, and truncation is explicit. No migration and no live provider, directory, or model call. `tests/test_person_enrichment.py`: 12 passed. `tests/test_person_salience.py`: 9 passed. `tests/test_person_evidence_ledger.py`: 6 passed. `tests/test_person_identity.py`: 12 passed. Combined focused run: 39 passed. Ruff and compile of touched Python passed. `git diff --check` clean. No UI, auto-merge, or P5. Production remains `296b4735f9473ea60ef22f1827ed94260603128e`, Alembic `0047 / 0047`. Telegram MTProto AI gate unchanged.
- Graph Refined P4R confirmation-conflict safety implemented at `f6f255ea4fbd0e873afdab29297a88a50d2abbab`. Several active `user_confirmed` rows for one identity stay an explicit `multiple_user_confirmations` conflict. The planner does not attach, does not treat the confirmations as absent, and does not rewrite the evidence ledger. An existing owner stays in place, including when the owner and another Person are both confirmed and when two other People are confirmed. Retracting down to one active confirmation restores the single-confirmation attach path. Rejected, deleted, and cross-user confirmations do not count. Repeated planning is stable. `tests/test_person_enrichment.py`: 19 passed. `tests/test_person_salience.py`: 9 passed. `tests/test_person_evidence_ledger.py`: 6 passed. `tests/test_person_identity.py`: 12 passed. Combined focused run: 46 passed. Ruff and compile of touched Python passed. `git diff --check` clean. No migration, UI, auto-merge, or P5. Production remains `296b4735f9473ea60ef22f1827ed94260603128e`, Alembic `0047 / 0047`. Telegram MTProto AI gate unchanged.

- Architect final acceptance of Graph Refined P4/P4R: ACCEPTED / BOUNDED ENRICHMENT ORCHESTRATION READY / NOT DEPLOYED. Accepted implementation SHAs: P4 `a0bfa2bc256cae628d492bce0a3562f624e48b76`, P4R `f6f255ea4fbd0e873afdab29297a88a50d2abbab`; Executor HOLD `a711bf425b37ed0bfb578444252195b5e73247c3`. The enrichment planner uses stored exact provider facts, P2 evidence, and P3 salience to decide which already-known People/candidate identities merit further resolution. It does not create People for unknown public authors, does not merge People, does not attach on name/score alone, and performs no live provider/directory/model calls. Explicit single `user_confirmed` may authorize exact attach; conflicting ownership fails closed. P4R preserves multiple active confirmations as explicit `multiple_user_confirmations` ambiguity, never treats them as absent, never reassigns the existing owner, and restores normal single-confirmation behavior after explicit retraction/rejection leaves one active confirmation. Rejected/deleted/cross-user confirmations are excluded. Latest reported checks: `tests/test_person_enrichment.py` 19 passed; `tests/test_person_salience.py` 9 passed; `tests/test_person_evidence_ledger.py` 6 passed; `tests/test_person_identity.py` 12 passed; combined 46 passed; Ruff/compile and `git diff --check` clean. No migration. Production remains exact `296b4735f9473ea60ef22f1827ed94260603128e`, Alembic `0047 / 0047`; development migrations `0048/0049` remain unapplied. Telegram MTProto AI gate unchanged.
